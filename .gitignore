# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Compiled binaries
/pergo
pergo.test

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories
# vendor/

# Go workspace file
go.work
go.work.sum

# env / configurations
.env
.env.*
!.env.example

# Local SQLite databases (if any are generated in the future)
*.db
*.sqlite
tmp/
