---
requirements: ["WABA-01"]
depends_on: []
must_haves: []
---
# Phase 25: Implement JSON-to-Protobuf mapping for rich interactive messages (hybrid approach)

<threat_model>
- **ASVS Level:** 1
- **Block On:** high
- **Description:** 
  - **Injection (High):** Processing raw JSON for `channel_overrides` poses an injection risk if passed directly to unsanitized sinks. For WABA (`whatsapp_cloud`), passing raw JSON directly as an HTTP body payload requires ensuring proper content types are maintained. For `whatsmeow`, decoding using `protojson.Unmarshal` safely mitigates malformed payloads but requires robust error handling.
  - **Denial of Service (Medium):** Complex interactive payloads or deep nested structures in overrides might cause excessive CPU consumption during mapping or `protojson` parsing. Downstream components should bound the size of degraded plaintext fallback. Existing Echo layer HTTP body size limits offer primary mitigation.
</threat_model>

<task>
<files>
- internal/domain/message.go
- internal/domain/message_test.go
</files>
<action>
- Define unified structures: `Interactive` (with `Type`, `Header`, `Body`, `Footer`, `Action`) and sub-structures like `Action` and `Button` in `internal/domain/message.go`.
- Extend `CreateMessageRequest` and `QueueMessage` structs to include:
  - `Interactive *Interactive`
  - `ChannelOverrides map[string]json.RawMessage`
  - `FallbackBehavior string`
- Update `ValidateMessage()` to ensure `FallbackBehavior` is either empty, `"degrade"`, or `"fail"`. Ensure structural integrity of `Interactive` without enforcing channel-specific limits (D-01).
- Add unit tests in `internal/domain/message_test.go` for `FallbackBehavior` and structural validation logic, verifying that limits (e.g. 4+ buttons) are correctly allowed to pass the gateway layer.
</action>
<verify>
<automated>
go test ./internal/domain/... -v
</automated>
</verify>
<done>
- Domain structures are defined.
- `CreateMessageRequest` and `QueueMessage` include new fields.
- Validation successfully allows channel-specific limits to pass while catching structure and fallback logic errors.
- All unit tests pass.
</done>
</task>

<task>
<files>
- internal/outbound/processor.go
- internal/channel/dispatcher.go
- internal/platform/queue/orchestrator.go
</files>
<action>
- In `internal/outbound/processor.go`, update `Processor.Ingest` to map `Interactive`, `ChannelOverrides`, and `FallbackBehavior` from `CreateMessageRequest` to `QueueMessage` before publishing to NATS.
- In `internal/channel/dispatcher.go`, update `MessagePayload` to include `Interactive`, `ChannelOverrides`, and `FallbackBehavior`.
- In `internal/platform/queue/orchestrator.go`, update `dispatchToChannel` to map the new fields from `domain.QueueMessage` to `channel.MessagePayload` during dispatcher delegation.
</action>
<verify>
<automated>
go test ./internal/outbound/... ./internal/channel/... ./internal/platform/queue/... -v
</automated>
</verify>
<done>
- New message fields propagate completely from ingestion to the channel dispatcher payload.
</done>
</task>

<task>
<files>
- internal/channel/whatsapp/waba.go
- internal/channel/whatsapp/waba_test.go
</files>
<action>
- In `internal/channel/whatsapp/waba.go`, implement full override handling: if `channel_overrides.whatsapp_cloud` is present, inject the raw JSON directly as the WABA API HTTP payload (D-02).
- Implement unified mapping: if no override is present and `Interactive` exists, map it into Meta's standard `"type": "interactive"` JSON structure.
- In `internal/channel/whatsapp/waba_test.go`, add tests to ensure unified schema maps to valid Meta interactive JSON, and that `channel_overrides.whatsapp_cloud` completely replaces the payload.
</action>
<verify>
<automated>
go test ./internal/channel/whatsapp/... -v
</automated>
</verify>
<done>
- WABA adapter maps unified schema correctly.
- Overrides for WABA bypass unified mapping and inject directly.
- Tests pass.
</done>
</task>

<task>
<files>
- internal/channel/whatsapp/adapter.go
- internal/channel/whatsapp/adapter_test.go
</files>
<action>
- In `internal/channel/whatsapp/adapter.go`, implement override handling: if `channel_overrides.whatsapp` is present, use `protojson.Unmarshal` to parse the raw JSON directly into a `waE2E.Message`.
- Implement unified mapping: convert `Interactive` into the appropriate `waE2E.Message` structures (`waE2E.ButtonsMessage`, `waE2E.ListMessage`, etc.).
- Implement degradation & fallback (D-03): check native limits (e.g., max 3 buttons). 
  - If violated and `FallbackBehavior == "fail"`, return a `TerminalError`.
  - If `FallbackBehavior == "degrade"`, elegantly degrade the interactive elements into a plain-text payload (e.g., text body + numbered list) and clear the interactive components before dispatch.
- In `internal/channel/whatsapp/adapter_test.go`, write test cases asserting `protojson` unmarshaling for overrides. Verify unified `Interactive` schema translates accurately into `waE2E.Message` variants. Assert that `fallback_behavior == "degrade"` correctly transforms payloads when native limits are exceeded, and `"fail"` emits `TerminalError`.
</action>
<verify>
<automated>
go test ./internal/channel/whatsapp/... -v
</automated>
</verify>
<done>
- Whatsmeow adapter accurately transforms unified schemas to `waE2E.Message`.
- Fallback logic behaves as specified.
- Overrides effectively bypass logic via `protojson.Unmarshal`.
- Tests pass.
</done>
</task>
