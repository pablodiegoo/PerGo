---
phase: 24.1
slug: close-gap-wire-typebot-forwarder-and-reconcile-form-schema
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-19
---

# Phase 24.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `go test` (stdlib) |
| **Config file** | `go.mod` |
| **Quick run command** | `go test ./cmd/pergo/... ./internal/api/handler/... ./internal/inbound/... ./internal/integration/typebot/... ./internal/api/handler/admin/...` |
| **Full suite command** | `go test ./... -race -count=1` |
| **Estimated runtime** | ~10s (integration tests touch real DB) |

---

## Sampling Rate

- **After every task commit:** `go test ./cmd/pergo/... ./internal/api/handler/... ./internal/inbound/... ./internal/integration/typebot/... ./internal/api/handler/admin/...`
- **After every plan wave:** `go test ./... -race -count=1`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~10s

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 24.1-01-01 | 01 | 1 | TYPE-04 | T-24.1-01 | `typebotForwarder != nil` after composition root wiring | unit (composition) | `go test ./cmd/pergo/... -run TestCompositionRoot_AllIntegrationsWired -v` | ❌ W0 | ⬜ pending |
| 24.1-01-02 | 01 | 1 | TYPE-01 | T-24.1-02 | Form submission persists `typebot.Config` shape (per-bot `public_token`, `trigger_words []string`, `connection_id`, `is_default`) | unit (admin) | `go test ./internal/api/handler/admin/ -run TestTypebotSettings -v` | ❌ W0 | ⬜ pending |
| 24.1-01-03 | 01 | 1 | TYPE-01 | T-24.1-02 | GET handler round-trips stored `typebot.Config` | unit (admin) | `go test ./internal/api/handler/admin/ -run TestTypebotSettings_Get -v` | ❌ W0 | ⬜ pending |
| 24.1-01-04 | 01 | 1 | TYPE-01 | T-24.1-03 | `typebot.Config` JSON round-trips losslessly | unit (integration) | `go test ./internal/integration/typebot/ -run TestConfigRoundTrip -v` | ❌ W0 | ⬜ pending |
| 24.1-01-05 | 01 | 1 | TYPE-02 | T-24.1-04 | Valid webhook payload → publisher receives `messages.outbound` message | integration (handler) | `go test ./internal/api/handler/ -run TestTypebotWebhookHandler_HappyPath -v` | ❌ W0 | ⬜ pending |
| 24.1-01-06 | 01 | 1 | Regression | — | Existing `TestDefaultInboundRouter_Route` still passes after wiring change | unit (inbound) | `go test ./internal/inbound/ -run TestDefaultInboundRouter_Route -v` | ✅ | ⬜ pending |
| 24.1-01-07 | 01 | 1 | Regression | — | Existing `TestTypebotForwarder/*` unit tests still pass | unit (integration) | `go test ./internal/integration/typebot/ -v` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `internal/api/handler/typebot_webhook_test.go` — extend to cover happy path (parse payload → resolve connection → resolve customer identity from `contact_identities` → publish to NATS `messages.outbound`)
- [ ] `internal/api/handler/admin/typebot_test.go` (NEW) — unit tests for GET (loads stored config) and POST (validates required fields including `bot_public_token`, `connection_id`; builds correct `typebot.Config`; persists via `IntegrationRepository.Save`)
- [ ] `internal/integration/typebot/config_test.go` (NEW) — round-trip test: marshal `typebot.Config` with all fields populated, unmarshal, assert equality
- [ ] `cmd/pergo/main_test.go` (NEW) — composition-root smoke test asserting both `chatwootSyncer != nil` and `typebotForwarder != nil` after wiring
- [ ] **Refactor Wave 0:** extract `buildInboundRouter(pool, encryptor, publisher)` helper from `main.go` so the smoke test can call it. Without the helper, the test would need to repeat the full main.go setup or use reflection.

If Wave 0 is deferred: the test map above must be filled before the phase can pass `/gsd-verify-work`.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| (none) | — | — | — |

*All phase behaviors have automated verification.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
