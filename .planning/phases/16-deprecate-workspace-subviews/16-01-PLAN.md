---
phase: 16
plan: 01
mode: standard
depends_on: [15]
files_modified:
  - internal/api/handler/admin/waba_template.go
  - cmd/pergo/main.go
  - templates/pages/workspaces.templ
  - internal/api/handler/admin/workspace.go
  - internal/api/handler/admin/waba_template_test.go
  - internal/api/handler/admin/workspace_test.go
autonomous: true
---

# Plan 01: Deprecating Redundant Workspace Subviews

Simplify workspace settings views by deprecating duplicate channel credentials configurations, migrating WABA templates synchronization to pull credentials directly from active connection objects.

## Wave 1: Decoupling WABA Template Handler

### Task 1.1: Refactor WABA Template Handler Credentials Source
- **`<read_first>`**:
  - `internal/api/handler/admin/waba_template.go`
  - `.planning/phases/16-deprecate-workspace-subviews/16-RESEARCH.md`
- **`<action>`**:
  - In `WABATemplateHandler` struct, replace `CredsRepo *repository.CredentialsRepository` with `ConnectionsRepo *repository.ConnectionRepository`.
  - In `NewWABATemplateHandler`, update constructor parameters to receive `connectionsRepo *repository.ConnectionRepository`.
  - In `Create` (line 107) and `Sync` (line 222) methods, retrieve credentials from `h.ConnectionsRepo` by calling `ListByWorkspace`. Iterate to select the first active connection where `conn.Channel == "whatsapp_cloud"`.
- **`<acceptance_criteria>`**:
  - `waba_template.go` no longer imports or references `repository.CredentialsRepository`.
  - `waba_template.go` resolves WABA configuration JSON using `connectionsRepo.ListByWorkspace` and successfully extracts Token and Account ID.

### Task 1.2: Update Main.go Injection and Template Tests
- **`<read_first>`**:
  - `cmd/pergo/main.go`
  - `internal/api/handler/admin/waba_template_test.go`
- **`<action>`**:
  - In `cmd/pergo/main.go` line 270, update `admin.NewWABATemplateHandler` call to pass `connectionRepo` instead of `credentialsRepo`.
  - In `internal/api/handler/admin/waba_template_test.go`, update mocks and handlers setup. Set up test calls to verify templates sync when a mock connection is present.
- **`<acceptance_criteria>`**:
  - `main.go` passes the `connectionRepo` object during handler initialization.
  - `waba_template_test.go` compiles and runs successfully with mock connection inputs.

---

## Wave 2: Simplifying Workspace Handler & Layout

### Task 2.1: Simplify Workspaces Page Template Layout
- **`<read_first>`**:
  - `templates/pages/workspaces.templ`
- **`<action>`**:
  - In `templates/pages/workspaces.templ`, update `WorkspaceDetailPage` and `WorkspaceDetailContent` signatures to remove `waba WABAConfig`, `tg TelegramConfig`, and `externalURL string` parameters.
  - Remove WABA and Telegram cards generation (`@WABACredentialsCard(...)` and `@TelegramCredentialsCard(...)`).
  - Delete `WABACredentialsCard` and `TelegramCredentialsCard` templ definitions completely from the file.
- **`<acceptance_criteria>`**:
  - `templates/pages/workspaces.templ` does not contain `WABACredentialsCard` or `TelegramCredentialsCard`.
  - File compiles without template syntax errors.

### Task 2.2: Refactor Workspace Handler Logic & Credentials Removal
- **`<read_first>`**:
  - `internal/api/handler/admin/workspace.go`
- **`<action>`**:
  - In `internal/api/handler/admin/workspace.go` struct `WorkspaceHandler`, remove `Credentials` and `Templates` repository dependencies.
  - In `Detail` method, remove calls to `h.Credentials.Get`. Simplify `WorkspaceDetailPage` rendering parameters to pass only workspace and keys.
  - Delete `SaveCredentials` and `DeleteCredentials` methods.
- **`<acceptance_criteria>`**:
  - `workspace.go` does not reference `h.Credentials` or `h.Templates`.
  - `SaveCredentials` and `DeleteCredentials` methods are deleted.

### Task 2.3: Update Workspace Handler Tests
- **`<read_first>`**:
  - `internal/api/handler/admin/workspace_test.go`
- **`<action>`**:
  - In `workspace_test.go`, adjust handler and test suites initialization to omit `credentials` and `templates` dependencies.
  - Remove tests for saving and deleting credentials.
- **`<acceptance_criteria>`**:
  - `workspace_test.go` compiles cleanly and all remaining tests pass.

---

## Wave 3: Routing Updates & Redirections

### Task 3.1: Cleanup Routes and Implement Redirects
- **`<read_first>`**:
  - `cmd/pergo/main.go`
- **`<action>`**:
  - In `cmd/pergo/main.go`, remove routes POST `/admin/workspaces/:id/credentials/:channel` and DELETE `/admin/workspaces/:id/credentials/:channel`.
  - Update `adminGroup.GET("/workspaces", workspaceHandler.List)` or map `/admin/workspaces` requests to redirect to the active workspace settings page (`/admin/workspace`).
- **`<acceptance_criteria>`**:
  - Credentials endpoints are no longer registered on the router.
  - Access to `/admin/workspaces` redirects to `/admin/workspace`.

---

## Wave 4: Validation & Compilation

### Task 4.1: Code Compilation and Verification
- **`<read_first>`**:
  - `Makefile`
- **`<action>`**:
  - Run template compiler via `$(go env GOPATH)/bin/templ generate`.
  - Execute full test suite `go test ./...` to verify all components compile and pass.
- **`<acceptance_criteria>`**:
  - Template generation terminates with a complete/successful state.
  - `go test ./...` returns `PASS` status.

---

## Artifacts This Phase Produces
- `.planning/phases/16-deprecate-workspace-subviews/16-01-PLAN.md` (This file)
- `.planning/phases/16-deprecate-workspace-subviews/16-RESEARCH.md`
