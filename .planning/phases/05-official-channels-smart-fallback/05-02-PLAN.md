---
phase: 05-official-channels-smart-fallback
plan: 02
type: tdd
wave: 2
depends_on: ["05-01-PLAN.md"]
files_modified:
  - internal/platform/postgres/migrations/005_create_waba_templates.sql
  - internal/repository/waba_template.go
  - internal/repository/waba_template_test.go
  - internal/domain/message.go
  - internal/channel/dispatcher.go
  - internal/channel/whatsapp/waba.go
  - internal/api/handler/admin/waba_template.go
  - internal/api/handler/admin/waba_template_test.go
  - templates/pages/waba_templates.templ
  - cmd/pergo/main.go
autonomous: true
requirements: [WABA-02, WABA-03]
must_haves:
  truths:
    - "Schema 005_create_waba_templates.sql creates waba_templates table with meta_template_id and unique constraint on (workspace_id, name, language)"
    - "WABATemplateRepository provides CRUD operations for templates"
    - "CreateMessageRequest is extended with TemplateName, Language, and Components fields"
    - "WABAAdapter formats payload as template message when TemplateName is specified"
    - "Admin REST endpoints support template CRUD and sync actions"
    - "Sync API calls Meta Graph API GET /{meta_template_id} to retrieve current approval status"
    - "Templates page displays templates list, creation form, and sync status triggers"
  artifacts:
    - path: "internal/platform/postgres/migrations/005_create_waba_templates.sql"
      provides: "Goose migration SQL file for WABA templates table"
    - path: "internal/repository/waba_template.go"
      provides: "WABATemplateRepository for local template management"
      exports: ["WABATemplate", "WABATemplateRepository", "NewWABATemplateRepository"]
    - path: "internal/repository/waba_template_test.go"
      provides: "Tests for template local database operations"
    - path: "internal/api/handler/admin/waba_template.go"
      provides: "Echo handlers for template creation, listing, and Meta API syncing"
      exports: ["WABATemplateHandler"]
    - path: "internal/api/handler/admin/waba_template_test.go"
      provides: "Unit tests for WABA template endpoints"
    - path: "templates/pages/waba_templates.templ"
      provides: "Templ page for listing and creating WABA templates via admin dashboard"
---

<objective>
Implement local WABA template database storage, extend the message domain models and WABA dispatch adapter to support template-based sending, create the CRUD/sync handlers integrated with Meta's Graph API, and build the admin UI templates panel.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-official-channels-smart-fallback/05-CONTEXT.md
@.planning/phases/05-official-channels-smart-fallback/05-RESEARCH.md
@.planning/phases/05-official-channels-smart-fallback/05-VALIDATION.md
@internal/domain/message.go
@internal/channel/dispatcher.go
@internal/channel/whatsapp/waba.go
@templates/pages/workspaces.templ
@cmd/pergo/main.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration and repository for waba_templates table</name>
  <read_first>
    - internal/platform/postgres/migrations/004_create_channel_credentials.sql
    - internal/repository/workspace.go
  </read_first>
  <acceptance_criteria>
    - Migration file `005_create_waba_templates.sql` exists and goose migration runs successfully.
    - Database table `waba_templates` exists with columns `id`, `workspace_id`, `meta_template_id`, `name`, `language`, `status`, `category`, `components`.
    - `WABATemplateRepository` implements Create, ListByWorkspace, GetByID, UpdateStatus, and Delete.
    - Integration tests in `internal/repository/waba_template_test.go` cover all repository functions.
  </acceptance_criteria>
  <action>
    - Create database migration file `internal/platform/postgres/migrations/005_create_waba_templates.sql`.
    - Create `internal/repository/waba_template.go` containing `WABATemplateRepository` and constructors.
    - Create `internal/repository/waba_template_test.go` for database tests.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend message models and WABA adapter for template-based sending</name>
  <read_first>
    - internal/domain/message.go
    - internal/channel/dispatcher.go
    - internal/channel/whatsapp/waba.go
    - .planning/phases/05-official-channels-smart-fallback/05-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `CreateMessageRequest` has optional JSON fields: `template_name`, `language`, `components`.
    - `ValidateMessage` accepts requests with these template fields when target channel is `whatsapp_cloud`, and enforces `language` is present if `template_name` is set.
    - `MessagePayload` carries template name, language, and components fields.
    - `WABAAdapter.Dispatch` builds and executes a template message send request to Meta Graph API when `TemplateName` is present, verifying payload JSON matches Meta's specifications (e.g. `type: "template"`).
    - Unit tests in `internal/channel/whatsapp/waba_test.go` assert correct template payload composition.
  </acceptance_criteria>
  <action>
    - Modify `internal/domain/message.go` to include template fields, `TemplateComponent`, `TemplateParameter` structs, and update `ValidateMessage`.
    - Modify `internal/channel/dispatcher.go` to include template fields in `MessagePayload`.
    - Modify `internal/channel/whatsapp/waba.go` to parse and build the template REST payload for Meta Graph API.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WABA template CRUD and Sync REST endpoints</name>
  <read_first>
    - internal/repository/waba_template.go
    - cmd/pergo/main.go
  </read_first>
  <acceptance_criteria>
    - `WABATemplateHandler` exposes endpoints:
      - `POST /admin/workspaces/:workspace_id/templates`
      - `GET /admin/workspaces/:workspace_id/templates`
      - `POST /admin/workspaces/:workspace_id/templates/:template_id/sync`
    - Template creation endpoint calls Meta Graph API `POST /{waba_account_id}/message_templates` to register the template and saves the returned ID.
    - Sync endpoint calls Meta Graph API `GET /{meta_template_id}` to retrieve current status and updates the local DB.
    - API tests in `internal/api/handler/admin/waba_template_test.go` mock Meta HTTP endpoints and verify correct JSON structures.
  </acceptance_criteria>
  <action>
    - Create `internal/api/handler/admin/waba_template.go` with template HTTP handlers.
    - Create `internal/api/handler/admin/waba_template_test.go` for tests.
    - Modify `cmd/pergo/main.go` to register these handlers and endpoints under the protected admin group.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Admin UI pages for template management</name>
  <read_first>
    - templates/pages/workspaces.templ
    - internal/api/handler/admin/waba_template.go
  </read_first>
  <acceptance_criteria>
    - `templates/pages/waba_templates.templ` renders a beautiful table listing templates (with status, category, language), a creation form modal, and status sync triggers.
    - Actioning sync sends an HTMX request to the sync endpoint and updates the row status dynamically.
    - Running `make build` or `templ generate` compiles templ components without error.
  </acceptance_criteria>
  <action>
    - Create `templates/pages/waba_templates.templ` containing the dashboard layout page for templates.
  </action>
</task>

</tasks>
