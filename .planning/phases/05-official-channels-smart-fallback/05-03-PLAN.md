---
phase: 05-official-channels-smart-fallback
plan: 03
type: tdd
wave: 3
depends_on: ["05-02-PLAN.md"]
files_modified:
  - internal/platform/postgres/migrations/006_create_recipient_sessions.sql
  - internal/repository/recipient_session.go
  - internal/repository/recipient_session_test.go
  - internal/session/window.go
  - internal/session/window_test.go
  - internal/channel/whatsapp/waba.go
  - internal/api/handler/telegram_webhook.go
  - internal/api/handler/telegram_webhook_test.go
  - cmd/pergo/main.go
  - internal/session/manager.go
autonomous: true
requirements: [WABA-04, TGRAM-02]
must_haves:
  truths:
    - "Schema 006_create_recipient_sessions.sql creates recipient_sessions table with primary key on (workspace_id, recipient_phone, channel)"
    - "RecipientSessionRepository supports upserting the last_inbound_at timestamp and fetching session details"
    - "WABAAdapter rejects free-form (non-template) messages with TerminalError when last inbound is > 24 hours ago or missing"
    - "Telegram webhook endpoint validates X-Telegram-Bot-Api-Secret-Token against registered credentials"
    - "Successful webhook validations update recipient_sessions"
    - "Inbound events from whatsmeow WhatsApp Web sessions also trigger recipient_sessions updates"
  artifacts:
    - path: "internal/platform/postgres/migrations/006_create_recipient_sessions.sql"
      provides: "Goose migration SQL file for recipient sessions table"
    - path: "internal/repository/recipient_session.go"
      provides: "Repository for tracking recipient session timestamps"
      exports: ["RecipientSession", "RecipientSessionRepository", "NewRecipientSessionRepository"]
    - path: "internal/repository/recipient_session_test.go"
      provides: "Integration tests for recipient sessions"
    - path: "internal/session/window.go"
      provides: "Customer service window checker helper"
      exports: ["WindowChecker", "NewWindowChecker"]
    - path: "internal/session/window_test.go"
      provides: "Unit tests for window checking logic"
    - path: "internal/api/handler/telegram_webhook.go"
      provides: "HTTP handler for Telegram incoming webhook POST requests"
      exports: ["TelegramWebhookHandler"]
    - path: "internal/api/handler/telegram_webhook_test.go"
      provides: "Unit tests verifying bot webhook signature validation"
---

<objective>
Build the database structure and checking logic to enforce the 24-hour customer window for WhatsApp Cloud, return terminal errors when outside the window for non-template dispatches, and implement the Telegram inbound webhook with signature validation that updates recipient session timestamps.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-official-channels-smart-fallback/05-CONTEXT.md
@.planning/phases/05-official-channels-smart-fallback/05-RESEARCH.md
@.planning/phases/05-official-channels-smart-fallback/05-VALIDATION.md
@internal/channel/whatsapp/waba.go
@internal/session/manager.go
@cmd/pergo/main.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration and repository for recipient_sessions table</name>
  <read_first>
    - internal/platform/postgres/migrations/005_create_waba_templates.sql
    - internal/repository/workspace.go
  </read_first>
  <acceptance_criteria>
    - Migration file `006_create_recipient_sessions.sql` exists and goose migration runs successfully.
    - Database table `recipient_sessions` exists with primary key `(workspace_id, recipient_phone, channel)` and column `last_inbound_at`.
    - `RecipientSessionRepository.Upsert` inserts or updates `last_inbound_at` to the current time, and `Get` retrieves the session.
    - Tests in `internal/repository/recipient_session_test.go` pass and verify correctness of SQL statements.
  </acceptance_criteria>
  <action>
    - Create database migration file `internal/platform/postgres/migrations/006_create_recipient_sessions.sql`.
    - Create `internal/repository/recipient_session.go` containing repository code.
    - Create `internal/repository/recipient_session_test.go` for tests.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WABA 24-hour customer window checking logic</name>
  <read_first>
    - internal/repository/recipient_session.go
    - internal/channel/whatsapp/waba.go
    - .planning/phases/05-official-channels-smart-fallback/05-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `WindowChecker` in `internal/session/window.go` correctly identifies whether a recipient has sent an inbound message within the last 24 hours.
    - If `CreateMessageRequest` contains a free-form message (no template name) for `whatsapp_cloud` and the window is expired or missing, `WABAAdapter.Dispatch` immediately returns a `TerminalError` (wrapping "customer service window expired").
    - Unit tests in `internal/session/window_test.go` verify that a mock repository returning timestamps <= 24 hours returns true, and > 24 hours returns false.
    - Adapter tests verify that `WABAAdapter.Dispatch` returns terminal error when window is expired.
  </acceptance_criteria>
  <action>
    - Create `internal/session/window.go` containing the window checking service.
    - Create `internal/session/window_test.go` containing unit tests.
    - Modify `internal/channel/whatsapp/waba.go` to inject `WindowChecker` and invoke it before dispatch.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Telegram inbound webhook token validation and upsert</name>
  <read_first>
    - internal/repository/credentials.go
    - internal/repository/recipient_session.go
    - .planning/phases/05-official-channels-smart-fallback/05-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `TelegramWebhookHandler` exposes `/webhooks/telegram/:workspace_id` and processes inbound JSON.
    - Incoming webhook requests are verified by comparing the header `X-Telegram-Bot-Api-Secret-Token` against the workspace's Telegram bot secret token; non-matching headers return `403 Forbidden` immediately.
    - Valid requests upsert `recipient_sessions` for the sender's phone/chat_id on channel `"telegram"` and return `200 OK`.
    - Unit tests in `internal/api/handler/telegram_webhook_test.go` mock requests with valid/invalid header tokens and assert correct response statuses.
  </acceptance_criteria>
  <action>
    - Create `internal/api/handler/telegram_webhook.go` containing HTTP webhook handler.
    - Create `internal/api/handler/telegram_webhook_test.go` for tests.
    - Modify `cmd/pergo/main.go` to register public route `/webhooks/telegram/:workspace_id` to this handler.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Inbound session updates for whatsmeow WhatsApp Web</name>
  <read_first>
    - internal/session/manager.go
    - internal/repository/recipient_session.go
  </read_first>
  <acceptance_criteria>
    - The WhatsApp Web session manager (whatsmeow event handler) intercepts incoming messages from recipients.
    - Incoming message events call `recipient_sessions` repository upsert for the sender's JID on channel `"whatsapp"`.
    - Session manager compilation and unit tests pass.
  </acceptance_criteria>
  <action>
    - Modify `internal/session/manager.go` (or individual device session handler) to inject `RecipientSessionRepository` and upsert the session last_inbound_at on incoming messages.
  </action>
</task>

</tasks>
