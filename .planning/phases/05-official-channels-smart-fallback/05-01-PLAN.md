---
phase: 05-official-channels-smart-fallback
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/platform/postgres/migrations/004_create_channel_credentials.sql
  - internal/repository/credentials.go
  - internal/repository/credentials_test.go
  - internal/channel/whatsapp/waba.go
  - internal/channel/whatsapp/waba_test.go
  - internal/channel/telegram/telegram.go
  - internal/channel/telegram/telegram_test.go
  - internal/channel/registry.go
  - cmd/pergo/main.go
autonomous: true
requirements: [WABA-01, TGRAM-01]
must_haves:
  truths:
    - "Schema 004_create_channel_credentials.sql creates channel_credentials table with workspace_id, channel, credentials BYTEA, key_id, key_version"
    - "CredentialsRepository provides Save, Get, and Delete operations scoped to workspace"
    - "CredentialsRepository uses crypto.Encryptor to encrypt credentials byte slice on save and decrypt on get"
    - "WhatsApp Cloud (WABA) REST adapter implements channel.Dispatcher interface, targeting the Meta message send API"
    - "Telegram adapter implements channel.Dispatcher interface, targeting the Telegram sendMessage Bot API"
    - "Both adapters retrieve credentials from repository using workspace ID from context"
    - "Channel Registry integrates WABA and Telegram Dispatchers"
  artifacts:
    - path: "internal/platform/postgres/migrations/004_create_channel_credentials.sql"
      provides: "Goose migration SQL file for channel credentials table"
    - path: "internal/repository/credentials.go"
      provides: "Credentials Repository with GCM encryption and decryption"
      exports: ["Credentials", "CredentialsRepository", "NewCredentialsRepository"]
    - path: "internal/repository/credentials_test.go"
      provides: "Unit and integration tests for credentials encryption and DB storage"
    - path: "internal/channel/whatsapp/waba.go"
      provides: "WhatsApp Cloud (WABA) adapter implementing Dispatcher interface"
      exports: ["WABAAdapter", "NewWABAAdapter"]
    - path: "internal/channel/whatsapp/waba_test.go"
      provides: "Unit tests with mocked Meta Graph API responses"
    - path: "internal/channel/telegram/telegram.go"
      provides: "Telegram Bot adapter implementing Dispatcher interface"
      exports: ["TelegramAdapter", "NewTelegramAdapter"]
    - path: "internal/channel/telegram/telegram_test.go"
      provides: "Unit tests with mocked Telegram Bot API responses"
---

<objective>
Setup workspace channel credentials repository with envelope encryption, define the adapters for WhatsApp Cloud (WABA) and Telegram using the Dispatcher interface, and register them in the composition root.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-official-channels-smart-fallback/05-CONTEXT.md
@.planning/phases/05-official-channels-smart-fallback/05-RESEARCH.md
@.planning/phases/05-official-channels-smart-fallback/05-VALIDATION.md
@internal/channel/dispatcher.go
@internal/channel/registry.go
@internal/platform/crypto/encrypt.go
@internal/repository/workspace.go
@cmd/pergo/main.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration for channel credentials table and repository</name>
  <read_first>
    - internal/platform/postgres/migrations/003_add_device_whatsapp_fields.sql
    - internal/platform/crypto/encrypt.go
    - internal/repository/workspace.go
  </read_first>
  <acceptance_criteria>
    - Migration file `004_create_channel_credentials.sql` exists and goose migration runs successfully.
    - Database table `channel_credentials` exists with columns `id`, `workspace_id`, `channel`, `credentials`, `key_id`, `key_version`.
    - `CredentialsRepository.Save` and `CredentialsRepository.Get` correctly encrypt and decrypt credentials via `crypto.Encryptor`.
    - Integration tests in `internal/repository/credentials_test.go` assert successful database insert, encryption verification (verifying database bytea matches encrypted format), and correct decryption.
  </acceptance_criteria>
  <action>
    - Create database migration file `internal/platform/postgres/migrations/004_create_channel_credentials.sql`.
    - Create `internal/repository/credentials.go` containing `CredentialsRepository` and constructors.
    - Create `internal/repository/credentials_test.go` for tests.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WABA REST Adapter implementation</name>
  <read_first>
    - internal/channel/dispatcher.go
    - internal/platform/crypto/encrypt.go
    - internal/repository/credentials.go
    - .planning/phases/05-official-channels-smart-fallback/05-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `WABAAdapter` implements `channel.Dispatcher` interface.
    - `WABAAdapter.Dispatch` extracts `workspace_id` from context, retrieves credentials, and sends POST request to `https://graph.facebook.com/v18.0/{phone_number_id}/messages` with Authorization Bearer header.
    - Meta API error responses are parsed; rate limit or transient network errors return standard errors, while invalid numbers or token validation errors return `channel.TerminalError`.
    - Unit tests in `internal/channel/whatsapp/waba_test.go` mock HTTP responses and verify correct headers, endpoints, payloads, and error mappings.
  </acceptance_criteria>
  <action>
    - Create `internal/channel/whatsapp/waba.go` with `WABAAdapter` structure and `Dispatch` method.
    - Create `internal/channel/whatsapp/waba_test.go` for unit testing the adapter.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Telegram REST Adapter implementation</name>
  <read_first>
    - internal/channel/dispatcher.go
    - internal/repository/credentials.go
    - .planning/phases/05-official-channels-smart-fallback/05-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `TelegramAdapter` implements `channel.Dispatcher` interface.
    - `TelegramAdapter.Dispatch` retrieves bot token credentials and sends POST request to `https://api.telegram.org/bot{bot_token}/sendMessage` containing `chat_id` and `text`.
    - Error responses are parsed; bad requests, unauthorized tokens, or blocked bots return `channel.TerminalError`.
    - Unit tests in `internal/channel/telegram/telegram_test.go` mock Telegram HTTP response codes and verify correct mapping to terminal or retryable errors.
  </acceptance_criteria>
  <action>
    - Create `internal/channel/telegram/telegram.go` with `TelegramAdapter` structure and `Dispatch` method.
    - Create `internal/channel/telegram/telegram_test.go` for unit testing the adapter.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Registry wiring in main composition root</name>
  <read_first>
    - internal/channel/registry.go
    - cmd/pergo/main.go
  </read_first>
  <acceptance_criteria>
    - `registry.Register` stores adapters under keys `"whatsapp_cloud"` and `"telegram"`.
    - `cmd/pergo/main.go` instantiates `CredentialsRepository` and both adapters, and registers them in the channel dispatcher registry.
    - `go test -v ./internal/channel/...` runs and passes successfully.
  </acceptance_criteria>
  <action>
    - Modify `internal/channel/registry.go` to ensure keys `"whatsapp_cloud"` and `"telegram"` map cleanly to dispatcher registry.
    - Modify `cmd/pergo/main.go` to construct `WABAAdapter` and `TelegramAdapter` and register them.
  </action>
</task>

</tasks>
