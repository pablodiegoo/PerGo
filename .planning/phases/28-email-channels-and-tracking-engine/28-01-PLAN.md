---
wave: 1
depends_on: []
files_modified:
  - internal/domain/message.go
  - internal/channel/email/email.go
  - internal/channel/email/smtp.go
  - internal/channel/email/email_test.go
  - cmd/pergo/main.go
autonomous: true
requirements:
  - EMAIL-01
---

# Plan: Email Channel Foundation & SMTP/Mailtrap Dispatcher

## Verification Criteria
- `ValidChannels` in `internal/domain/message.go` contains `"email"`, `"email_ses"`, `"email_smtp"`, and `"email_mautic"`.
- `internal/channel/email` package compiles clean and provides `EmailAdapter` implementing `channel.Dispatcher`.
- `SMTPProvider` correctly constructs MIME multi-part text/html emails and dispatches them via SMTP/Mailtrap.
- Unit tests in `internal/channel/email/email_test.go` pass.

## must_haves
- Must implement `channel.Dispatcher` interface cleanly.
- Must support both Plain Text body and HTML body via `ChannelOverrides["email"]` or metadata.
- Must support custom Subject line via `metadata["subject"]` or `channel_overrides`.

## Tasks

### 1. Update Domain ValidChannels
```xml
<task>
  <read_first>
    - internal/domain/message.go
  </read_first>
  <acceptance_criteria>
    - `ValidChannels` includes `"email"`, `"email_ses"`, `"email_smtp"`, `"email_mautic"`.
    - `go test ./internal/domain/...` passes.
  </acceptance_criteria>
  <action>
    Update `ValidChannels` map in `internal/domain/message.go` to include "email", "email_ses", "email_smtp", and "email_mautic".
  </action>
</task>
```

### 2. Implement Deep Email Package & SMTP Provider
```xml
<task>
  <read_first>
    - internal/channel/dispatcher.go
    - internal/domain/message.go
  </read_first>
  <acceptance_criteria>
    - `internal/channel/email/email.go` defines `Provider` interface and `EmailAdapter` struct implementing `channel.Dispatcher`.
    - `internal/channel/email/smtp.go` defines `SMTPProvider` implementing `Provider` using Go's `net/smtp`.
    - `internal/channel/email/email_test.go` tests MIME generation and mock dispatching.
    - `go test ./internal/channel/email/...` passes cleanly.
  </acceptance_criteria>
  <action>
    Create `internal/channel/email/email.go` with `EmailMessage` struct, `Provider` interface, and `EmailAdapter` struct.
    Create `internal/channel/email/smtp.go` with `SMTPConfig` and `SMTPProvider` that formats MIME headers, Subject, Text/HTML multipart bodies, and sends via `smtp.SendMail`.
    Create `internal/channel/email/email_test.go` verifying MIME payload formatting and error handling.
  </action>
</task>
```

### 3. Register Email Dispatchers in Dispatcher Registry
```xml
<task>
  <read_first>
    - cmd/pergo/main.go
    - internal/channel/registry.go
  </read_first>
  <acceptance_criteria>
    - `email.NewAdapter` is registered in `dispatcherRegistry` for `"email"` and `"email_smtp"`.
    - `go build ./cmd/pergo` builds without errors.
  </acceptance_criteria>
  <action>
    In `cmd/pergo/main.go`, instantiate `email.NewAdapter` and register it with `dispatcherRegistry` under keys `"email"` and `"email_smtp"`.
  </action>
</task>
```
