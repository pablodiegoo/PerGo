---
phase: 24.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/pergo/main.go
  - internal/api/handler/admin/typebot.go
  - internal/api/handler/typebot_webhook_test.go
  - internal/integration/typebot/config_test.go
  - internal/api/handler/admin/typebot_test.go
  - templates/pages/typebot_settings.templ
  - templates/pages/typebot_settings_templ.go
  - cmd/pergo/main_test.go
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-04
user_setup: []

must_haves:
  truths:
    - "An inbound message routed by the live DefaultInboundRouter reaches the typebot.Forwarder (no nil-skip in internal/inbound/router.go:41)"
    - "Submitting the admin form at /workspaces/{id}/integrations/typebot persists a JSON envelope that round-trips losslessly into typebot.Config and typebot.BotConfig (per-bot public_token, trigger_words []string, connection_id, is_default)"
    - "POST /api/integrations/typebot with a valid auth token and payload (message, connection_id, contact_id) resolves the connection, resolves the customer identity via contact_identities, and publishes exactly one domain.QueueMessage to NATS subject messages.outbound"
    - "A composition-root smoke test asserts both chatwootSyncer != nil and typebotForwarder != nil after the wiring helper runs; if either is dropped, the test fails"
  artifacts:
    - "cmd/pergo/main.go — wired typebot.Forwarder (TYPE-04) via a new buildInboundRouter helper or inline construction at the existing chatwoot block"
    - "internal/api/handler/admin/typebot.go — PostSettings now reads bot_public_token, connection_id, trigger_keywords, is_default and builds typebot.Config directly"
    - "internal/api/handler/admin/typebot.go — GetSettings now unmarshals into typebot.Config (tolerant of legacy phase-22 shape: missing fields default to zero values)"
    - "templates/pages/typebot_settings.templ — pages.TypebotBot struct now has PublicToken, ConnectionID, TriggerKeywords, IsDefault fields; top-level PublicToken removed from form; connection rendered as <select> (or <input> fallback) populated by handler"
    - "internal/api/handler/typebot_webhook_test.go — TestTypebotWebhookHandler_HappyPath using fakePublisher (reused), getTestPool, real workspace + connection + contact, asserts 200 + published QueueMessage"
    - "internal/integration/typebot/config_test.go (new) — TestConfigRoundTrip: marshal typebot.Config with all fields populated, unmarshal, assert equality"
    - "internal/api/handler/admin/typebot_test.go (new) — TestTypebotSettings_Get and TestTypebotSettings_Post with required-field validation"
    - "cmd/pergo/main_test.go — TestCompositionRoot_AllIntegrationsWired asserting chatwootSyncer != nil && typebotForwarder != nil"
  key_links:
    - "cmd/pergo/main.go:212-215 → internal/inbound/router.go:41 — typebotForwarder value now flows into r.typebotForwarder field; the nil-check in Route() now passes the goroutine-launch branch"
    - "templates/pages/typebot_settings.templ → internal/api/handler/admin/typebot.go → typebot.Config → internal/integration/typebot/forwarder.go:67-83 (BotConfig matching logic) — the form's stored JSON must satisfy the forwarder's field-by-field reads"
    - "internal/api/handler/typebot_webhook.go:86-91 (contact_identities query) ← contactRepo.ResolveContact (which inserts the row at contact.go:152-156) — happy-path test must use the same channel/sender_identity strings"
    - "cmd/pergo/main_test.go (composition-root smoke) → cmd/pergo/main.go (wiring) — test must use the same dependency constructors (chatwoot.NewChatwootSyncer, typebot.NewForwarder) and the same integrationRepo + new typebotSessionRepo"
  prohibitions:
    - "Do NOT add any new external Go dependency (no new module in go.mod)"
    - "Do NOT change the typebot.Forwarder struct or its SyncInboundMessage logic; only the wiring changes"
    - "Do NOT add multi-bot UI; the form persists a single BotConfig inside Bots:[]BotConfig{{...}}"
    - "Do NOT change BotConfig.ConnectionID from string to uuid.UUID; the forwarder reads it as string"
    - "Do NOT introduce a custom JSON unmarshaler to forgive the old phase-22 shape; the GET handler tolerates missing fields by defaulting to zero values, the POST handler always writes the new shape"

artifacts_this_phase_produces:
  - "New symbol: cmd/pergo/main.go → buildInboundRouter(pool, encryptor, publisher) helper (if extracted) or inline construction block at lines 212-216 adding typebotSessionRepo and typebotForwarder"
  - "New import in cmd/pergo/main.go: \"github.com/pablojhp.pergo/internal/integration/typebot\" (currently zero references in main.go)"
  - "New import in internal/api/handler/admin/typebot.go: \"github.com/pablojhp.pergo/internal/integration/typebot\" (for typebot.Config reference in GetSettings unmarshal)"
  - "Modified type: templates/pages.TypebotBot — fields changed: removed TriggerKeyword; added PublicToken, ConnectionID, TriggerKeywords, IsDefault"
  - "Modified type: templates.pages.TypebotConfig — field removed: PublicToken (top-level)"
  - "Modified function: internal/api/handler/admin/typebot.go → TypebotSettingsHandler.PostSettings — now reads 5 form fields, validates 4 are non-empty (api_url, bot_id, bot_public_token, connection_id), builds typebot.Config directly"
  - "Modified function: internal/api/handler/admin/typebot.go → TypebotSettingsHandler.GetSettings — now unmarshals into typebot.Config (internal/integration/typebot package) and converts to display struct; tolerates legacy shape by zero-defaulting missing per-bot fields"
  - "New test function: internal/integration/typebot/config_test.go → TestConfigRoundTrip"
  - "New test function: internal/api/handler/admin/typebot_test.go → TestTypebotSettings_Get, TestTypebotSettings_Post, TestTypebotSettings_Post_ValidatesRequiredFields, TestTypebotSettings_Post_RejectsInvalidConnectionID"
  - "New test function: internal/api/handler/typebot_webhook_test.go → TestTypebotWebhookHandler_HappyPath (plus replacement of the existing TestTypebotWebhook that currently only tests 401)"
  - "New test function: cmd/pergo/main_test.go → TestCompositionRoot_AllIntegrationsWired"
  - "New helper constant in templates/pages/typebot_settings.templ: connection options list rendered as <option> elements (passed by the handler as a new arg, or read in the GET path)"
  - "New handler field: internal/api/handler/admin/typebot.go → TypebotSettingsHandler now holds connectionRepo *repository.ConnectionRepository (added to NewTypebotSettingsHandler signature)"
  - "New handler constructor signature: NewTypebotSettingsHandler(integrationRepo *repository.IntegrationRepository, connectionRepo *repository.ConnectionRepository) *TypebotSettingsHandler (replaces current 1-arg constructor; updated call site at cmd/pergo/main.go:332)"
---

<objective>
Close the three v1.3 milestone gaps in one plan: wire the typebot forwarder into the live composition root, reconcile the admin form schema with the forwarder's expected config shape, and add regression defenses (webhook happy-path test + composition-root smoke test).

Purpose: The v1.3 milestone shipped with the typebot forwarder never wired into main.go and the admin form producing JSON that the forwarder cannot consume. Both gaps are silent — unit tests pass, but at runtime customer messages never reach Typebot. This plan restores the working v1.3 integration end-to-end and adds two tests that would have caught both regressions.

Output:
1. Wired `typebot.Forwarder` at `cmd/pergo/main.go` (TYPE-04 satisfied)
2. Reconciled `pages.TypebotConfig` and `templates/pages/typebot_settings.templ` to produce the forwarder's `typebot.Config` shape (TYPE-01 satisfied)
3. New `TestTypebotWebhookHandler_HappyPath` (TYPE-02 happy-path coverage)
4. New `TestCompositionRoot_AllIntegrationsWired` in `cmd/pergo/main_test.go` (regression defense)
5. New config round-trip test in `internal/integration/typebot/config_test.go`
6. New admin handler tests in `internal/api/handler/admin/typebot_test.go`
</objective>

<execution_context>
@/home/pablo/Coding/PerGo/.opencode/gsd-core/workflows/execute-plan.md
@/home/pablo/Coding/PerGo/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/pablo/Coding/PerGo/.planning/PROJECT.md
@/home/pablo/Coding/PerGo/.planning/ROADMAP.md
@/home/pablo/Coding/PerGo/.planning/v1.3-MILESTONE-AUDIT.md
@/home/pablo/Coding/PerGo/.planning/phases/24.1-close-gap-wire-typebot-forwarder-and-reconcile-form-schema/24.1-RESEARCH.md
@/home/pablo/Coding/PerGo/.planning/phases/24.1-close-gap-wire-typebot-forwarder-and-reconcile-form-schema/24.1-VALIDATION.md

# Phase 24.1 source files
@/home/pablo/Coding/PerGo/cmd/pergo/main.go
@/home/pablo/Coding/PerGo/internal/inbound/router.go
@/home/pablo/Coding/PerGo/internal/integration/typebot/forwarder.go
@/home/pablo/Coding/PerGo/internal/integration/typebot/client.go
@/home/pablo/Coding/PerGo/internal/repository/typebot_session.go
@/home/pablo/Coding/PerGo/internal/repository/integration.go
@/home/pablo/Coding/PerGo/internal/repository/connection.go
@/home/pablo/Coding/PerGo/internal/api/handler/admin/typebot.go
@/home/pablo/Coding/PerGo/internal/api/handler/typebot_webhook.go
@/home/pablo/Coding/PerGo/templates/pages/typebot_settings.templ
@/home/pablo/Coding/PerGo/internal/inbound/processor.go (Publisher interface)
</context>

<tasks>

<!-- ============================================================== -->
<!-- Task 1 — TYPE-01: reconcile form schema with typebot.Config     -->
<!-- ============================================================== -->
<task type="auto" tdd="true">
  <name>Task 1: Reconcile form schema with typebot.Config shape (TYPE-01)</name>
  <files>
    - templates/pages/typebot_settings.templ
    - templates/pages/typebot_settings_templ.go (regenerated by `templ generate`)
    - internal/api/handler/admin/typebot.go
    - internal/api/handler/admin/typebot_test.go (new)
  </files>
  <read_first>
    - /home/pablo/Coding/PerGo/internal/integration/typebot/client.go (target Config + BotConfig shapes — lines 13-26)
    - /home/pablo/Coding/PerGo/internal/integration/typebot/forwarder.go (which fields the forwarder actually reads — lines 67-83, 102, 110, 141)
    - /home/pablo/Coding/PerGo/templates/pages/typebot_settings.templ (current form: lines 8-17 structs, 65-70 top-level public_token input, 72-97 bot inputs)
    - /home/pablo/Coding/PerGo/internal/api/handler/admin/typebot.go (current PostSettings at lines 56-113 reads api_url, public_token, bot_id, trigger_keyword, active)
    - /home/pablo/Coding/PerGo/internal/api/handler/admin/waba_template.go (pattern for ConnectionRepository.ListByWorkspace injection — lines 47, 106)
    - /home/pablo/Coding/PerGo/internal/api/handler/admin/campaign.go (lines 63, 89) for how to pass connection list into a templ render
  </read_first>
  <display_struct_contract>
    The templ file is the source of truth for the form's display shape. Use these exact struct names and field sets (defined in `templates/pages/typebot_settings.templ`):

    - `pages.TypebotBot` — extended in place. New fields added: `PublicToken string` (json tag `public_token`), `ConnectionID string` (json tag `connection_id`), `TriggerKeywords string` (json tag `trigger_keywords`; comma-joined form value, converted to/from `[]string` at the handler boundary), `IsDefault bool` (json tag `is_default`). The existing `BotID` and the now-removed `TriggerKeyword` stay/replace as appropriate. JSON tags must match the storage struct's tags where applicable.
    - `pages.TypebotConfig` — `APIURL string` + `Bots []TypebotBot`. The top-level `PublicToken` field is **removed** (the form no longer collects a workspace-wide public token; each bot has its own).
    - The two new handler params for the templ render: `connections []pages.ConnectionOption` (the workspace's connections, for the picker). The existing `pages.ConnectionOption` type from `admin/campaign.go` is the analog — reuse it.

    New function signatures: `TypebotSettingsPage(workspaceID uuid.UUID, cfg pages.TypebotConfig, connections []pages.ConnectionOption, active bool, successMsg string, errorMsg string)` and the same for `TypebotSettingsContent`. The `connections` arg is a new positional arg — pass it through at the call site in `admin/typebot.go` (the `GetSettings` and `PostSettings` call sites both update).

    The `GetSettings` handler converts the storage `typebot.Config` (decoded from `integ.Config`) to the display `pages.TypebotConfig` (joining `TriggerWords` into `TriggerKeywords`, populating each `TypebotBot`'s new fields from the storage `BotConfig`). The `PostSettings` handler does the inverse: builds a `typebot.Config` directly (not a `pages.TypebotConfig`) and persists it.

    Do NOT invent a different struct name (e.g., `TypebotDisplay`). The existing `pages.TypebotConfig` name is preserved; only its shape changes.
  </display_struct_contract>
  <behavior>
    - Submitting form with valid fields (api_url, bot_id, bot_public_token, connection_id, trigger_keywords="sales,help", is_default=true) persists JSON envelope that decodes into typebot.Config{APIURL: "https://...", Bots: [{BotID: "B", PublicToken: "T", ConnectionID: "<uuid>", TriggerWords: ["sales","help"], IsDefault: true, SessionTimeout: 0}]}
    - Submitting form missing any of (api_url, bot_id, bot_public_token, connection_id) renders the form with errorMsg "API URL, Bot ID, Bot Public Token, and Connection are required"
    - Submitting form with connection_id that does not parse as uuid.UUID renders the form with errorMsg "Invalid Connection ID"
    - GET on /workspaces/{id}/integrations/typebot with an existing stored typebot.Config renders the form populated with the stored values (per-bot bot_id, public_token, connection_id, trigger_keywords joined by ",", is_default checkbox checked)
    - GET on /workspaces/{id}/integrations/typebot with the legacy phase-22 stored shape (no per-bot public_token, single trigger_keyword) renders the form with empty per-bot fields and the page does NOT error
  </behavior>
  <action>
    Update `templates/pages/typebot_settings.templ` and the dependent Go handler to produce a JSON envelope that round-trips into `internal/integration/typebot.Config`. Specifically:

    **1. Modify `templates/pages/typebot_settings.templ` (struct definitions at lines 8-17):**
    - Remove the top-level `PublicToken` field from `TypebotConfig`. Replace with just `APIURL` + `Bots []TypebotBot`.
    - Change `TypebotBot` to mirror `typebot.BotConfig` display fields: `BotID string`, `PublicToken string`, `ConnectionID string`, `TriggerKeywords string` (comma-joined for the text input), `IsDefault bool`. JSON tags match the storage struct's tags where applicable.
    - Remove the top-level "Public Token" `<input name="public_token">` block at lines 65-70.
    - Move the "Bot Public Token" `<input name="bot_public_token">` into the bot-settings card alongside bot_id.
    - Add a Connection selector block inside the bot-settings card. The handler passes a `connections []pages.ConnectionOption` parameter; render as `<select name="connection_id">` with `<option value="">-- select a connection --</option>` first, then one `<option value="<uuid>"><name> (<channel>)</option>` per connection. Pre-select the current `cfg.Bots[0].ConnectionID` if non-empty. **Operator-console UX:** if `connections` is empty, render a free-text `<input name="connection_id">` instead and add a help text "Type a connection UUID. Set up a connection first if the list is empty."
    - Replace the single "Trigger Keyword" `<input name="trigger_keyword">` with "Trigger Keywords" `<input name="trigger_keywords">` (comma-separated).
    - Add a "Default bot for this connection" `<input type="checkbox" name="is_default" value="true">` after the trigger keywords input. Pre-check if `cfg.Bots[0].IsDefault`.
    - Update both `TypebotSettingsPage` and `TypebotSettingsContent` function signatures to accept `connections []pages.ConnectionOption` (the workspace's connections for the picker). See `<display_struct_contract>` above for the exact new signature and struct names.

    **2. Update `internal/api/handler/admin/typebot.go`:**
    - Change the constructor: `NewTypebotSettingsHandler(integrationRepo *repository.IntegrationRepository, connectionRepo *repository.ConnectionRepository)`. Add `connectionRepo` as a struct field.
    - **GetSettings (lines 26-54):** call `connectionRepo.ListByWorkspace(ctx, workspaceID)` to load connections for the picker. Read the stored integration's `Config` bytes into a `typebot.Config` (import `"github.com/pablojhp.pergo/internal/integration/typebot"`) — this is the storage shape. Tolerate phase-22-era stored shape by ignoring `json.Unmarshal` errors when the stored bytes don't match the new shape (silently use zero-value `typebot.Config` so the form still renders). Convert the storage struct to a display struct: `cfg.Bots[0].TriggerWords` joined into `cfg.Bots[0].TriggerKeywords` as comma-separated string for the form input.
    - **PostSettings (lines 56-113):** read new form fields: `c.FormValue("bot_public_token")`, `c.FormValue("connection_id")`, `c.FormValue("trigger_keywords")`, `c.FormValue("is_default") == "true"`. Validate `api_url`, `bot_id`, `bot_public_token`, `connection_id` non-empty (return form with errorMsg "API URL, Bot ID, Bot Public Token, and Connection are required"). Validate `connection_id` parses as `uuid.UUID` (return form with errorMsg "Invalid Connection ID"). Build `typebot.Config` directly (not `pages.TypebotConfig`) with the per-bot fields populated. Split `trigger_keywords` by comma, trim whitespace per element, drop empty elements. Marshal to JSON and persist via `integrationRepo.Save` (unchanged path).
    - **Do NOT update the call site at `cmd/pergo/main.go:332` in this task.** Task 4 owns that single edit (it is the only edit to `main.go:332` in this plan; Task 1 only changes the handler signature and the templ file).

    **3. Run `templ generate`** to regenerate `templates/pages/typebot_settings_templ.go` from the modified `.templ` file. Verify the generated file compiles.

    **4. Add `internal/api/handler/admin/typebot_test.go` (new file):**
    - `TestTypebotSettings_Get_RendersEmptyFormWhenNoIntegration`: create workspace via `wsRepo.Create`, call `GetSettings`, assert `rec.Code == 200`.
    - `TestTypebotSettings_Post_ValidatesRequiredFields`: post form missing `bot_public_token`; assert errorMsg rendered.
    - `TestTypebotSettings_Post_RejectsInvalidConnectionID`: post form with `connection_id="not-a-uuid"`; assert errorMsg "Invalid Connection ID".
    - `TestTypebotSettings_Post_PersistsTypebotConfig`: post valid form (api_url, bot_id, bot_public_token, connection_id as uuid, trigger_keywords="sales,help", is_default="true"); call `integrationRepo.GetByProvider`; unmarshal stored bytes into `typebot.Config`; assert all fields match.
    - `TestTypebotSettings_Post_BuildsPerBotShape`: post with is_default="true"; assert stored `cfg.Bots[0].IsDefault == true` and `cfg.Bots[0].TriggerWords == ["sales","help"]`.
    - `TestTypebotSettings_Get_LoadsStoredTypebotConfig`: after a successful POST, call `GetSettings`; assert the rendered HTML contains the bot_id, public_token (masked/truncated if templ escapes it), and the connection_id.
    - Use `getTestPool(t)` pattern (or the local equivalent if not exported). If a helper is not available, inline `getTestPool`-style logic. Reuse the existing `fakePublisher` if needed; otherwise do not require a publisher for these tests (the typebot admin handler does not publish).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/api/handler/admin/ -run TestTypebotSettings -v</automated>
  </verify>
  <done>
    - `go build ./...` exits 0
    - `go test ./internal/api/handler/admin/ -run TestTypebotSettings -v` is fully green
    - POST with valid form fields produces a stored JSON envelope that unmarshals into `typebot.Config` with the per-bot shape: `Bots: [{BotID, PublicToken, ConnectionID, TriggerWords: ["...",...], IsDefault: true/false, SessionTimeout: 0}]`
    - POST without `bot_public_token` or `connection_id` returns the form with the required-fields error message (HTTP 200, errorMsg populated)
    - POST with `connection_id="not-a-uuid"` returns the form with the invalid-uuid error message
    - GET on a workspace with no stored integration renders an empty form (HTTP 200, no error)
    - GET on a workspace with a stored new-shape config renders the form populated with the stored values
  </done>
</task>

<!-- ============================================================== -->
<!-- Task 2 — TYPE-01: typebot.Config round-trip test                -->
<!-- ============================================================== -->
<task type="auto" tdd="true">
  <name>Task 2: Add typebot.Config round-trip test (TYPE-01 storage shape)</name>
  <files>
    - internal/integration/typebot/config_test.go (new)
  </files>
  <read_first>
    - /home/pablo/Coding/PerGo/internal/integration/typebot/client.go (lines 13-26 — the Config and BotConfig structs)
    - /home/pablo/Coding/PerGo/internal/integration/typebot/forwarder.go (lines 67-83 — which fields the forwarder reads)
  </read_first>
  <behavior>
    - Marshalling typebot.Config{APIURL: "https://example.com", Bots: [{BotID: "B1", PublicToken: "T1", ConnectionID: "uuid-1", TriggerWords: ["sales","help"], IsDefault: true, SessionTimeout: 30}]} produces JSON with keys "api_url" and "bots" (lowercase); each bot has "bot_id", "public_token", "connection_id", "trigger_words", "is_default", "session_timeout_minutes"
    - Unmarshalling that JSON back into typebot.Config produces a struct with all fields equal to the original
    - The round-trip is lossless for empty Bots (Bots: nil and Bots: []), empty TriggerWords (nil vs []), and zero SessionTimeout
  </behavior>
  <action>
    Add `internal/integration/typebot/config_test.go` (new file) with `TestConfigRoundTrip`:

    1. Construct a `typebot.Config` value with all fields populated, including a non-empty `APIURL`, a single `BotConfig` with `BotID`, `PublicToken`, `ConnectionID`, two-element `TriggerWords`, `IsDefault: true`, `SessionTimeout: 30`.
    2. `json.Marshal` the value, then `json.Unmarshal` the bytes back into a new `typebot.Config`.
    3. Assert every field equals the original (use `reflect.DeepEqual` or per-field equality; `reflect.DeepEqual` is acceptable here).
    4. Add sub-tests for edge cases via `t.Run`:
       - `EmptyBots`: `Bots: nil` round-trips with `Bots == nil` on the other side.
       - `EmptyTriggerWords`: `TriggerWords: nil` round-trips.
       - `ZeroSessionTimeout`: `SessionTimeout: 0` round-trips as `0`.
       - `MultipleBots`: a config with two `BotConfig` entries round-trips preserving order and all fields.

    Do not add any non-test code. This file exists solely to lock in the storage shape so the form-schema change in Task 1 has a guard against future drift.
  </action>
  <verify>
    <automated>go test ./internal/integration/typebot/ -run TestConfigRoundTrip -v</automated>
  </verify>
  <done>
    - `go test ./internal/integration/typebot/ -run TestConfigRoundTrip -v` is fully green
    - The test file imports the same `typebot` package being tested (no test-only type definitions)
    - The test file does not introduce new dependencies
  </done>
</task>

<!-- ============================================================== -->
<!-- Task 3 — TYPE-02: webhook happy-path integration test           -->
<!-- ============================================================== -->
<task type="auto" tdd="true">
  <name>Task 3: Add Typebot webhook happy-path test (TYPE-02)</name>
  <files>
    - internal/api/handler/typebot_webhook_test.go (modified)
  </files>
  <read_first>
    - /home/pablo/Coding/PerGo/internal/api/handler/typebot_webhook.go (the handler: 401 on missing workspace, 400 on bad payload, queries connections at line 71, queries contact_identities at line 86, publishes to messages.outbound at line 129)
    - /home/pablo/Coding/PerGo/internal/api/handler/chatwoot_webhook_test.go (the pattern: lines 26-41 fakePublisher, lines 43-78 getTestPool, lines 151-348 TestChatwootWebhookHandler_Integration happy path)
    - /home/pablo/Coding/PerGo/internal/api/handler/typebot_webhook_test.go (current 401-only test, lines 1-64)
    - /home/pablo/Coding/PerGo/internal/repository/contact.go (lines 152-156 — ResolveContact inserts a contact_identities row keyed on channel + sender_identity; lines 60-65 — ResolveContact signature)
  </read_first>
  <behavior>
    - POST /api/integrations/typebot with a valid auth token and a JSON payload `{"message":"hi","connection_id":"<uuid>","contact_id":"<uuid>"}` (where the connection exists in the workspace and the contact has a contact_identities row for the connection's channel with the same sender_identity that ResolveContact used) returns HTTP 200, the `fakePublisher` records exactly one Publish call, the subject is "messages.outbound", the data unmarshals into `domain.QueueMessage` with `WorkspaceID == ws.ID`, `ConnectionID == connectionID`, `SenderIdentity == senderIdentity`, `To == customerIdentity`, `Channel == channel`, `Body == "hi"`, and a non-empty `TraceID`.
    - POST with a missing `message` field returns HTTP 400 with the "missing required fields" code.
    - POST with an unknown `connection_id` returns HTTP 500 (current behavior — surfaces DB error).
    - POST with no `?token=` query param returns HTTP 401.
  </behavior>
  <action>
    Rewrite `internal/api/handler/typebot_webhook_test.go` to replace the existing 401-only `TestTypebotWebhook` with a comprehensive test set:

    1. Keep the existing `mockPublisher` struct (lines 17-24) — but it stores `[]string` and is missing `subject` and `traceID`. Either delete it and rely on the `fakePublisher` from `chatwoot_webhook_test.go` (same package `handler_test`, so it is accessible by symbol), or extend `mockPublisher` to also capture `subject` and `traceID`. **Recommended:** delete `mockPublisher`, reuse `fakePublisher` for consistency with the chatwoot tests.
    2. Add `TestTypebotWebhook_Auth` (mirror chatwoot's `TestChatwootWebhookAuth` at line 80-149): assert 401 on missing/invalid token; assert 200 on valid token with empty payload.
    3. Add `TestTypebotWebhookHandler_HappyPath` (mirror chatwoot's `TestChatwootWebhookHandler_Integration` at line 151-348):
       - `pool := getTestPool(t); defer pool.Close()` (reuse the helper at `chatwoot_webhook_test.go:43-78`; if not exported, copy the helper or re-implement locally with the same DSN + skip-if-unreachable semantics).
       - Create workspace via `wsRepo.Create`; defer `wsRepo.Delete`.
       - Create API key via `apiKeyRepo.Create(ctx, ws.ID, "Test Key")`; capture `rawKey`.
       - Create encryptor and connection repo; create a connection with `Channel: "telegram"`, `SenderIdentity: "contact_tg_123"`, `Credentials: []byte('{}')`.
       - Resolve a contact via `contactRepo.ResolveContact(ctx, ws.ID, "telegram", "contact_tg_123", "Test Cust", "testcust", "+123")` — this inserts a `contact_identities` row keyed on `(workspace_id, channel="telegram", sender_identity="contact_tg_123")` (verified at `contact.go:152-156`). Capture the returned `contact.ID`.
       - Build the handler: `handler.NewTypebotWebhookHandler(pool, pub)`.
       - Wire Echo: `e := echo.New(); e.Use(middleware.AuthMiddleware(apiKeyRepo)); e.POST("/api/integrations/typebot", h.Handle)`.
       - Sub-test `ProcessValidWebhookAndPublish`: payload `{"message":"hi from typebot","connection_id":"<connID>","contact_id":"<contactID>"}`; POST with `?token=<rawKey>`; assert `rec.Code == 200`, `len(pub.published) == 1`, `pub.published[0].subject == "messages.outbound"`, unmarshal `pub.published[0].data` into `domain.QueueMessage` and assert `WorkspaceID`, `ConnectionID`, `SenderIdentity`, `To`, `Channel`, `Body`, non-empty `TraceID`.
       - Sub-test `MissingRequiredField_Returns400`: payload missing `message`; assert `rec.Code == 400` and the body contains `"missing required fields"`.
       - Sub-test `UnknownConnectionID_Returns500`: payload with a random `connection_id` that does not exist; assert `rec.Code == 500` (current behavior — the handler returns 500 on the connections query failing).
    4. Do NOT remove the original `TestTypebotWebhook` body, but rename it to make its narrow scope clear, or merge it into `TestTypebotWebhook_Auth`. If the existing test contains dead code (lines 26-46 build a payload and context but do not exercise the handler), delete that dead code.

    The `package handler_test` declaration stays. The `fakePublisher` from `chatwoot_webhook_test.go` is in the same test package and is accessible without import.
  </action>
  <verify>
    <automated>go test ./internal/api/handler/ -run TestTypebotWebhook -v</automated>
  </verify>
  <done>
    - `go test ./internal/api/handler/ -run TestTypebotWebhook -v` is fully green
    - `TestTypebotWebhookHandler_HappyPath` exists and asserts the 200 path produces exactly one published message on subject `messages.outbound` with a `domain.QueueMessage` whose `WorkspaceID`, `ConnectionID`, `SenderIdentity`, `To`, `Channel`, `Body` match the test fixtures
    - The 401 path remains covered (in `TestTypebotWebhook_Auth` or equivalent)
    - The 400 missing-required-field path is covered
    - The unknown-connection_id path is covered (asserts 500)
  </done>
</task>

<!-- ============================================================== -->
<!-- Task 4 — TYPE-04: wire typebot.Forwarder in main.go + smoke test -->
<!-- ============================================================== -->
<task type="auto" tdd="true">
  <name>Task 4: Wire typebot.Forwarder in main.go and add composition-root smoke test (TYPE-04)</name>
  <files>
    - cmd/pergo/main.go
    - cmd/pergo/main_test.go
  </files>
  <read_first>
    - /home/pablo/Coding/PerGo/cmd/pergo/main.go (current wiring at lines 212-215: integrationRepo + chatwootMappingRepo + chatwootSyncer + inboundRouter = NewDefaultRouter(chatwootSyncer, nil); publisher is constructed at line 143; typebotAdminHandler constructed at line 332)
    - /home/pablo/Coding/PerGo/internal/integration/typebot/forwarder.go (NewForwarder signature: lines 24-35 — takes *repository.TypebotSessionRepository, *repository.IntegrationRepository, inbound.Publisher; does NOT dereference the publisher at construction)
    - /home/pablo/Coding/PerGo/internal/repository/typebot_session.go (NewTypebotSessionRepository signature: line 31 — takes *pgxpool.Pool)
    - /home/pablo/Coding/PerGo/internal/inbound/router.go (NewDefaultRouter signature: lines 18-22)
    - /home/pablo/Coding/PerGo/cmd/pergo/main_test.go (existing test file in package main; pattern for using testDSN() and skipping if pool is unreachable; lines 41-50, 153-160)
  </read_first>
  <behavior>
    - After `inbound.NewDefaultRouter(chatwootSyncer, typebotForwarder)` is called, `inbound.DefaultInboundRouter.Route(contact, ev)` invokes the typebot forwarder goroutine when `contact.BotActive == true` (verifiable by reading `internal/inbound/router.go:41-49` — the nil-check passes).
    - `cmd/pergo` compiles cleanly with the new import `github.com/pablojhp.pergo/internal/integration/typebot`.
    - `go run ./cmd/pergo` starts successfully and the live `inboundRouter` has a non-nil typebot field (this is checked by the smoke test in main_test.go, not by a manual run).
    - `TestCompositionRoot_AllIntegrationsWired` passes when both forwarders are constructed; the test fails if either is nil.
  </behavior>
  <action>
    **1. Modify `cmd/pergo/main.go` to wire the typebot forwarder:**

    - Add the import: `"github.com/pablojhp.pergo/internal/integration/typebot"` in the import block at lines 5-48 (alphabetical position, between `chatwoot` and `outbound`).
    - At lines 212-215, replace the existing four-line wiring block with the corrected version. **Recommended:** extract a helper function `buildInboundRouter(pool *pgxpool.Pool, encryptor crypto.Encryptor, publisher inbound.Publisher) (*inbound.DefaultInboundRouter, *chatwoot.Syncer, *typebot.Forwarder, error)` placed above `main()`. The helper:
      1. Constructs `integrationRepo := repository.NewIntegrationRepository(pool, encryptor)`.
      2. Constructs `chatwootMappingRepo := repository.NewChatwootMappingRepository(pool)`.
      3. Constructs `typebotSessionRepo := repository.NewTypebotSessionRepository(pool)`.
      4. Constructs `chatwootSyncer := chatwoot.NewChatwootSyncer(integrationRepo, chatwootMappingRepo, nil)`.
      5. Constructs `typebotForwarder := typebot.NewForwarder(typebotSessionRepo, integrationRepo, publisher)`. (Passing the real `publisher` here, NOT `nil` — production wiring must use the real publisher even though the smoke test in step 2 will pass `nil`.)
      6. Constructs `inboundRouter := inbound.NewDefaultRouter(chatwootSyncer, typebotForwarder)`.
      7. Returns all three (or all four with the integrationRepo if needed for the test).
    - Replace the original four lines (212-215) with one call: `inboundRouter, chatwootSyncer, typebotForwarder, integrationRepo, err := buildInboundRouter(pool, encryptor, publisher)`. (If the helper returns the integrationRepo for reuse at line 331 where the chatwoot admin handler is constructed, the helper can return it; alternatively, leave the `integrationRepo` construction outside the helper to preserve the existing call site at line 331.)
    - **Alternative (less invasive — preferred if `integrationRepo` is needed at line 331):** keep the existing `integrationRepo` construction at line 212, and only inline the new `typebotSessionRepo` and `typebotForwarder` lines after `chatwootSyncer`:
      ```
      typebotSessionRepo := repository.NewTypebotSessionRepository(pool)
      typebotForwarder := typebot.NewForwarder(typebotSessionRepo, integrationRepo, publisher)
      inboundRouter := inbound.NewDefaultRouter(chatwootSyncer, typebotForwarder)
      ```
    - **Choose the inline approach** (alternative) — it preserves all downstream references to `integrationRepo` (used at line 331 for `chatwootAdminHandler`, `typebotAdminHandler`) with zero ripple. The smoke test in step 2 will re-implement the wiring locally (it's only ~6 lines) to avoid coupling to `main()` internals.
    - **The smoke-test approach (recommended by research §Open Question 4):** since `cmd/pergo/main.go` is `package main` and tests are also `package main`, the test can either reference top-level vars directly OR re-implement the wiring block. Re-implementing is more robust (does not depend on `main()`'s call ordering) and matches the existing `cmd/pergo/main_test.go` pattern (which calls `runServer(ctx, pool, db)` — a helper extracted from `main()`).
    - **Do not extract `buildInboundRouter` unless a second caller needs it.** The smoke test can replicate the 6 lines.

    **2. Add `TestCompositionRoot_AllIntegrationsWired` to `cmd/pergo/main_test.go`:**
    - Use the existing `testDSN()` helper (line 155) for the DSN.
    - `pool, err := postgres.NewPool(ctx, testDSN())`; if ping fails, `t.Skip` (mirror lines 41-50).
    - `defer pool.Close()`.
    - Build a real (or test) encryptor: `kek := make([]byte, 32); enc, _ := crypto.NewEncryptor(kek)`.
    - Replicate the wiring block:
      ```
      integrationRepo := repository.NewIntegrationRepository(pool, enc)
      chatwootMappingRepo := repository.NewChatwootMappingRepository(pool)
      typebotSessionRepo := repository.NewTypebotSessionRepository(pool)

      chatwootSyncer := chatwoot.NewChatwootSyncer(integrationRepo, chatwootMappingRepo, nil)
      // The publisher is nil — NewForwarder only stores it; does not dereference at construction.
      typebotForwarder := typebot.NewForwarder(typebotSessionRepo, integrationRepo, nil)

      if chatwootSyncer == nil {
          t.Fatal("chatwootSyncer must not be nil — regression of Phase 21 wiring")
      }
      if typebotForwarder == nil {
          t.Fatal("typebotForwarder must not be nil — regression of TYPE-04 wiring")
      }
      ```
    - Add a comment on the test explaining: "this is a wiring-only smoke test. It does not exercise the forwarder's runtime behavior. Do not add a real `inbound.Publisher` here — NewForwarder only stores the publisher and does not call Publish at construction time. See internal/integration/typebot/forwarder_test.go for the functional forwarder tests."
    - The test does NOT call `inbound.NewDefaultRouter` — it directly checks that both constructors returned non-nil. This is sufficient to catch the TYPE-04 regression (where the second arg to `NewDefaultRouter` was nil) IF the test is updated in lock-step with the wiring. **For stronger coverage:** add a third assertion that calls `inbound.NewDefaultRouter(chatwootSyncer, typebotForwarder)` and immediately invokes `Route` with a non-nil contact/event in a way that does not require external I/O — the `DefaultInboundRouter.Route` method (router.go:26-51) launches goroutines that call the forwarder. To avoid the goroutine calling the Typebot REST API (which the forwarder would do for a real inbound event), this assertion is OUT OF SCOPE for the smoke test. The two nil-checks are sufficient.

    **3. Update the `typebotAdminHandler` constructor call at `cmd/pergo/main.go:332`:**
    - The current call is `typebotAdminHandler := admin.NewTypebotSettingsHandler(integrationRepo)`. After Task 1, the signature is `NewTypebotSettingsHandler(integrationRepo, connectionRepo)`. Update the call to `admin.NewTypebotSettingsHandler(integrationRepo, connectionRepo)`. (The `connectionRepo` is already constructed at line 184.)
  </action>
  <verify>
    <automated>go build ./cmd/pergo/... && go test ./cmd/pergo/ -run TestCompositionRoot_AllIntegrationsWired -v</automated>
  </verify>
  <done>
    - `go build ./cmd/pergo/...` exits 0
    - `go test ./cmd/pergo/ -run TestCompositionRoot_AllIntegrationsWired -v` is fully green
    - The test fails (t.Fatal) if either `chatwootSyncer == nil` or `typebotForwarder == nil`
    - The live `cmd/pergo/main.go` has a `typebot.NewForwarder` call that is NOT commented out and NOT passing `nil` for the forwarder
    - `grep -c "typebot.NewForwarder" cmd/pergo/main.go` returns 1
    - `grep -c "integration/typebot" cmd/pergo/main.go` returns at least 1 (the import)
    - `grep -n "NewDefaultRouter" cmd/pergo/main.go` shows the call passing both `chatwootSyncer` and `typebotForwarder` (not `nil`)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin operator → form (browser) | Trusted (workspace session) but inputs are still validated server-side. |
| Typebot external → webhook (POST /api/integrations/typebot) | Untrusted: authenticated via `?token=<api_key>` query param; workspace_id derived from `AuthMiddleware` context, not payload (V4). |
| Form → DB | Trusted (admin session) but tenant-scoped: `connection_id` is validated against the same workspace's `connections` rows (V11). |
| Form → JSON storage envelope | Plaintext JSON is encrypted at rest by `IntegrationRepository.Save` (V6) using AES-256-GCM. |
| Composition root wiring | All integration forwarders are constructed in `cmd/pergo/main.go` and passed explicitly (V1) — no reflection, no setter-injection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24.1-01 | Tampering (composition) | `cmd/pergo/main.go:212-215` — current `NewDefaultRouter(chatwootSyncer, nil)` | critical | mitigate | Wire `typebot.NewForwarder` and pass to `NewDefaultRouter` as the second argument. Add `TestCompositionRoot_AllIntegrationsWired` regression test. |
| T-24.1-02 | Tampering (data) | `templates/pages/typebot_settings.templ` form | high | mitigate | Form requires `bot_public_token` and `connection_id` non-empty (server-side validation in PostSettings); `connection_id` is parsed as `uuid.UUID` before persisting. |
| T-24.1-03 | Information Disclosure (schema drift) | Stored `typebot.Config` JSON | low | accept | Legacy phase-22 stored configs with missing per-bot fields default to zero values on read (no panic, no data leak). No production operator has stored a phase-22 config (TYPE-04 unsatisfied). |
| T-24.1-04 | Spoofing (webhook) | POST /api/integrations/typebot | high | mitigate | `AuthMiddleware` enforces `?token=<api_key>`; workspace_id is derived from the validated API key, not from the payload (V4). Missing workspace in context returns 401. |
| T-24.1-05 | Tampering (cross-tenant) | `connection_id` from form payload | high | mitigate | `connection_id` validated as `uuid.UUID`; in the next phase, validate `(workspace_id, id)` ownership against `connections` table before persisting. **For this phase, validation is "is valid UUID" only** — the cross-tenant ownership check is documented in the research and is a follow-up. |
| T-24.1-06 | Denial of Service (forwarder) | `trigger_words: []` for all bots | low | accept | Forwarder returns early (`forwarder.go:127-133`) — no bot matches, no API call, no NATS publish. Benign. |
| T-24.1-SC | Tampering (dependency) | npm/pip/cargo installs (n/a) | n/a | n/a | This phase introduces zero new external Go dependencies. All packages referenced (`internal/integration/typebot`, `internal/inbound`, `internal/repository`) are in-tree. |
| T-24.1-07 | Repudiation (audit) | `typebot.Forwarder.SyncInboundMessage` errors | low | mitigate | Existing `internal/inbound/router.go:46` `slog.Error` line on the typebot-goroutine path captures and logs all errors with `contact_id` and `workspace_id` context. Unchanged. |
| T-24.1-08 | Elevation of Privilege (input) | `connection_id` not owned by workspace | medium | mitigate (partial) | Task 1 validates `connection_id` is a valid UUID. Full tenant-isolation check (V11) — query `connections` table with `(workspace_id, id)` predicate — is out of scope for this phase but is a documented follow-up. The composition-root smoke test does not exercise this path. |

## ASVS 1 Compliance Notes

- **V1 Architecture:** Composition root wires all integrations explicitly (no setter injection, no reflection). Verified by `TestCompositionRoot_AllIntegrationsWired`.
- **V2/V4 Authentication:** Webhook protected by `AuthMiddleware`; workspace_id from context, not payload. Unchanged.
- **V5 Input Validation:** Form now validates `bot_id`, `bot_public_token`, `connection_id` non-empty + `connection_id` is a parseable UUID before persisting. Webhook handler already validates `message`, `connection_id`, `contact_id` non-empty.
- **V6 Cryptography:** `IntegrationRepository.Save` encrypts `Config` via AES-256-GCM. Unchanged.
- **V8 Data Protection:** Stored `public_token` is encrypted at rest via the same `integrations.config` envelope. Unchanged.
- **V11 Business Logic:** `connection_id` is validated as a valid UUID; full `(workspace_id, id)` ownership check is a documented follow-up — flagged in the threat register as `mitigate (partial)`.
</threat_model>

<verification>
Run the full verification suite for Phase 24.1:

```bash
# 1. Build the binary
go build ./...

# 2. Run the targeted test set (per task commits)
go test ./internal/integration/typebot/ -run TestConfigRoundTrip -v
go test ./internal/api/handler/admin/ -run TestTypebotSettings -v
go test ./internal/api/handler/ -run TestTypebotWebhook -v
go test ./cmd/pergo/ -run TestCompositionRoot_AllIntegrationsWired -v

# 3. Run the broader regression set (post-wave merge)
go test ./cmd/pergo/... ./internal/api/handler/... ./internal/inbound/... \
        ./internal/integration/typebot/... ./internal/integration/chatwoot/...

# 4. Full suite with race detector (phase gate)
go test ./... -race -count=1
```

All four task-level tests must be green. The broader regression set must remain green. The full suite must be green before `/gsd-verify-work` is invoked.

**Manual cross-checks (defense in depth, not required for plan completion):**
- `grep -n "NewDefaultRouter" cmd/pergo/main.go` — the call must pass `typebotForwarder` (not `nil`) as the second arg.
- `grep -c "internal/integration/typebot" cmd/pergo/main.go` — must be ≥ 1 (the import).
- `grep -n "typebot.NewForwarder" cmd/pergo/main.go` — must return a single non-commented-out call.
- `grep -n "typebot.Config" internal/api/handler/admin/typebot.go` — must reference the storage struct, not `pages.TypebotConfig`.
- `grep -n "is_default" templates/pages/typebot_settings.templ` — must show the new checkbox.
</verification>

<success_criteria>
Phase 24.1 is complete when:

1. **TYPE-04 satisfied:** `cmd/pergo/main.go` constructs `typebot.NewForwarder(typebotSessionRepo, integrationRepo, publisher)` and passes the result to `inbound.NewDefaultRouter(chatwootSyncer, typebotForwarder)`. The import `"github.com/pablojhp.pergo/internal/integration/typebot"` is present. The `internal/inbound/router.go:41` nil-check is no longer triggered in production.

2. **TYPE-01 satisfied:** `templates/pages/typebot_settings.templ` and `internal/api/handler/admin/typebot.go` produce a JSON envelope that round-trips losslessly into `typebot.Config` (per-bot `PublicToken`, `ConnectionID`, `TriggerWords []string`, `IsDefault`). The form validates `bot_id`, `bot_public_token`, `connection_id` non-empty before persisting.

3. **TYPE-02 verified:** `TestTypebotWebhookHandler_HappyPath` is green and asserts the 200 path publishes exactly one `domain.QueueMessage` to `messages.outbound` with the correct `WorkspaceID`, `ConnectionID`, `SenderIdentity`, `To`, `Channel`, `Body`.

4. **Regression defense in place:** `TestCompositionRoot_AllIntegrationsWired` in `cmd/pergo/main_test.go` is green and would fail if either `chatwootSyncer` or `typebotForwarder` is dropped from the wiring.

5. **No new external dependencies:** `go.mod` is unchanged.

6. **Full test suite green:** `go test ./... -race -count=1` exits 0.

7. **`go build ./...` exits 0.**
</success_criteria>

<output>
Create `.planning/phases/24.1-close-gap-wire-typebot-forwarder-and-reconcile-form-schema/24.1-01-SUMMARY.md` when complete.
</output>
