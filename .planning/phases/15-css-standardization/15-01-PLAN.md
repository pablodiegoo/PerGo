---
phase: 15
plan: 01
mode: standard
depends_on: [14]
files_modified:
  - templates/pages/audit.templ
  - templates/pages/telemetry.templ
  - templates/pages/workspace.templ
  - templates/pages/webhooks.templ
  - templates/pages/onboarding.templ
  - templates/pages/devices.templ
  - templates/pages/campaigns.templ
autonomous: true
---

# Plan 01: CSS Standardization Refactoring

Refactor all dashboard templates to utilize the design system visual tokens, ensuring visual consistency across headers, card containers, form fields, buttons, tables, and status badges.

## Wave 1: Core Layouts & Audit Pages (Unification)

### Task 1.1: Refactor Audit Logs & Telemetry Pages
- **`<read_first>`**:
  - `templates/pages/audit.templ`
  - `templates/pages/telemetry.templ`
  - `.planning/phases/15-css-standardization/15-RESEARCH.md`
- **`<action>`**:
  - In `templates/pages/audit.templ`, update the filter inputs, labels, and table definition to match the standardized structure (zinc-50 header bg, hover transitions, px-6 py-4 cell padding).
  - In `templates/pages/telemetry.templ`, apply the standard typography to the telemetry dashboard title, card container wrappers, and telemetry table rows.
- **`<acceptance_criteria>`**:
  - `templates/pages/audit.templ` contains `LogsHeaderTabs("outbound")` and `LogsHeaderTabs("inbound")` calls.
  - Both templates build successfully without templ errors.

---

## Wave 2: Setup & Configuration Pages

### Task 2.1: Refactor Workspace, Webhooks & Onboarding Pages
- **`<read_first>`**:
  - `templates/pages/workspace.templ`
  - `templates/pages/webhooks.templ`
  - `templates/pages/onboarding.templ`
- **`<action>`**:
  - In `templates/pages/workspace.templ`, update API Key creation form elements (labels, text inputs, button elements) and the keys table.
  - In `templates/pages/webhooks.templ`, align webhook configuration card containers, form selectors, subscription inputs, and action buttons.
  - In `templates/pages/onboarding.templ`, style onboarding setup text inputs, labels, and card wrappers.
- **`<acceptance_criteria>`**:
  - Form tags use `flex flex-col gap-1` with upper-case tracking-wider labels.
  - Buttons use standard class list `btn btn-black bg-zinc-950 hover:bg-zinc-900 text-white border-none`.
  - All three templates compile successfully.

---

## Wave 3: Operational Views (Devices & Campaigns)

### Task 3.1: Refactor Devices & Campaigns Pages
- **`<read_first>`**:
  - `templates/pages/devices.templ`
  - `templates/pages/campaigns.templ`
- **`<action>`**:
  - In `templates/pages/devices.templ`, update connection status badges (Conectado, Pendente, Desconectado), pairing form inputs, code modals, and primary action buttons.
  - In `templates/pages/campaigns.templ`, refactor CSV file upload fields, validation metrics blocks, scheduled datetime inputs, batch throttling inputs, and campaign lists.
- **`<acceptance_criteria>`**:
  - Badges use standard soft background color classes (`bg-emerald-50 text-emerald-700 border-emerald-200` etc.).
  - Card elements use `bg-white border border-zinc-200 rounded-lg p-6 shadow-sm` class definition.
  - Both files compile successfully.

---

## Wave 4: Validation & Verify

### Task 4.1: Code Compilation and Verification
- **`<read_first>`**:
  - `Makefile`
- **`<action>`**:
  - Run template compiler via `$(go env GOPATH)/bin/templ generate`.
  - Execute full test suite `go test ./...` to verify no syntactic or rendering regressions are introduced.
- **`<acceptance_criteria>`**:
  - Template generation terminates with a complete/successful state.
  - `go test ./...` returns `PASS` status.

---

## Artifacts This Phase Produces
- `.planning/phases/15-css-standardization/15-01-PLAN.md` (This file)
- `.planning/phases/15-css-standardization/15-RESEARCH.md`
