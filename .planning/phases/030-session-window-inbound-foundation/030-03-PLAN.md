---
phase: 30
plan: 03
type: backend
wave: 2
depends_on: ["01", "02"]
files_modified:
  - internal/channel/whatsapp/waba_inbound.go
  - internal/inbound/processor.go
autonomous: true
requirements:
  - SESS-01
  - SESS-05
---

<objective>
Update the WABA inbound webhook adapter to detect Click-to-WhatsApp (CTWA) referrals and pass the `entry_point_type` metadata to the `InboundProcessor`, which in turn passes it to the updated `RecipientSessionRepository.Upsert`.
</objective>

<tasks>

### Task 1: Detect CTWA referrals in WABA Webhook
<read_first>
- internal/channel/whatsapp/waba_inbound.go
</read_first>
<action>
In `internal/channel/whatsapp/waba_inbound.go`, define a `wabaReferralObj` struct within the Meta payload structures. Update the payload unmarshaling to extract the `referral` object from incoming messages. In the mapping logic (e.g. `HandlePost` or the specific mapping functions), evaluate if `msg.Referral != nil` or `msg.Referral.SourceType == "ad"`. If true, set `Metadata["entry_point_type"] = "ctwa"` on the resulting `InboundEvent`. Otherwise, set it to `"standard"`.
</action>
<acceptance_criteria>
- Webhook JSON decoding extracts the `referral` object.
- `InboundEvent.Metadata["entry_point_type"]` is set to `"ctwa"` when an ad referral is present.
- `InboundEvent.Metadata["entry_point_type"]` is set to `"standard"` for standard messages.
</acceptance_criteria>
<verify>
grep -A 5 "entry_point_type" internal/channel/whatsapp/waba_inbound.go
</verify>

### Task 2: Pass entry_point_type in InboundProcessor
<read_first>
- internal/inbound/processor.go
- internal/repository/recipient_session.go
</read_first>
<action>
In `internal/inbound/processor.go`, locate the `Process` function where `recipientSessionRepo.Upsert` is called. Extract the `entry_point_type` from `ev.Metadata["entry_point_type"]`. If it's missing, default to `"standard"`. Pass this value as the new argument to the `Upsert` method call.
</action>
<acceptance_criteria>
- `InboundProcessor` extracts `entry_point_type` from metadata.
- `Upsert` is called with the extracted `entry_point_type` value.
</acceptance_criteria>
<verify>
grep -A 3 "recipientSessionRepo.Upsert" internal/inbound/processor.go
</verify>

</tasks>

<verification>
## Verification
- Manually run tests or review code to ensure the integration between `waba_inbound.go` and `processor.go` passes the `entry_point_type` all the way down to the database upsert.
</verification>

<must_haves>
## Must-Haves
truths:
  - `waba_inbound.go` correctly assigns `entry_point_type` in the `Metadata` map.
  - `InboundProcessor` passes the `entry_point_type` down to `RecipientSessionRepository.Upsert`.
</must_haves>

## Artifacts this phase produces
- `wabaReferralObj` in `internal/channel/whatsapp/waba_inbound.go` — Struct to unmarshal Meta referral data.
- Payload mapping in `internal/channel/whatsapp/waba_inbound.go` — Updated to set `entry_point_type` metadata.
- `Process` in `internal/inbound/processor.go` — Updated to extract and pass `entry_point_type` to `Upsert`.
