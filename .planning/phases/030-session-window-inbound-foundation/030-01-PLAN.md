---
phase: 30
plan: 01
type: backend
wave: 1
depends_on: []
files_modified:
  - internal/platform/postgres/migrations/031_extend_recipient_sessions.sql
  - internal/repository/recipient_session.go
autonomous: true
requirements:
  - SESS-01
  - SESS-05
---

<objective>
Create a database migration to add `entry_point_type` and `notified_expiring_at` columns to `recipient_sessions`, and update the `RecipientSessionRepository` to support these new fields.
</objective>

<tasks>

### Task 1: Create database migration
<read_first>
- internal/platform/postgres/migrations/
</read_first>
<action>
Create a new goose migration file `internal/platform/postgres/migrations/031_extend_recipient_sessions.sql` that adds `entry_point_type VARCHAR(20) NOT NULL DEFAULT 'standard'` and `notified_expiring_at TIMESTAMPTZ` to the `recipient_sessions` table. It must also create a partial index `idx_recipient_sessions_expiring` on `last_inbound_at` where `notified_expiring_at IS NULL`. Include corresponding `DOWN` statements to drop the index and columns.
</action>
<acceptance_criteria>
- File `031_extend_recipient_sessions.sql` is created with valid goose `-- +goose Up` and `-- +goose Down` sections.
- Migration contains statements to add `entry_point_type` and `notified_expiring_at` columns.
- Migration creates a partial index on `last_inbound_at WHERE notified_expiring_at IS NULL`.
</acceptance_criteria>
<verify>
cat internal/platform/postgres/migrations/031_extend_recipient_sessions.sql
</verify>

### Task 2: Update RecipientSessionRepository Upsert
<read_first>
- internal/repository/recipient_session.go
</read_first>
<action>
Update the `Upsert` function signature in `internal/repository/recipient_session.go` to accept `entryPointType string`. Modify the SQL `INSERT ... ON CONFLICT` statement to insert `entry_point_type` and `notified_expiring_at` (set to `NULL`). In the `DO UPDATE` clause, set `last_inbound_at = EXCLUDED.last_inbound_at`, `entry_point_type = EXCLUDED.entry_point_type`, and `notified_expiring_at = NULL` (resetting the notification state on new inbound message).
</action>
<acceptance_criteria>
- `Upsert` signature includes `entryPointType string` argument.
- SQL statement handles insertion and conflict update for `entry_point_type` and resetting `notified_expiring_at` to `NULL`.
</acceptance_criteria>
<verify>
grep -A 10 "func (r \*RecipientSessionRepository) Upsert" internal/repository/recipient_session.go
</verify>

### Task 3: Implement GetExpiringSessions in RecipientSessionRepository
<read_first>
- internal/repository/recipient_session.go
</read_first>
<action>
Add a new method `GetExpiringSessions(ctx context.Context, start time.Time, end time.Time) ([]RecipientSession, error)` to `RecipientSessionRepository`. The SQL query must select sessions where `last_inbound_at` is between `start` and `end`, AND `notified_expiring_at IS NULL`. Update the `RecipientSession` struct to include `EntryPointType` and `NotifiedExpiringAt` if it doesn't already. Add a method `MarkNotifiedExpiring(ctx context.Context, workspaceID uuid.UUID, recipientPhone string, channel string, recipientIdentity string) error` to set `notified_expiring_at = time.Now().UTC()`.
</action>
<acceptance_criteria>
- `GetExpiringSessions` method is implemented and queries by `last_inbound_at` with `notified_expiring_at IS NULL`.
- `MarkNotifiedExpiring` method is implemented and updates `notified_expiring_at`.
</acceptance_criteria>
<verify>
grep "func (r \*RecipientSessionRepository) GetExpiringSessions" internal/repository/recipient_session.go
</verify>

</tasks>

<verification>
## Verification
- Run database migrations locally using goose to verify valid SQL syntax.
- Build the project to ensure `RecipientSessionRepository` compiles correctly.
</verification>

<must_haves>
## Must-Haves
truths:
  - `internal/platform/postgres/migrations/031_extend_recipient_sessions.sql` exists and contains correct goose up/down syntax.
  - `Upsert` method in `RecipientSessionRepository` accepts `entryPointType` and resets `notified_expiring_at` to NULL.
  - `GetExpiringSessions` exists and filters by `notified_expiring_at IS NULL`.
</must_haves>

## Artifacts this phase produces
- `internal/platform/postgres/migrations/031_extend_recipient_sessions.sql` — Database migration adding new session columns.
- `Upsert` in `internal/repository/recipient_session.go` — Modified to support new columns.
- `GetExpiringSessions` in `internal/repository/recipient_session.go` — New method to fetch expiring sessions.
- `MarkNotifiedExpiring` in `internal/repository/recipient_session.go` — New method to mark sessions as notified.
