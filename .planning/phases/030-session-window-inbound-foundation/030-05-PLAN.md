---
phase: 30
plan: 05
type: backend
wave: 2
depends_on: ["02"]
files_modified:
  - internal/channel/whatsapp/waba.go
autonomous: true
requirements:
  - SESS-03
---

<objective>
Implement dispatch-time re-validation in the WABA worker using a 5-minute safety buffer to catch messages delayed in the queue.
</objective>

<tasks>

### Task 1: Add Session Window Check to WABA Dispatcher
<read_first>
- internal/channel/whatsapp/waba.go
</read_first>
<action>
In `internal/channel/whatsapp/waba.go`, inside the `WABAAdapter.Dispatch` method, add a session window check before executing the Meta Graph API request. Only run this check if the message is NOT a template (e.g., `m.TemplateName == ""`). Call `windowChecker.IsWindowOpen(ctx, workspaceID, m.To, "whatsapp_cloud", conn.SenderIdentity, 5*time.Minute)`. If the window is closed, return a terminal error (e.g., `channel.NewTerminalError(ErrSessionWindowExpired)`). Also ensure that `message.window_expired` webhook event is somehow handled (the `DispatchOrchestrator` handles terminal errors and marks failed, but ensure an event is emitted if standard failure events don't cover it).
</action>
<acceptance_criteria>
- `WABAAdapter.Dispatch` executes `IsWindowOpen` for non-template messages.
- A `5 * time.Minute` safety buffer is used.
- A terminal error is returned if the window is closed, preventing NATS retries.
</acceptance_criteria>
<verify>
grep -A 10 "IsWindowOpen" internal/channel/whatsapp/waba.go
</verify>

</tasks>

<verification>
## Verification
- Write or manually test a scenario where a WABA message sits in the queue until the session expires (or within the 5m safety buffer), and verify the worker fails fast without retrying.
</verification>

<must_haves>
## Must-Haves
truths:
  - `WABAAdapter.Dispatch` blocks non-template messages near expiration using a 5-minute safety buffer.
  - The returned error is wrapped as a `TerminalError` to prevent JetStream retries.
</must_haves>

## Artifacts this phase produces
- `WABAAdapter.Dispatch` in `internal/channel/whatsapp/waba.go` — Updated to re-validate the session window.
