---
wave: 1
depends_on: []
files_modified:
  - internal/domain/message.go
  - internal/inbound/processor.go
  - internal/channel/instagram/adapter.go
  - internal/channel/instagram/inbound.go
  - cmd/pergo/main.go
autonomous: true
requirements:
  - INSTA-01
---

# Plan: Implement Instagram Stories handling and Quick Replies mapping

## Verification Criteria
- `ValidChannels` includes `"instagram"`.
- `InboundStoryEvent` struct is present in `internal/inbound/processor.go`.
- `InstagramAdapter` is implemented and registered in the dispatcher.
- Instagram Quick Replies are correctly mapped to outbound Meta Graph API requests.
- Instagram Story Mentions and Replies are properly processed by the inbound adapter.

## must_haves
- All requirement IDs from REQUIREMENTS.md must be mapped.
- Instagram Quick Replies MUST map to the generic `Interactive` payload schema.
- IG Story Mentions and Replies MUST map to a unified `story_event` type with a `subtype`.
- Raw Meta CDN URLs MUST be passed for media.

## Artifacts this phase produces
- `InboundStoryEvent` (struct in `internal/inbound/processor.go`)
- `instagram.Adapter` (struct in `internal/channel/instagram/adapter.go`)
- `instagram.InboundAdapter` (struct in `internal/channel/instagram/inbound.go`)
- `internal/channel/instagram/adapter.go` (new file)
- `internal/channel/instagram/inbound.go` (new file)

## Tasks

### 1. Update Domain Models
```xml
<task>
  <read_first>
    - internal/domain/message.go
    - internal/inbound/processor.go
  </read_first>
  <acceptance_criteria>
    - "instagram" is a key in `ValidChannels` with a `true` value.
    - `InboundStoryEvent` struct with fields `Subtype` (string) and `MediaURL` (string) is added.
    - `Story *InboundStoryEvent` field is added to `InboundEvent` and `InboundEventPayload`.
    - `go test ./internal/domain/...` and `go test ./internal/inbound/...` pass.
  </acceptance_criteria>
  <action>
    Add `"instagram": true` to the `ValidChannels` map in `internal/domain/message.go`.
    Create a new struct `InboundStoryEvent` in `internal/inbound/processor.go` with JSON tags `subtype` and `media_url`.
    Add `Story *InboundStoryEvent` to both `InboundEvent` and `InboundEventPayload` in `internal/inbound/processor.go` with JSON tag `story_event,omitempty`.
  </action>
</task>
```

### 2. Implement Instagram Outbound Adapter
```xml
<task>
  <read_first>
    - internal/channel/instagram/adapter.go
    - internal/channel/waba/adapter.go
    - internal/domain/message.go
  </read_first>
  <acceptance_criteria>
    - `InstagramAdapter` implements `channel.Adapter`.
    - `Send` method properly formats the Meta Graph API payload for Instagram messages, including mapping `domain.Interactive` (Quick Replies).
    - `go build ./internal/channel/instagram/...` succeeds.
  </acceptance_criteria>
  <action>
    Create `internal/channel/instagram/adapter.go`.
    Define `InstagramAdapter` struct implementing `channel.Adapter`.
    Implement `Send` to map the unified `channel.MessagePayload` to Meta Graph API Instagram format. Map `channel.MessagePayload.Interactive` into IG Quick Replies.
  </action>
</task>
```

### 3. Implement Instagram Inbound Adapter
```xml
<task>
  <read_first>
    - internal/channel/instagram/inbound.go
    - internal/channel/waba/inbound.go
    - internal/inbound/processor.go
  </read_first>
  <acceptance_criteria>
    - `InstagramInboundAdapter` implements `channel.InboundAdapter`.
    - Story Mentions and Replies map to `InboundEvent.Story` with subtype and raw `MediaURL`.
    - Quick reply responses map to `InboundEvent.Interactive`.
    - `go build ./internal/channel/instagram/...` succeeds.
  </acceptance_criteria>
  <action>
    Create `internal/channel/instagram/inbound.go`.
    Define `InstagramInboundAdapter` struct implementing `channel.InboundAdapter`.
    Implement `Parse` to handle Instagram-specific webhooks (`messaging_product: "instagram"`). Map story events to `InboundStoryEvent` and quick reply callbacks to `InboundInteractive`.
  </action>
</task>
```

### 4. Register Instagram Adapter
```xml
<task>
  <read_first>
    - cmd/pergo/main.go
    - internal/channel/instagram/adapter.go
  </read_first>
  <acceptance_criteria>
    - `instagram.NewAdapter()` is initialized.
    - `dispatcherRegistry.Register("instagram", ...)` is called.
    - `go build ./cmd/pergo` succeeds.
  </acceptance_criteria>
  <action>
    Import `internal/channel/instagram` in `cmd/pergo/main.go`.
    Initialize the Instagram adapter and register it with the dispatcher registry using the key `"instagram"`.
  </action>
</task>
```
