---
phase: 01-Foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - internal/platform/obs/pprof.go
  - internal/platform/obs/expvar.go
  - internal/platform/shutdown/orchestrator.go
  - cmd/omnigo/main.go
autonomous: true
requirements:
  - INFRA-05
  - OBS-01
  - OBS-02
  - OBS-04

must_haves:
  truths:
    - "net/http/pprof available on localhost:6060 (separate from public port)"
    - "expvar exposes memory stats, goroutine count, and custom audit drop counter"
    - "SIGTERM triggers ordered shutdown: Echo → drain workers → flush audit → close NATS → close pgx"
    - "Shutdown completes within 30 seconds (Echo.Shutdown timeout)"
    - "Health endpoints still respond during shutdown drain period"
  artifacts:
    - path: "internal/platform/obs/pprof.go"
      provides: "pprof debug server on localhost:6060"
      exports: ["StartDebugServer"]
    - path: "internal/platform/obs/expvar.go"
      provides: "expvar metrics counters"
      exports: ["RegisterCounter", "AuditDrops"]
    - path: "internal/platform/shutdown/orchestrator.go"
      provides: "Graceful shutdown sequence coordinator"
      exports: ["Orchestrator", "Shutdown"]
    - path: "cmd/omnigo/main.go"
      provides: "Composition root with full shutdown wiring"
  key_links:
    - from: "cmd/omnigo/main.go"
      to: "internal/platform/shutdown/orchestrator.go"
      via: "Main creates orchestrator and registers cleanup functions"
      pattern: "shutdown\\.NewOrchestrator\\("
    - from: "internal/platform/shutdown/orchestrator.go"
      to: "internal/platform/audit/writer.go"
      via: "Orchestrator calls auditWriter.Close() during shutdown"
      pattern: "auditWriter\\.Close\\("
    - from: "internal/platform/obs/expvar.go"
      to: "internal/platform/audit/batch.go"
      via: "Audit batch writer increments expvar drop counter on flush failure"
      pattern: "AuditDrops\\.Add\\("
    - from: "internal/platform/obs/pprof.go"
      to: "cmd/omnigo/main.go"
      via: "Main starts debug server on separate listener"
      pattern: "StartDebugServer\\("
---

<objective>
Complete the foundation with observability and graceful shutdown: pprof debug server, expvar metrics, and an orchestrated shutdown sequence that drains all resources in the correct order.

Purpose: Production-readiness — runtime profiling, live metrics, and clean shutdown prevent resource leaks and enable debugging in production.
Output: Server with debug endpoint on :6060, expvar counters, and SIGTERM-triggered ordered shutdown completing within 30 seconds.
</objective>

<execution_context>
@/home/pablo/Coding/OmniGo/.opencode/gsd-core/workflows/execute-plan.md
@/home/pablo/Coding/OmniGo/.opencode/gsd-core/templates/summary.md
</context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-Foundation/CONTEXT.md
@.planning/phases/01-Foundation/01-RESEARCH.md
@.planning/phases/01-Foundation/01-01-SUMMARY.md
@.planning/phases/01-Foundation/01-02-SUMMARY.md
@.planning/phases/01-Foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing end-to-end test for pprof, expvar, and graceful shutdown</name>
  <files>cmd/omnigo/obs_test.go</files>
  <read_first>
    - .planning/phases/01-Foundation/01-RESEARCH.md (Code Examples: pprof + expvar Setup)
    - .planning/phases/01-Foundation/CONTEXT.md (Observability section, Graceful Shutdown Order)
    - .planning/phases/01-Foundation/01-03-SUMMARY.md (audit writer Close() details)
  </read_first>
  <behavior>
    - Test 1: Debug server starts on localhost:6060, GET /debug/pprof/ returns 200
    - Test 2: expvar handler available at /debug/vars returns JSON with memstats
    - Test 3: Custom expvar counter (audit_drops) increments on audit overflow
    - Test 4: Orchestrator registers cleanup functions and executes them in reverse order on Shutdown()
    - Test 5: Shutdown completes within 5 seconds with mock cleanup functions
    - Test 6: Shutdown with slow cleanup (模拟 2s delay) still completes within 30 seconds
    - Test 7: Shutdown context is cancelled after completion
    - Test 8: Multiple Shutdown() calls are safe (idempotent)
  </behavior>
  <action>
    Create tests in cmd/omnigo/obs_test.go that exercise observability and shutdown:

    1. Define TestPprofServer: start debug server on random port, GET /debug/pprof/, assert 200 + Content-Type is text/html
    2. Define TestExpvarHandler: start debug server, GET /debug/vars, assert 200 + response is valid JSON containing "memstats"
    3. Define TestAuditDropCounter: register custom expvar counter via obs.RegisterCounter("audit_drops"), increment it, query /debug/vars, assert counter value matches
    4. Define TestOrchestratorOrder: create orchestrator, register 3 cleanup functions with recorded execution order, call Shutdown(), assert functions executed in reverse registration order (LIFO)
    5. Define TestOrchestratorTimeout: register cleanup that takes 2 seconds, call Shutdown() with 5-second context, assert completes within 5 seconds
    6. Define TestOrchestratorSlowCleanup: register cleanup that takes 2 seconds, call Shutdown() with 30-second context, assert completes and context is cancelled
    7. Define TestOrchestratorIdempotent: call Shutdown() twice, assert no panic, second call is no-op
    8. Tests reference constructors that do not exist yet — this is intentional (RED phase)
  </action>
  <verify>
    <automated>cd /home/pablo/Coding/OmniGo && go test ./cmd/omnigo/... -run TestObs -v -count=1 2>&1 | grep -E 'FAIL|PASS|SKIP|cannot'</automated>
  </verify>
  <acceptance_criteria>
    - File cmd/omnigo/obs_test.go exists
    - Tests reference: obs.StartDebugServer(), obs.RegisterCounter(), shutdown.NewOrchestrator(), orchestrator.Register(), orchestrator.Shutdown()
    - Tests compile or show expected failures (RED phase)
    - Test output shows FAIL or compilation error for missing implementations
  </acceptance_criteria>
  <done>Integration test file exists with 8 test functions covering pprof, expvar, shutdown order, timeout, and idempotency; tests fail because implementations do not exist yet (RED phase)</done>
</task>

<task type="auto">
  <name>Task 2: Implement pprof, expvar, shutdown orchestrator, and wire into composition root</name>
  <files>
    internal/platform/obs/pprof.go, internal/platform/obs/expvar.go,
    internal/platform/shutdown/orchestrator.go,
    cmd/omnigo/main.go
  </files>
  <read_first>
    - cmd/omnigo/obs_test.go (from Task 1 — the tests define the expected API surface)
    - .planning/phases/01-Foundation/01-RESEARCH.md (Code Examples: pprof + expvar Setup)
    - .planning/phases/01-Foundation/CONTEXT.md (Observability, Graceful Shutdown Order)
    - cmd/omnigo/main.go (from Plans 1-3 — current composition root)
  </read_first>
  <action>
    Implement observability and shutdown to make the tests from Task 1 pass:

    1. **internal/platform/obs/pprof.go**:
       - `StartDebugServer(addr string) *http.Server` — creates an http.Server on the given address with:
         - `/debug/pprof/` handler (import `_ "net/http/pprof"` for side-effect registration)
         - `/debug/vars` handler via `expvar.Handler()`
       - Starts ListenAndServe in a goroutine
       - Returns the server for graceful shutdown
       - Use `net/http.NewServeMux()` (not DefaultServeMux) for isolation

    2. **internal/platform/obs/expvar.go**:
       - `var AuditDrops = expvar.NewInt("audit_drops")` — package-level expvar counter for audit channel overflows
       - `RegisterCounter(name string) *expvar.Int` — registers a new expvar.Int counter; returns it for incrementing
       - Export AuditDrops for use by audit writer

    3. **internal/platform/shutdown/orchestrator.go**:
       - `Orchestrator` struct: functions ([]func() error), mu (sync.Mutex), done (sync.Once)
       - `NewOrchestrator() *Orchestrator` — creates empty orchestrator
       - `Register(fn func() error)` — appends cleanup function to list
       - `Shutdown(ctx context.Context) error` — executes all registered functions in reverse order (LIFO), respects context deadline, returns first error encountered, idempotent (second call returns nil)
       - Use `sync.Once` for idempotency
       - Each function gets the context so it can respect cancellation

    4. **cmd/omnigo/main.go** (final update — full composition root):
       - Import all internal packages
       - Load config via config.Load()
       - Create pgxpool.Pool, stdlib sql.DB
       - Run goose migrations
       - Connect to NATS
       - Create Echo server with middleware stack: RequestID → TraceMiddleware → slog → Recover → AuthMiddleware (on protected routes)
       - Create repositories: WorkspaceRepository, APIKeyRepository
       - Create audit Writer (buffered, 2 workers)
       - Create Encryptor
       - Start debug server on config.DebugPort via obs.StartDebugServer()
       - Register shutdown orchestrator with cleanup functions in this order:
         1. Stop Echo server (echo.Shutdown with 30s timeout)
         2. Stop debug server (debugServer.Shutdown)
         3. Close audit writer (auditWriter.Close)
         4. Close NATS connection (natsConn.Close)
         5. Close pgxpool (pool.Close)
         6. Close stdlib sql.DB (sqlDB.Close)
       - Handle SIGTERM/SIGINT: create context with signal.NotifyContext, on signal call orchestrator.Shutdown()
       - Register health handlers: GET /healthz, GET /readyz
       - Register a test route: GET /api/v1/me (returns workspace_id from auth context)
       - Start Echo on config.ServerPort

    Ensure the shutdown order matches CONTEXT.md: HTTP → drain → audit → NATS → PostgreSQL.
  </action>
  <verify>
    <automated>cd /home/pablo/Coding/OmniGo && go build ./cmd/omnigo && echo "BUILD OK" && go vet ./... && echo "VET OK"</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./cmd/omnigo` succeeds with exit code 0
    - `go vet ./...` passes with no warnings
    - internal/platform/obs/pprof.go exports StartDebugServer
    - internal/platform/obs/expvar.go exports AuditDrops and RegisterCounter
    - internal/platform/shutdown/orchestrator.go exports Orchestrator with Register and Shutdown
    - cmd/omnigo/main.go wires all components: Echo, pgxpool, goose, NATS, audit, auth, debug server, shutdown
    - Shutdown order: Echo → debug → audit → NATS → pgxpool → sqlDB
    - `go test ./cmd/omnigo/... -run TestObs -short -count=1` passes (GREEN phase)
    - `go test ./... -short` passes (all tests from Plans 1-4)
  </acceptance_criteria>
  <done>Observability and shutdown complete: pprof on :6060, expvar counters, orchestrated LIFO shutdown. Full composition root wires all components. All tests from Plans 1-4 pass. Server boots, authenticates, logs audit events, exposes debug metrics, and shuts down gracefully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Debug server → localhost | pprof/expvar exposed only on localhost:6060, not public |
| Shutdown orchestrator → all subsystems | Orchestrator has access to close all resources |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | pprof on public port | high | mitigate | Debug server binds to localhost only (127.0.0.1:6060); not exposed via Docker or reverse proxy |
| T-04-02 | Denial of Service | pprof CPU profiling | medium | accept | pprof is operator-only; CPU profiling requires auth at infrastructure level |
| T-04-03 | Tampering | Shutdown race condition | medium | mitigate | sync.Once ensures idempotency; context deadline prevents infinite hang |
| T-04-04 | Information Disclosure | expvar memory stats | low | accept | expvar on localhost only; memstats are non-sensitive operational data |
| T-04-SC | Tampering | Go module dependencies | high | mitigate | pprof and expvar are stdlib packages; no external dependencies added |
</threat_model>

<verification>
- `go build ./cmd/omnigo` succeeds
- `go vet ./...` passes
- `go test ./... -short` passes (all 4 plans' tests)
- Manual: `curl localhost:6060/debug/pprof/` returns pprof index page
- Manual: `curl localhost:6060/debug/vars` returns JSON with memstats
- Manual: send SIGTERM, observe ordered shutdown logs within 30 seconds
</verification>

<success_criteria>
pprof available on localhost:6060. expvar exposes memstats and audit_drops counter. SIGTERM triggers ordered shutdown (Echo → debug → audit → NATS → pgxpool → sqlDB) completing within 30 seconds. All Phase 1 tests pass. Server is production-ready foundation.
</success_criteria>

<output>
Create `.planning/phases/01-Foundation/01-04-SUMMARY.md` when done
</output>
