---
phase: 03-ingest-api-queue
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/domain/message.go
  - internal/domain/message_test.go
  - internal/api/handler/message.go
  - internal/api/handler/message_test.go
autonomous: true
requirements: [API-01, API-02, API-03, API-04, API-05]

must_haves:
  truths:
    - "POST /messages accepts a valid JSON payload and returns 202 Accepted"
    - "Response includes X-Trace-Id header for end-to-end correlation"
    - "Invalid payloads return structured 400 errors with field-level details"
    - "Missing or invalid Authorization header returns 401"
    - "Message status enum defines queued, sent, delivered, read, failed"
    - "TTL field rejects zero or negative values"
  artifacts:
    - path: "internal/domain/message.go"
      provides: "Message types, status enum, request/response structs, validation"
      exports: ["MessageStatus", "CreateMessageRequest", "CreateMessageResponse", "ErrorResponse", "FieldError", "ValidateMessage"]
    - path: "internal/api/handler/message.go"
      provides: "POST /messages handler with dependency injection"
      exports: ["MessageHandler", "Publisher"]
    - path: "internal/domain/message_test.go"
      provides: "Unit tests for domain types and validation"
    - path: "internal/api/handler/message_test.go"
      provides: "Handler tests for happy path and error cases"
  key_links:
    - from: "internal/api/handler/message.go"
      to: "internal/domain/message.go"
      via: "Handler imports domain types for request binding and validation"
      pattern: "domain\\.(CreateMessageRequest|ValidateMessage|ErrorResponse)"
    - from: "internal/api/handler/message.go"
      to: "internal/api/middleware/trace.go"
      via: "Handler extracts trace_id from request context"
      pattern: "middleware\\.TraceIDFrom"
    - from: "internal/api/handler/message.go"
      to: "internal/platform/postgres/tenant/tenant.go"
      via: "Handler extracts workspace_id from request context"
      pattern: "tenant\\.WorkspaceIDFrom"
---

<objective>
Unified POST /messages endpoint: domain types, payload validation, and structured error responses.

Purpose: Establishes the message ingestion contract — the API surface that all subsequent queue and dispatch work builds on. After this plan, a backend developer can POST a message and receive a 202 Accepted with trace correlation, or a structured error explaining what went wrong.

Output: Domain types (message.go), HTTP handler (message.go), and their test suites.
</objective>

<execution_context>
@/home/pablo/Coding/PerGo/.opencode/gsd-core/workflows/execute-plan.md
@/home/pablo/Coding/PerGo/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ingest-api-queue/03-CONTEXT.md
@.planning/phases/03-ingest-api-queue/03-RESEARCH.md
@.planning/phases/01-Foundation/01-02-SUMMARY.md
@.planning/phases/01-Foundation/01-03-SUMMARY.md
@internal/api/middleware/trace.go
@internal/api/middleware/auth.go
@internal/platform/postgres/tenant/tenant.go
@internal/config/config.go
@cmd/pergo/main.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Domain types, validation, and error contract (RED + GREEN)</name>
  <files>internal/domain/message.go, internal/domain/message_test.go</files>
  <behavior>
    - MessageStatus constants: "queued", "sent", "delivered", "read", "failed"
    - Valid channels: "whatsapp", "whatsapp_cloud", "telegram"
    - CreateMessageRequest binds from JSON with to (required), channel (required, oneof), body (optional), metadata (optional map), ttl_seconds (optional *int)
    - ValidateMessage returns nil for valid request, ErrorResponse with field-level details for invalid
    - ValidateMessage rejects: empty to, empty channel, invalid channel value, ttl_seconds <= 0 when present
    - ErrorResponse marshals to JSON with code, message, more_info, details fields
    - FieldError provides field-level validation detail
  </behavior>
  <action>
Create `internal/domain/message.go` with the following types and functions per D-01, API-03, API-04:

1. MessageStatus string type with constants: StatusQueued, StatusSent, StatusDelivered, StatusRead, StatusFailed (all lowercase per API-03).

2. Status transitions map (documented, not enforced in this phase — Phase 4/5 will use it): queued→sent→delivered→read (happy path), sent→failed (retriable), delivered→failed (terminal), queued→failed (TTL expiry). Mark sent→failed and queued→failed as retriable, delivered→failed as terminal.

3. CreateMessageRequest struct with JSON tags: To string `json:"to" validate:"required"`, Channel string `json:"channel" validate:"required,oneof=whatsapp whatsapp_cloud telegram"`, Body string `json:"body"`, Metadata map[string]string `json:"metadata,omitempty"`, TTLSeconds *int `json:"ttl_seconds,omitempty"`. Add `binding:"required"` tags where Echo v5 expects them.

4. CreateMessageResponse struct: MessageID uuid.UUID `json:"message_id"`, Status MessageStatus `json:"status"`, QueuedAt time.Time `json:"queued_at"`.

5. ErrorResponse struct per API-04: Code string `json:"code"`, Message string `json:"message"`, MoreInfo string `json:"more_info,omitempty"`, Details []FieldError `json:"details,omitempty"`.

6. FieldError struct: Field string `json:"field"`, Message string `json:"message"`.

7. ValidateMessage(req *CreateMessageRequest) *ErrorResponse function that checks: to is non-empty, channel is one of the valid values, ttl_seconds is nil or > 0. Returns nil if valid, pointer to ErrorResponse if invalid. Each failed check appends a FieldError to the Details slice.

Create `internal/domain/message_test.go` with table-driven tests:
- TestMessageStatusValues: verify all five constants exist and are lowercase strings
- TestValidateMessageValid: valid request returns nil
- TestValidateMessageMissingTo: empty to field returns ErrorResponse with code "invalid_payload" and FieldError for "to"
- TestValidateMessageMissingChannel: empty channel returns ErrorResponse with FieldError for "channel"
- TestValidateMessageInvalidChannel: channel="sms" returns ErrorResponse with FieldError for "channel"
- TestValidateMessageZeroTTL: ttl_seconds=0 returns ErrorResponse with FieldError for "ttl_seconds"
- TestValidateMessageNegativeTTL: ttl_seconds=-5 returns ErrorResponse with FieldError for "ttl_seconds"
- TestValidateMessageNilTTL: ttl_seconds=nil is valid (no error)
- TestValidateMessageOptionalBody: body="" is valid (no error)
- TestErrorResponseJSON: ErrorResponse marshals to JSON with correct field names

Run tests to verify RED (tests pass — the implementation is written in the same task since types and validation are inseparable).
  </action>
  <verify>
    <automated>go test ./internal/domain/... -v -count=1</automated>
  </verify>
  <done>Domain types defined, validation logic working, all tests pass. Message status enum documented with transitions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /messages handler with happy path and error responses (RED + GREEN)</name>
  <files>internal/api/handler/message.go, internal/api/handler/message_test.go</files>
  <behavior>
    - Handler accepts *echo.Context pointer (Echo v5 signature)
    - Valid request returns 202 with CreateMessageResponse JSON body
    - Response includes X-Trace-Id header matching request context trace_id
    - Invalid JSON body returns 400 with ErrorResponse code "invalid_payload"
    - Validation failure returns 400 with ErrorResponse code "invalid_payload" and field-level details
    - Missing Authorization header returns 401 (handled by existing auth middleware, not the handler)
    - Publisher interface decouples handler from JetStream implementation
  </behavior>
  <action>
Create `internal/api/handler/message.go` per D-01, API-01, API-02, API-04:

1. Define Publisher interface for dependency injection (Phase 2 will implement with JetStream):
   - Publish(ctx context.Context, subject string, data []byte, opts ...interface{}) error
   - Keep the interface minimal — only what the handler needs

2. MessageHandler struct with fields: Publisher (for queue publish — nil in this plan, real in Plan 2). No rate limiter or queue depth tracker yet (Plan 3).

3. Create method (POST /messages handler):
   - Extract trace_id from context via middleware.TraceIDFrom (established in Phase 1)
   - Extract workspace_id from context via tenant.WorkspaceIDFrom (established in Phase 1)
   - Bind JSON body to domain.CreateMessageRequest via c.Bind()
   - On bind error: return c.JSON(400, domain.ErrorResponse{Code: "invalid_payload", Message: "request body validation failed", Details: []domain.FieldError{{Field: "body", Message: "invalid JSON or missing required fields"}}})
   - Call domain.ValidateMessage(&req)
   - If validation fails: return c.JSON(400, *validationErr)
   - Generate message_id via uuid.New()
   - Marshal request to JSON for queue payload (publisher will be nil in tests — skip publish if publisher is nil for now, wire in Plan 2)
   - Set X-Trace-Id response header
   - Return c.JSON(202, domain.CreateMessageResponse{MessageID: msgID, Status: domain.StatusQueued, QueuedAt: time.Now().UTC()})
   - Log the ingestion event via slog with trace_id and workspace_id context

4. RegisterRoutes method: e.POST("/api/v1/messages", h.Create)

Create `internal/api/handler/message_test.go`:

Use Echo v5's echotest or create a minimal echo.Echo instance for testing. Tests do NOT need a real JetStream — the handler skips publish when Publisher is nil (stub behavior for this plan).

Table-driven tests:
- TestCreateMessageValid: POST valid JSON with Authorization header, expect 202 with message_id, status "queued", queued_at, and X-Trace-Id header
- TestCreateMessageInvalidJSON: POST malformed JSON, expect 400 with code "invalid_payload"
- TestCreateMessageMissingTo: POST JSON missing "to" field, expect 400 with FieldError for "to"
- TestCreateMessageInvalidChannel: POST with channel="sms", expect 400 with FieldError for "channel"
- TestCreateMessageZeroTTL: POST with ttl_seconds=0, expect 400 with FieldError for "ttl_seconds"
- TestCreateMessageMissingAuth: POST without Authorization header, expect 401 (auth middleware rejects)
- TestCreateMessageTraceHeader: Verify X-Trace-Id in response matches trace_id in context

For tests that need auth context: inject workspace_id and trace_id into the request context manually (bypassing auth middleware for unit tests). For TestCreateMessageMissingAuth: use a handler without auth middleware to verify 401 behavior.

Run tests to verify all pass.
  </action>
  <verify>
    <automated>go test ./internal/api/handler/... -v -count=1</automated>
  </verify>
  <done>POST /messages accepts valid payloads returning 202 with trace correlation, rejects invalid payloads with structured 400 errors. All handler tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP Client → POST /messages | Untrusted input crosses from external caller into validated domain model |
| Auth Middleware → Handler | workspace_id injected from trusted auth context (not from payload) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | Message payload | high | mitigate | Input validation via ValidateMessage; channel whitelist enforced; payload bound via Echo v5 c.Bind() |
| T-03-02 | Elevation of Privilege | workspace_id spoofing | high | mitigate | workspace_id extracted from auth middleware context (not from request body); per D-01 established pattern |
| T-03-03 | Denial of Service | Queue flooding | medium | accept | Backpressure and rate limiting added in Plan 3; this plan has no queue limits yet (worker stub only) |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed — all dependencies already in go.mod |
</threat_model>

<verification>
- All domain tests pass: `go test ./internal/domain/... -v -count=1`
- All handler tests pass: `go test ./internal/api/handler/... -v -count=1`
- Full test suite compiles: `go build ./...`
- Vet passes: `go vet ./...`
</verification>

<success_criteria>
- POST /messages with valid payload returns 202 + CreateMessageResponse JSON
- POST /messages with invalid payload returns 400 + ErrorResponse with field-level details
- X-Trace-Id header present in 202 response
- Message status enum defined with all five values
- TTL validation rejects zero/negative values
- All tests green
</success_criteria>

<output>
Create `.planning/phases/03-ingest-api-queue/03-01-SUMMARY.md` when done
</output>
