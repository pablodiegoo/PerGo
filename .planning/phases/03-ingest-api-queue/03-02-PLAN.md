---
phase: 03-ingest-api-queue
plan: 02
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - internal/platform/queue/jetstream.go
  - internal/platform/queue/jetstream_test.go
  - internal/platform/queue/worker.go
  - cmd/pergo/main.go
autonomous: true
requirements: [QUEUE-01, QUEUE-03, API-01]

must_haves:
  truths:
    - "Messages published to JetStream survive server restart (durable queue)"
    - "JetStream stream uses WorkQueuePolicy for single-consumer semantics"
    - "Nats-Msg-Id header set to trace_id for publish-side idempotency"
    - "Worker stub reads messages from queue and logs them"
    - "Handler publishes to JetStream after accepting the request"
    - "Shutdown drains the JetStream consumer before closing NATS"
  artifacts:
    - path: "internal/platform/queue/jetstream.go"
      provides: "JetStream stream setup, Publisher implementation"
      exports: ["EnsureStream", "JetStreamPublisher"]
    - path: "internal/platform/queue/worker.go"
      provides: "Worker stub that reads and logs dispatched messages"
      exports: ["Worker", "NewWorker"]
    - path: "internal/platform/queue/jetstream_test.go"
      provides: "Integration tests for stream creation and publish"
    - path: "cmd/pergo/main.go"
      provides: "Composition root wiring JetStream + worker lifecycle"
  key_links:
    - from: "internal/api/handler/message.go"
      to: "internal/platform/queue/jetstream.go"
      via: "Handler calls Publisher.Publish() to enqueue message"
      pattern: "Publisher\\.Publish"
    - from: "internal/platform/queue/worker.go"
      to: "internal/platform/queue/jetstream.go"
      via: "Worker reads from JetStream consumer"
      pattern: "consumer\\.Messages"
    - from: "cmd/pergo/main.go"
      to: "internal/platform/queue/jetstream.go"
      via: "Composition root creates stream and wires publisher"
      pattern: "queue\\.EnsureStream|queue\\.JetStreamPublisher"
---

<objective>
JetStream durability layer: WorkQueue stream, publisher with dedup, worker stub, and composition root wiring.

Purpose: Messages now persist in JetStream and survive server restarts. The worker stub validates the consumer pipeline end-to-end. After this plan, POST /messages durably enqueues a message and a worker goroutine reads and logs it — the first half of the send path.

Output: Queue package (jetstream.go, worker.go), updated main.go with lifecycle wiring.
</objective>

<execution_context>
@/home/pablo/Coding/PerGo/.opencode/gsd-core/workflows/execute-plan.md
@/home/pablo/Coding/PerGo/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ingest-api-queue/03-CONTEXT.md
@.planning/phases/03-ingest-api-queue/03-RESEARCH.md
@.planning/phases/03-ingest-api-queue/03-01-SUMMARY.md
@.planning/phases/01-Foundation/01-02-SUMMARY.md
@internal/api/handler/message.go
@internal/domain/message.go
@internal/api/middleware/trace.go
@internal/platform/postgres/tenant/tenant.go
@cmd/pergo/main.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: JetStream stream setup, publisher, and worker stub (RED + GREEN)</name>
  <files>internal/platform/queue/jetstream.go, internal/platform/queue/jetstream_test.go, internal/platform/queue/worker.go</files>
  <behavior>
    - EnsureStream creates a WorkQueuePolicy stream named "MESSAGES" with subjects ["messages.>"], MaxMsgs=1000, DiscardNew, FileStorage
    - EnsureStream is idempotent — calling twice does not error (CreateOrUpdateStream)
    - JetStreamPublisher.Publish sends message with Nats-Msg-Id header set to trace_id
    - JetStreamPublisher.Publish returns error when stream is full (DiscardNew)
    - Worker reads messages from JetStream consumer and logs them via slog
    - Worker stops gracefully when context is cancelled
  </behavior>
  <action>
Create `internal/platform/queue/jetstream.go` per QUEUE-01, QUEUE-03:

1. Constants: StreamName = "MESSAGES", StreamSubject = "messages.>", MaxQueueDepth = 1000.

2. EnsureStream(ctx context.Context, nc *nats.Conn) (jetstream.Stream, error):
   - Call jetstream.New(nc) to get JetStream instance
   - Call js.CreateOrUpdateStream(ctx, jetstream.StreamConfig{...}) with:
     - Name: StreamName
     - Subjects: []string{StreamSubject}
     - Retention: jetstream.WorkQueuePolicy
     - MaxMsgs: MaxQueueDepth
     - Discard: jetstream.DiscardNew
     - Storage: jetstream.FileStorage
     - MaxAge: 24 * time.Hour (messages older than 24h are deleted)
   - Return stream and error

3. JetStreamPublisher struct: holds jetstream.JetStream instance

4. NewJetStreamPublisher(js jetstream.JetStream) *JetStreamPublisher

5. Publisher interface (in handler package or here — define where it's consumed):
   - Publish(ctx context.Context, subject string, data []byte, opts ...PublishOption) error
   - PublishOption type for extensibility (trace_id option)

6. Publish method: publishes to "messages.outbound" subject with jetstream.WithMsgID(traceID) for dedup per QUEUE-03. Extract traceID from ctx via middleware.TraceIDFrom. Return error (caller checks for ErrMaxMsgsExceeded for backpressure — Plan 3).

Create `internal/platform/queue/jetstream_test.go`:
- TestEnsureStream: connects to real NATS (skip if unavailable), calls EnsureStream, verifies stream config (WorkQueuePolicy, MaxMsgs=1000)
- TestEnsureStreamIdempotent: calls EnsureStream twice, no error second time
- TestPublishAndConsume: publishes a message, creates a consumer, reads it back, verifies payload matches
- TestPublishDedup: publishes same trace_id twice with WithMsgID, verifies only one message in stream

These are integration tests — skip with t.Skip() when NATS is unavailable.

Create `internal/platform/queue/worker.go` per D-09:

1. Worker struct: holds jetstream.Consumer, context.CancelFunc, done channel

2. NewWorker(ctx context.Context, consumer jetstream.Consumer) *Worker:
   - Starts a goroutine that reads from consumer.Messages()
   - Logs each message via slog with trace_id from NATS message headers
   - Acknowledges each message after logging
   - Stops when context is cancelled

3. Worker.Stop(): cancels context, waits for goroutine to finish

4. Message processing stub: just logs "message dispatched" with trace_id and payload preview. Phase 4 replaces with real Dispatcher interface.

The worker uses jetstream.ConsumeMessages (push-based) or manual pull — choose whichever is simpler for the stub. Push-based (Messages() channel) is simpler for a stub.

Run tests to verify integration tests pass with NATS available, unit behavior correct.
  </action>
  <verify>
    <automated>go test ./internal/platform/queue/... -v -count=1</automated>
  </verify>
  <done>JetStream stream created with WorkQueuePolicy, publisher sends with dedup header, worker reads and logs messages. Integration tests pass with NATS.</done>
</task>

<task type="auto">
  <name>Task 2: Wire publisher into handler and worker lifecycle into composition root</name>
  <files>cmd/pergo/main.go</files>
  <action>
Update `cmd/pergo/main.go` per D-01, QUEUE-01 to wire JetStream and worker lifecycle:

1. After NATS connection (line ~67), initialize JetStream:
   - js, err := jetstream.New(nc)
   - stream, err := queue.EnsureStream(ctx, js)
   - publisher := queue.NewJetStreamPublisher(js)

2. Create MessageHandler with publisher injected:
   - messageHandler := &handler.MessageHandler{Publisher: publisher}
   - messageHandler.RegisterRoutes(e)

3. Create JetStream consumer for worker:
   - consumer, err := stream.Consumer(ctx, "worker-1")
   - worker := queue.NewWorker(ctx, consumer)

4. Register worker shutdown in orchestrator (before NATS close):
   - orch.Register(func() error { worker.Stop(); return nil })
   - This runs BEFORE the NATS close step, ensuring the consumer drains

5. Register stream cleanup (optional — stream persists, but consumer stops):
   - The consumer is push-based and stops when context is cancelled
   - No explicit delete needed

6. Add JetStream-related log lines for observability:
   - slog.Info("JetStream stream ready", "stream", queue.StreamName)
   - slog.Info("message worker started", "consumer", "worker-1")

7. Update the test route /api/v1/me to remain as-is (no changes needed there).

Do NOT add rate limiting or backpressure yet — that is Plan 3.

The handler's Publisher field will be nil-safe: if Publisher is nil, skip publish (defensive for tests). This is already handled in Plan 1's handler design.

Run `go build ./...` and `go vet ./...` to verify compilation.
  </action>
  <verify>
    <automated>go build ./... && go vet ./...</automated>
  </verify>
  <done>main.go wires JetStream stream, publisher into handler, worker goroutine, and graceful shutdown. Server compiles and starts with JetStream.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP Handler → JetStream | Message payload crosses from HTTP layer into message queue |
| JetStream → Worker | Queued message crosses from durable queue into processing goroutine |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | Message publish | medium | mitigate | Nats-Msg-Id = trace_id prevents duplicate publishes; workspace_id from auth context scoped to message |
| T-03-05 | Denial of Service | Stream overflow | medium | mitigate | MaxMsgs=1000 + DiscardNew rejects publishes when full; caller translates to HTTP 429 |
| T-03-06 | Information Disclosure | Worker logs | low | accept | Worker logs message preview at INFO level; sensitive content not logged (only trace_id and first 100 bytes) |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages — nats.go already in go.mod |
</threat_model>

<verification>
- Queue integration tests pass: `go test ./internal/platform/queue/... -v -count=1`
- Full build passes: `go build ./...`
- Vet passes: `go vet ./...`
- Server starts with JetStream (manual check via docker-compose up)
</verification>

<success_criteria>
- JetStream stream "MESSAGES" created with WorkQueuePolicy and MaxMsgs=1000
- Publisher sends messages with Nats-Msg-Id = trace_id
- Worker reads and logs dispatched messages
- main.go wires publisher, worker, and shutdown lifecycle
- All tests green
</success_criteria>

<output>
Create `.planning/phases/03-ingest-api-queue/03-02-SUMMARY.md` when done
</output>
