---
phase: 06-webhook-delivery-dlq
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/platform/postgres/migrations/008_create_webhooks_and_dlq.sql
  - internal/repository/webhook_dlq.go
  - internal/repository/webhook_dlq_test.go
  - internal/platform/queue/webhook_worker.go
  - internal/platform/queue/webhook_worker_test.go
  - internal/api/handler/admin/webhook_dlq.go
  - cmd/pergo/admin_webhook_dlq_test.go
  - templates/pages/webhooks.templ
  - cmd/pergo/main.go
  - internal/api/middleware/auth.go
autonomous: true
requirements: [WHOOK-01, WHOOK-02, WHOOK-03, WHOOK-04, WHOOK-05]
must_haves:
  truths:
    - "Schema 008_create_webhooks_and_dlq.sql creates webhook_configs and webhook_dlqs tables"
    - "WebhookConfigRepository provides Save, Get, and Delete operations for webhook URLs and encrypted secrets"
    - "WebhookDLQRepository provides persistence for failed webhook payloads, log retries, and delete actions"
    - "Webhook delivery worker consumes events from a dedicated NATS JetStream pull consumer and POSTs with HMAC-SHA256 headers"
    - "Failed deliveries are retried via NATS NakWithDelay with dynamic exponential backoff, up to 10 attempts"
    - "Exhausted or terminal status failures (400, 401, 403, 404) are stored in the webhook_dlqs table"
    - "Admin console displays dead-letter logs, badge count, and allows re-enqueue / deletion"
  artifacts:
    - path: "internal/platform/postgres/migrations/008_create_webhooks_and_dlq.sql"
      provides: "Goose migrations for webhooks configuration and DLQ tables"
    - path: "internal/repository/webhook_dlq.go"
      provides: "Repository methods for webhook configs and DLQ storage"
      exports: ["WebhookConfig", "WebhookDLQ", "WebhookDLQRepository", "NewWebhookDLQRepository"]
    - path: "internal/repository/webhook_dlq_test.go"
      provides: "Integration tests for DLQ table writes and status queries"
    - path: "internal/platform/queue/webhook_worker.go"
      provides: "Outbound webhook pull consumer, HMAC signature signer, HTTP POST client with 10s timeout, error classifier, and JetStream NakWithDelay retry mechanism"
      exports: ["WebhookWorker", "NewWebhookWorker", "WebhookEvent", "SignPayload"]
    - path: "internal/platform/queue/webhook_worker_test.go"
      provides: "Tests for webhook client, HMAC signature logic, transient retry, and DLQ escalation"
    - path: "internal/api/handler/admin/webhook_dlq.go"
      provides: "REST endpoints for managing webhook configs, listing DLQ items, manual retries, and deletions"
      exports: ["WebhookDLQHandler", "NewWebhookDLQHandler"]
    - path: "cmd/pergo/admin_webhook_dlq_test.go"
      provides: "Integration tests for the Echo DLQ endpoints, ensuring workspace isolation"
    - path: "templates/pages/webhooks.templ"
      provides: "Templ page design for configuring webhooks, listing DLQ logs, and triggering retries/deletes"
---

<objective>
Implement outbound status webhook delivery with HMAC-SHA256 signatures, replay prevention timestamps, and exponential NATS JetStream retries. Persist permanently failed webhooks to a dedicated DLQ PostgreSQL table, and expose config and DLQ management via Templ page and Echo handlers.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-webhook-delivery-dlq/06-CONTEXT.md
@.planning/phases/06-webhook-delivery-dlq/06-RESEARCH.md
@.planning/phases/06-webhook-delivery-dlq/06-VALIDATION.md
@internal/platform/queue/jetstream.go
@internal/platform/queue/worker.go
@internal/repository/dispatch.go
@cmd/pergo/main.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration and Repository for webhook configs and DLQ logs</name>
  <read_first>
    - internal/platform/postgres/migrations/007_create_message_dispatches.sql
    - internal/repository/dispatch.go
    - internal/platform/crypto/encrypt.go
  </read_first>
  <acceptance_criteria>
    - Migration file `008_create_webhooks_and_dlq.sql` is successfully run by goose.
    - Tables `webhook_configs` and `webhook_dlqs` are created with workspace foreign keys.
    - `WebhookDLQRepository` provides SaveConfig, GetConfig, and DeleteConfig scoped by workspace ID. Config secret bytes are encrypted/decrypted via `crypto.Encryptor`.
    - `WebhookDLQRepository` provides methods to InsertDLQ, ListDLQ, DeleteDLQ, and GetDLQByID.
    - Integration tests in `internal/repository/webhook_dlq_test.go` verify all CRUD operations under workspace multi-tenant isolation.
  </acceptance_criteria>
  <action>
    - Create `internal/platform/postgres/migrations/008_create_webhooks_and_dlq.sql`.
    - Create `internal/repository/webhook_dlq.go`.
    - Create `internal/repository/webhook_dlq_test.go`.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ingestion and Message worker webhook event publishing</name>
  <read_first>
    - internal/platform/queue/jetstream.go
    - internal/platform/queue/worker.go
    - internal/repository/dispatch.go
  </read_first>
  <acceptance_criteria>
    - `EnsureStream` in `internal/platform/queue/jetstream.go` creates or updates a stream named `WEBHOOKS` with subject `webhooks.>` under LimitsPolicy.
    - A dedicated helper/method publishes status events (`queued`, `sending`, `sent`, `delivered`, `read`, `failed`) to `webhooks.events` on status updates.
    - Unit tests in `internal/platform/queue/webhook_worker_test.go` assert event serialization and NATS publishing on dispatch status updates.
  </acceptance_criteria>
  <action>
    - Modify `internal/platform/queue/jetstream.go` to support creating the `WEBHOOKS` stream.
    - Modify `internal/platform/queue/worker.go` to publish events to NATS on status transitions.
    - Create `internal/platform/queue/webhook_worker_test.go` containing event publish tests.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Webhook consumer worker with HMAC-SHA256 signatures and NakWithDelay retry</name>
  <read_first>
    - internal/platform/queue/worker.go
    - .planning/phases/06-webhook-delivery-dlq/06-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `WebhookWorker` is a durable pull consumer worker reading from the `WEBHOOKS` stream.
    - Payload is signed with HMAC-SHA256 using key `X-PerGo-Signature` format: `t=timestamp,v1=signature`.
    - Enforces a 10s request timeout.
    - Distinguishes terminal status errors (400, 401, 403, 404) and writes them straight to database DLQ.
    - Retries transient errors (429, 5xx, timeouts) using `NakWithDelay` with exponential backoff up to 10 attempts, then writes to DLQ.
    - Tests verify webhook worker signature generation, transient retry increment, and terminal failure database DLQ inserts.
  </acceptance_criteria>
  <action>
    - Create `internal/platform/queue/webhook_worker.go` with pull consumer loop and dispatch logic.
    - Implement `SignPayload` helper.
    - Add tests to `internal/platform/queue/webhook_worker_test.go` for the worker, retry loop, and DLQ dispatch.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Admin UI pages, API endpoints, and main wiring</name>
  <read_first>
    - templates/pages/devices.templ
    - internal/api/handler/admin/devices.go
    - cmd/pergo/main.go
  </read_first>
  <acceptance_criteria>
    - Templ page `templates/pages/webhooks.templ` displays webhook URL / secret inputs, DLQ table, detail modals, delete/retry buttons, and handles empty states.
    - Echo handlers in `internal/api/handler/admin/webhook_dlq.go` serve webhook config update, DLQ list, deletion, and manual retry (which publishes a fresh event to NATS).
    - Sidebar layout includes badge count for unresolved DLQ items.
    - Auth middleware allows access to webhooks endpoints.
    - `cmd/pergo/main.go` wires `WebhookDLQRepository`, instantiates and starts `WebhookWorker`, and registers admin endpoints.
    - `go test ./...` passes without errors.
  </acceptance_criteria>
  <action>
    - Create `templates/pages/webhooks.templ`.
    - Create `internal/api/handler/admin/webhook_dlq.go`.
    - Create `cmd/pergo/admin_webhook_dlq_test.go` to test endpoints.
    - Modify `templates/layout/sidebar.templ` to add navigation item and badge count.
    - Modify `internal/api/middleware/auth.go` to authorize webhook routes.
    - Modify `cmd/pergo/main.go` to initialize and boot the webhook stack.
  </action>
</task>

</tasks>
