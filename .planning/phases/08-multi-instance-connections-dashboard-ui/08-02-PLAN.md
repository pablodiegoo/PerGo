---
phase: 08-multi-instance-connections-dashboard-ui
plan: "08-02"
type: tdd
wave: 1
depends_on: ["08-01"]
files_modified:
  - internal/domain/message.go
  - internal/domain/message_test.go
  - internal/channel/dispatcher.go
  - internal/platform/queue/worker.go
  - internal/platform/queue/worker_test.go
  - internal/api/handler/message.go
  - internal/api/handler/message_test.go
  - internal/channel/whatsapp/client.go
  - internal/channel/whatsapp/proxy_test.go
  - internal/channel/whatsapp/waba.go
  - internal/channel/telegram/telegram.go
  - internal/session/manager.go
  - internal/session/qr.go
  - internal/api/handler/admin/device.go
  - cmd/pergo/main.go
autonomous: true
requirements: [D-02, D-03]
must_haves:
  truths:
    - "CreateMessageRequest includes 'from' field representing phone number or bot username"
    - "MessageHandler.Create resolves ConnectionID and SenderIdentity using ConnectionRepository"
    - "QueueMessage and MessagePayload carry ConnectionID and SenderIdentity through the NATS pipeline"
    - "Stateless channel dispatchers (Telegram, WABA) load credentials dynamically by ConnectionID on Dispatch"
    - "Stateful WhatsApp Web channel adapter looks up active whatsmeow client using SenderIdentity JID"
    - "whatsmeow client connection supports SOCKS5 and HTTP proxy configurations via ConfigureProxy"
    - "Active whatsmeow connection count per workspace is capped at PERGO_MAX_WHATSAPP_CONNECTIONS (default 5)"
    - "All compilation and test checks pass successfully"
  prohibitions:
    - "Do not dynamically instantiate adapters on dispatch; registry must remain static"
    - "No pairing allowed if the current active whatsmeow connection limit is exceeded (returns 422)"
---

## Artifacts this phase produces

| File Path | Symbol/Artifact | Type | Description |
|-----------|-----------------|------|-------------|
| [message.go](file:///home/pablo/Coding/PerGo/internal/domain/message.go) | `domain.CreateMessageRequest.From` | Struct Field | Field to specify the sender identity on API request |
| [message.go](file:///home/pablo/Coding/PerGo/internal/domain/message.go) | `domain.QueueMessage.ConnectionID` | Struct Field | Field carrying resolved ConnectionID to queue |
| [message.go](file:///home/pablo/Coding/PerGo/internal/domain/message.go) | `domain.QueueMessage.SenderIdentity` | Struct Field | Field carrying resolved SenderIdentity to queue |
| [dispatcher.go](file:///home/pablo/Coding/PerGo/internal/channel/dispatcher.go) | `channel.MessagePayload.ConnectionID` | Struct Field | Payload field for dispatcher instance lookup |
| [dispatcher.go](file:///home/pablo/Coding/PerGo/internal/channel/dispatcher.go) | `channel.MessagePayload.SenderIdentity` | Struct Field | Payload field for whatsmeow session JID mapping |
| [client.go](file:///home/pablo/Coding/PerGo/internal/channel/whatsapp/client.go) | `whatsapp.ConfigureProxy` | Function | Sets up SOCKS5/HTTP proxy for whatsmeow client connection |
| [proxy_test.go](file:///home/pablo/Coding/PerGo/internal/channel/whatsapp/proxy_test.go) | `TestConfigureProxy` | Test function | Validates proxy routing parser logic |

<objective>
Phase 8 Wave 1 dynamic routing and concurrency slice implements dynamic route resolution on ingest, NATS pipeline propagation of connections, dynamic credential/session retrieval in dispatchers, proxy configuration support, and active WhatsApp connection limit boundaries.
</objective>

<execution_context>
@/home/pablo/Coding/PerGo/.opencode/gsd-core/workflows/execute-plan.md
@/home/pablo/Coding/PerGo/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-multi-instance-connections-dashboard-ui/08-CONTEXT.md
@.planning/phases/08-multi-instance-connections-dashboard-ui/08-RESEARCH.md
@internal/domain/message.go
@internal/channel/dispatcher.go
@internal/platform/queue/worker.go
@internal/api/handler/message.go
@internal/channel/whatsapp/client.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add whatsmeow proxy configuration test stub (Wave 0)</name>
  <files>internal/channel/whatsapp/proxy_test.go</files>
  <requirements>[D-02]</requirements>
  <read_first>
Files:
- internal/channel/whatsapp/proxy_test.go (new file)
Sources of truth:
- .planning/phases/08-multi-instance-connections-dashboard-ui/08-VALIDATION.md (Wave 0 requirements)
  </read_first>
  <acceptance_criteria>
- File `internal/channel/whatsapp/proxy_test.go` exists.
- Contains `TestConfigureProxy` checking proxy parsing logic for socks5:// and http:// schemes.
- Test compiles successfully.
  </acceptance_criteria>
  <action>
Create `internal/channel/whatsapp/proxy_test.go` inside the `whatsapp` package. Define a test `TestConfigureProxy(t *testing.T)` verifying that proxy strings socks5://user:pass@host:port and http://host:port are parsed correctly, and unsupported schemas yield a predictable error.
  </action>
  <verify>
    <automated>go test -c ./internal/channel/whatsapp</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update message models, queue payload, and API handler for dynamic route resolution</name>
  <files>internal/domain/message.go, internal/domain/message_test.go, internal/channel/dispatcher.go, internal/api/handler/message.go, internal/api/handler/message_test.go</files>
  <requirements>[D-02]</requirements>
  <read_first>
Files:
- internal/domain/message.go (file being modified)
- internal/channel/dispatcher.go (file being modified)
- internal/api/handler/message.go (file being modified)
Sources of truth:
- .planning/phases/08-multi-instance-connections-dashboard-ui/08-RESEARCH.md (Route Resolution logic and schemas)
  </read_first>
  <acceptance_criteria>
- `CreateMessageRequest` struct contains `From` string field mapped to JSON key `from`.
- `QueueMessage` and `MessagePayload` contain `ConnectionID uuid.UUID` and `SenderIdentity string`.
- `MessageHandler.Create` performs route resolution: calls a function that queries `connections` table by `From` sender identity, or queries default connection if `From` is empty.
- If no route resolves, the API returns HTTP 400 validation error (or 422) with appropriate error code.
- Outbound messages queued to NATS include the resolved ConnectionID and SenderIdentity.
  </acceptance_criteria>
  <action>
Modify `CreateMessageRequest` in `internal/domain/message.go` to add `From string json:"from,omitempty"`. Modify `QueueMessage` and `MessagePayload` to include `ConnectionID uuid.UUID` and `SenderIdentity string`. Implement `ResolveRoute` helper or method in the database/repository context querying the `connections` table. Update `MessageHandler.Create` in `internal/api/handler/message.go` to query the connection using this resolver before publishing to NATS. Update the JSON serialization and test suites to assert routing resolutions.
  </action>
  <verify>
    <automated>go test -v -run TestMessageHandler ./internal/api/handler</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor queue worker, dispatchers, and whatsmeow client for dynamic connection routing</name>
  <files>internal/platform/queue/worker.go, internal/channel/whatsapp/client.go, internal/channel/whatsapp/proxy_test.go, internal/channel/whatsapp/waba.go, internal/channel/telegram/telegram.go</files>
  <requirements>[D-02]</requirements>
  <read_first>
Files:
- internal/platform/queue/worker.go (file being modified)
- internal/channel/whatsapp/client.go (file being modified)
- internal/channel/whatsapp/waba.go (file being modified)
- internal/channel/telegram/telegram.go (file being modified)
Sources of truth:
- .planning/phases/08-multi-instance-connections-dashboard-ui/08-RESEARCH.md (Static Adapters and Concurrency Model)
  </read_first>
  <acceptance_criteria>
- NATS Queue Worker extracts ConnectionID and SenderIdentity from `QueueMessage` and forwards them to `MessagePayload`.
- `TelegramAdapter` and `WABAAdapter` load credentials dynamically using ConnectionID on `Dispatch`.
- `WhatsAppAdapter` retrieves active whatsmeow client sessions from `ActiveSession` registry mapping `SenderIdentity` (JID).
- whatsmeow client proxy configuration uses native `SetProxyAddress` method from `go.mau.fi/whatsmeow`.
- `whatsmeow` client initialization in `whatsapp.NewWhatsAppClient` uses `container.GetDevice(jid)` (if JID is provided) instead of `container.GetFirstDevice()`.
  </acceptance_criteria>
  <action>
In `internal/platform/queue/worker.go`, assign ConnectionID and SenderIdentity to the outgoing `MessagePayload`. Update `TelegramAdapter.Dispatch` and `WABAAdapter.Dispatch` to call `connectionsRepo.GetCredentialsByID` to load decrypted credentials on the fly. Update `WhatsAppAdapter` to select client from the session pool using `m.SenderIdentity` as the key. Refactor `NewWhatsAppClient` in `internal/channel/whatsapp/client.go` to call `client.SetProxyAddress` when a connection proxy URL is set, and to fetch specific device keys using `container.GetDevice(jid)`.
  </action>
  <verify>
    <automated>go test -v ./internal/channel/... ./internal/platform/queue/...</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement whatsmeow active connection limit check per workspace</name>
  <files>internal/session/manager.go, internal/session/qr.go, internal/api/handler/admin/device.go</files>
  <requirements>[D-03]</requirements>
  <read_first>
Files:
- internal/session/qr.go (file being modified)
- internal/api/handler/admin/device.go (file being modified)
Sources of truth:
- .planning/phases/08-multi-instance-connections-dashboard-ui/08-RESEARCH.md (Workspace Connection Limits)
  </read_first>
  <acceptance_criteria>
- Environment variable `PERGO_MAX_WHATSAPP_CONNECTIONS` read (defaulting to 5).
- Before starting a pairing flow in `session.Manager.StartPairing`, check count of active whatsmeow WebSocket connections in the workspace.
- If limit is exceeded, `StartPairing` returns an error, and the admin endpoint returns HTTP 422 with appropriate message.
  </acceptance_criteria>
  <action>
In `internal/session/qr.go` / `StartPairing`, count currently active WhatsApp connections for the workspace. Compare this count against `PERGO_MAX_WHATSAPP_CONNECTIONS` environment variable value. If exceeded, return a custom error. In `internal/api/handler/admin/device.go`, handle this error, returning HTTP 422.
  </action>
  <verify>
    <automated>go test -v -run TestStartPairing ./internal/session/...</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Implement Re-pairing capability in session.Manager pairing flow</name>
  <files>internal/session/manager.go, internal/session/qr.go, internal/api/handler/admin/device.go</files>
  <requirements>[D-02]</requirements>
  <read_first>
Files:
- internal/session/manager.go (file being modified)
- internal/session/qr.go (file being modified)
- internal/api/handler/admin/device.go (file being modified)
Sources of truth:
- .planning/phases/08-multi-instance-connections-dashboard-ui/08-CONTEXT.md (D-02 Re-pairing Flow)
  </read_first>
  <acceptance_criteria>
- `session.Manager.StartPairing` accepts an optional `ConnectionID *uuid.UUID` parameter.
- `onPairingSuccess` receives this ConnectionID and, if it is not nil, updates the existing connection row in the database with the JID/phone/status instead of creating a new row with `uuid.New()`.
- DeviceHandler endpoint `/admin/devices/pair` parses an optional `connection_id` query parameter and forwards it to `StartPairing`.
  </acceptance_criteria>
  <action>
Modify the signature and implementation of `StartPairing` and `onPairingSuccess` in `internal/session/qr.go`. If an existing connection ID is supplied, bypass `repo.Create` and update the connection row using a repository update call (e.g. `repo.UpdateSessionDetails` or updating specific JID/Phone fields). Update `device.go` endpoint handler to extract `connection_id` from query/form parameters and delegate.
  </action>
  <verify>
    <automated>go test -v -run TestStartPairingRePair ./internal/session/...</automated>
  </verify>
</task>

</tasks>
