---
phase: 08-multi-instance-connections-dashboard-ui
plan: "08-01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/platform/postgres/migrations/012_consolidate_connections.sql
  - internal/repository/connection.go
  - internal/repository/connection_test.go
  - internal/repository/connection_migration_test.go
  - internal/repository/credentials.go
  - internal/session/device.go
autonomous: true
requirements: [D-01]
must_haves:
  truths:
    - "Goose migration 012_consolidate_connections.sql consolidates devices and channel_credentials into connections table"
    - "Existing whatsmeow devices and WABA/Telegram credentials migrate correctly with correct statuses, keys, and values"
    - "ConnectionRepository provides CRUD methods: Create, GetByID, GetBySenderIdentity, GetDefaultChannelConnection, ListByWorkspace, ListAll, UpdateStatus, Delete"
    - "Credentials encrypt and decrypt successfully via ConnectionRepository using crypto.Encryptor"
  prohibitions:
    - "No duplicate sender_identity is allowed in connections table (enforced via UNIQUE constraint)"
---

## Artifacts this phase produces

| File Path | Symbol/Artifact | Type | Description |
|-----------|-----------------|------|-------------|
| [012_consolidate_connections.sql](file:///home/pablo/Coding/PerGo/internal/platform/postgres/migrations/012_consolidate_connections.sql) | `012_consolidate_connections` | SQL Migration | Goose migration consolidating devices and credentials into connections |
| [connection.go](file:///home/pablo/Coding/PerGo/internal/repository/connection.go) | `repository.Connection` | Struct | Model representing a workspace connection configuration |
| [connection.go](file:///home/pablo/Coding/PerGo/internal/repository/connection.go) | `repository.ConnectionRepository` | Struct | Repository managing CRUD operations and credentials crypto for Connection |
| [connection_migration_test.go](file:///home/pablo/Coding/PerGo/internal/repository/connection_migration_test.go) | `TestConnectionMigration` | Test function | Test validating the migration from legacy tables to connections |
| [credentials.go](file:///home/pablo/Coding/PerGo/internal/repository/credentials.go) | `repository.CredentialsRepository` | Struct | Compatibility shim for old channel_credentials queries |
| [device.go](file:///home/pablo/Coding/PerGo/internal/session/device.go) | `session.DeviceRepository` | Struct | Compatibility shim for old devices queries |

<objective>
Phase 8 Wave 1 establishes the consolidated database schema for multi-instance routing, migrates all existing records safely (preserving status and encrypted payloads), and implements the unified ConnectionRepository.
</objective>

<execution_context>
@/home/pablo/Coding/PerGo/.opencode/gsd-core/workflows/execute-plan.md
@/home/pablo/Coding/PerGo/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-multi-instance-connections-dashboard-ui/08-CONTEXT.md
@.planning/phases/08-multi-instance-connections-dashboard-ui/08-RESEARCH.md
@.planning/phases/08-multi-instance-connections-dashboard-ui/08-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create connection migration validation test stub (Wave 0)</name>
  <files>internal/repository/connection_migration_test.go</files>
  <requirements>[D-01]</requirements>
  <read_first>
Files:
- internal/repository/connection_migration_test.go (new file)
Sources of truth:
- .planning/phases/08-multi-instance-connections-dashboard-ui/08-VALIDATION.md (Wave 0 requirements)
- .planning/phases/08-multi-instance-connections-dashboard-ui/08-RESEARCH.md (Migration SQL and setup)
  </read_first>
  <acceptance_criteria>
- File `internal/repository/connection_migration_test.go` exists.
- The package name is `repository_test` or `repository`.
- Contains `TestConnectionMigration` test function that sets up test containers or mock database structure to verify tables before and after running the Goose migration.
- Compilation is successful.
  </acceptance_criteria>
  <action>
Create file `internal/repository/connection_migration_test.go`. Inside it, define a test function `TestConnectionMigration(t *testing.T)` that asserts the existence of legacy tables `devices` and `channel_credentials` under test setup, executes migration Up, and verifies that the `connections` table is created containing the migrated rows with the expected column types: id, workspace_id, name, channel, sender_identity, status, is_default, credentials, key_id, key_version, jid, connected_since, proxy_url. Verify the test compiles successfully.
  </action>
  <verify>
    <automated>go test -c ./internal/repository</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write Goose migration to consolidate devices and credentials into connections</name>
  <files>internal/platform/postgres/migrations/012_consolidate_connections.sql</files>
  <requirements>[D-01]</requirements>
  <read_first>
Files:
- internal/platform/postgres/migrations/012_consolidate_connections.sql (new file)
Sources of truth:
- .planning/phases/08-multi-instance-connections-dashboard-ui/08-RESEARCH.md (Goose Migration Script)
  </read_first>
  <acceptance_criteria>
- Migration file `012_consolidate_connections.sql` exists in `internal/platform/postgres/migrations/`.
- Up block creates `connections` table, creates indexes `idx_connections_workspace_id` and `idx_connections_channel`, and migrates existing `devices` and `channel_credentials`.
- Up block drops tables `devices` and `channel_credentials`.
- Down block recreates legacy tables `devices` and `channel_credentials`, populates them from `connections`, and drops `connections`.
- Migration validates successfully and passes `TestConnectionMigration`.
  </acceptance_criteria>
  <action>
Create the SQL migration file `internal/platform/postgres/migrations/012_consolidate_connections.sql` with goose statement tags (`-- +goose Up` and `-- +goose Down`). Define the creation of the `connections` table as specified in 08-RESEARCH.md. Insert rows from `devices` (calculating sender_identity and name from JID/phone) and `channel_credentials` (creating safe unique placeholder identities like `legacy_telegram_<id>`). Ensure `is_default` flag is resolved correctly per workspace and channel. Drop legacy tables. Re-create them and restore columns in the Down statement block.
  </action>
  <verify>
    <automated>go test -v -run TestConnectionMigration ./internal/repository</automated>
  </verify>
</task>
 
<task type="auto" tdd="true">
  <name>Task 3: Implement Connection and ConnectionRepository with CRUD and crypto support</name>
  <files>internal/repository/connection.go, internal/repository/connection_test.go</files>
  <requirements>[D-01]</requirements>
  <read_first>
Files:
- internal/repository/connection.go (new file)
- internal/repository/connection_test.go (new file)
Sources of truth:
- internal/repository/credentials.go (legacy credentials repository pattern)
- internal/session/device.go (legacy device repository pattern)
- .planning/phases/08-multi-instance-connections-dashboard-ui/08-RESEARCH.md (Unified schema parameters)
  </read_first>
  <acceptance_criteria>
- `repository.Connection` struct defined containing: ID, WorkspaceID, Name, Channel, SenderIdentity, Status, IsDefault, Credentials, KeyID, KeyVersion, JID, ConnectedSince, ProxyURL, CreatedAt, UpdatedAt.
- `repository.NewConnectionRepository` instantiates a pool and encryptor.
- CRUD operations implemented: Create, GetByID, GetBySenderIdentity, GetDefaultChannelConnection, ListByWorkspace, ListAll, UpdateStatus, Delete.
- Credentials payload encrypts on SaveCredentials/Create (if credentials bytes provided) and decrypts on GetCredentials or retrieval.
- `connection_test.go` exercises all CRUD cases and key encryption round-trips.
  </acceptance_criteria>
  <action>
Create `internal/repository/connection.go` defining the `Connection` struct and `ConnectionRepository` type. Inject `*pgxpool.Pool` and `*crypto.Encryptor`. Implement CRUD methods querying the `connections` table. For credential operations, use `r.encryptor.Encrypt` and `r.encryptor.Decrypt` to handle `credentials` storage. Create unit tests in `internal/repository/connection_test.go` with mock pgx pool or testing database running CRUD checks, ensuring decryption retrieves exact plaintext.
  </action>
  <verify>
    <automated>go test -v -run TestConnectionRepository ./internal/repository</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Refactor legacy DeviceRepository and CredentialsRepository to query connections table as shims</name>
  <files>internal/repository/credentials.go, internal/session/device.go, internal/repository/credentials_test.go, internal/session/device_repository_test.go</files>
  <requirements>[D-01]</requirements>
  <read_first>
Files:
- internal/repository/credentials.go (file being modified)
- internal/session/device.go (file being modified)
- internal/repository/credentials_test.go (file being modified)
- internal/session/device_repository_test.go (file being modified)
Sources of truth:
- internal/repository/connection.go (new connections repository)
  </read_first>
  <acceptance_criteria>
- `CredentialsRepository.Save`, `Get`, and `Delete` are rewritten to query the `connections` table (filtering/setting channel in ('telegram', 'whatsapp_cloud')).
- `DeviceRepository.Create`, `GetByID`, `GetByJID`, `ListByWorkspace`, `ListAll`, `UpdateStatus`, and `Delete` are rewritten to query the `connections` table (filtering/setting channel = 'whatsapp').
- Old tests `credentials_test.go` and `device_repository_test.go` pass without errors on the consolidated schema.
  </acceptance_criteria>
  <action>
Refactor implementations in `internal/repository/credentials.go` and `internal/session/device.go`. Instead of querying the legacy tables `channel_credentials` and `devices`, modify their internal SQL commands to query/update/insert into the `connections` table. Ensure JID-mapping and encrypted payload formatting aligns exactly with the new columns. Run the legacy test suites to verify backward compatibility.
  </action>
  <verify>
    <automated>go test -v -run "TestCredentialsRepository|TestDeviceRepository" ./internal/repository/... ./internal/session/...</automated>
  </verify>
</task>

</tasks>

