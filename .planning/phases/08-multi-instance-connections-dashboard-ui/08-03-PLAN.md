---
phase: 08-multi-instance-connections-dashboard-ui
plan: "08-03"
type: tdd
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - templates/layout/base.templ
  - templates/layout/sidebar.templ
  - static/css/admin.css
  - internal/api/handler/admin/dashboard.go
  - internal/api/handler/admin/dashboard_test.go
  - templates/pages/dashboard.templ
  - internal/session/qr.go
  - internal/api/handler/admin/device.go
  - cmd/pergo/main.go
autonomous: true
requirements: [D-04, D-05, D-03]
must_haves:
  truths:
    - "Tailwind CSS and daisyUI CDNs are loaded in templates/layout/base.templ"
    - "admin.css defines monochromatic colors, grayscale borders, and black as primary button color"
    - "Sidebar is collapsible, rendering workspace dropdown, navigation links, and developer docs"
    - "DashboardHandler.Index uses cookie pergo-active-workspace or falls back to first workspace / creates default workspace"
    - "Onboarding checklist is rendered if API keys count or connections count for workspace is 0"
    - "Operational developer dashboard is rendered if workspace is fully onboarded"
    - "Dashboard includes webhook simulation trigger and active connections grid"
    - "StartPairing and pairing handler reject pairing requests with 422 if WhatsApp connection limits are exceeded"
  prohibitions:
    - "Do not display empty metrics widgets or graphs on onboarding state"
---

## Artifacts this phase produces

| File Path | Symbol/Artifact | Type | Description |
|-----------|-----------------|------|-------------|
| [dashboard.go](file:///home/pablo/Coding/PerGo/internal/api/handler/admin/dashboard.go) | `admin.DashboardHandler.SimulateWebhook` | Handler Method | Endpoint to simulate inbound/status webhook events |
| [dashboard.templ](file:///home/pablo/Coding/PerGo/templates/pages/dashboard.templ) | `pages.OnboardingChecklist` | Templ component | Renders the 4-step progressive onboarding checklist |
| [dashboard.templ](file:///home/pablo/Coding/PerGo/templates/pages/dashboard.templ) | `pages.OperationalDashboard` | Templ component | Renders connection grids, metrics telemetry, and audit list table |

<objective>
Phase 8 Wave 2 UI design and onboarding slice implements the Notion-style monochromatic layout, collapsible navigation sidebar with workspace selector, dynamic onboarding checker, metrics landing page, and active connection boundary checks on pairing initiation.
</objective>

<execution_context>
@/home/pablo/Coding/PerGo/.opencode/gsd-core/workflows/execute-plan.md
@/home/pablo/Coding/PerGo/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-multi-instance-connections-dashboard-ui/08-CONTEXT.md
@.planning/phases/08-multi-instance-connections-dashboard-ui/08-RESEARCH.md
@.planning/phases/08-multi-instance-connections-dashboard-ui/08-VALIDATION.md
@templates/layout/base.templ
@templates/layout/sidebar.templ
@static/css/admin.css
@internal/api/handler/admin/dashboard.go
@templates/pages/dashboard.templ
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Tailwind CSS/daisyUI CDNs and implement monochromatic Notion-style CSS theme</name>
  <files>templates/layout/base.templ, static/css/admin.css</files>
  <requirements>[D-04]</requirements>
  <read_first>
Files:
- templates/layout/base.templ (file being modified)
- static/css/admin.css (file being modified)
Sources of truth:
- .agents/skills/sketch-findings-pergo/references/admin-dashboard-ui.md (CSS patterns and colors)
  </read_first>
  <acceptance_criteria>
- `templates/layout/base.templ` loads Tailwind CSS play CDN `<script src="https://cdn.tailwindcss.com"></script>` and daisyUI CSS CDN in the `<head>` section.
- `static/css/admin.css` overrides CSS custom properties: `--color-bg: #fcfcfc`, `--color-surface: #ffffff`, `--color-border: #e9e9eb`, `--color-text: #1e1e1e`, `--color-primary: #000000`.
- Black `#000000` is established as the primary interactive button color.
- Project compiles and templates generate without syntax errors.
  </acceptance_criteria>
  <action>
In `templates/layout/base.templ`, add the Tailwind CSS Play CDN script tag and the daisyUI link tag in `<head>`. Update `static/css/admin.css` to declare the monochromatic custom properties and clean border styles. Run `make build` to verify styling compilation and template generation.
  </action>
  <verify>
    <automated>make build</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dynamic onboarding checker and dashboard view routing</name>
  <files>internal/api/handler/admin/dashboard.go, internal/api/handler/admin/dashboard_test.go, templates/pages/dashboard.templ, templates/layout/sidebar.templ</files>
  <requirements>[D-05]</requirements>
  <read_first>
Files:
- internal/api/handler/admin/dashboard.go (file being modified)
- templates/pages/dashboard.templ (file being modified)
- templates/layout/sidebar.templ (file being modified)
Sources of truth:
- .agents/skills/sketch-findings-pergo/references/admin-dashboard-ui.md (Sidebar structure and onboarding HTML)
  </read_first>
  <acceptance_criteria>
- `DashboardHandler.Index` extracts workspace ID from cookie `pergo-active-workspace`.
- If cookie is missing or invalid, it queries workspace repository to set the active workspace, or automatically creates a default one.
- Injects workspace ID cookie in response.
- Checks counts of active connections and API keys for the selected workspace.
- Renders `pages.OnboardingChecklist` if counts of connections or API keys are zero.
- Renders `pages.OperationalDashboard` (with active connections, developer metrics, recent audit logs table, and webhook simulator button) if onboarded.
- Disconnected WhatsApp connection cards render a "Re-pair" button linking/triggering the QR scan modal with `connection_id` populated.
- Dashboard tests execute successfully verifying both states.
  </acceptance_criteria>
  <action>
Update `DashboardHandler.Index` to parse `pergo-active-workspace` cookie. Query `APIKeyRepository` and `ConnectionRepository` counts. If any count is zero, return the onboarding checklist view. Update `templates/pages/dashboard.templ` to expose `pages.OnboardingChecklist` (4 progressive checklist items) and `pages.OperationalDashboard` (including connection statuses, widgets, live log tables, and simulation button). Ensure disconnected connection cards expose a button prompting a call to `/admin/devices/pair?connection_id={id}` to initiate re-pairing. Implement webhook simulator endpoint `POST /admin/webhook/simulate` in `DashboardHandler`. Update sidebar template `sidebar.templ` to show the active workspace dropdown. Add test cases in `dashboard_test.go`.
  </action>
  <verify>
    <automated>go test -v -run TestDashboardHandler ./internal/api/handler/admin/...</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce active WhatsApp connection limit boundaries on pairing attempt</name>
  <files>internal/session/qr.go, internal/api/handler/admin/device.go</files>
  <requirements>[D-03]</requirements>
  <read_first>
Files:
- internal/session/qr.go (file being modified)
- internal/api/handler/admin/device.go (file being modified)
Sources of truth:
- .planning/phases/08-multi-instance-connections-dashboard-ui/08-RESEARCH.md (Workspace Connection Limits)
  </read_first>
  <acceptance_criteria>
- Read limit from environment variable `PERGO_MAX_WHATSAPP_CONNECTIONS` (default 5).
- `StartPairing` in `session.Manager` checks workspace's active whatsmeow WebSocket connections.
- If current active count matches or exceeds the limit, returns `ErrConnectionLimitExceeded`.
- `DeviceHandler.StartPairing` intercepts the error and returns HTTP 422 with a clear error payload.
  </acceptance_criteria>
  <action>
In `internal/session/qr.go`, query repository for active WhatsApp connections in the workspace, compare against `PERGO_MAX_WHATSAPP_CONNECTIONS` configuration limit, and block pairing if exceeded. Handle the returned error in `internal/api/handler/admin/device.go` to return HTTP 422.
  </action>
  <verify>
    <automated>go test -v -run TestStartPairing ./internal/session/...</automated>
  </verify>
</task>

</tasks>
