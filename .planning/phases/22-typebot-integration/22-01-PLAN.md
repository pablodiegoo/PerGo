---
phase: 22
plan: 01
wave: 1
depends_on: "21"
files_modified:
  - internal/platform/postgres/migrations/028_create_typebot_sessions.sql
  - internal/repository/typebot_session.go
  - internal/integration/typebot/client.go
  - internal/integration/typebot/forwarder.go
  - internal/inbound/processor.go
autonomous: false
requirements:
  - TYPE-01
  - TYPE-04
---

<objective>
Implement the database schema for Typebot sessions, the HTTP client for Typebot execution API, and the inbound processor forwarder to route incoming contact messages to the bot. Focuses on implementing decisions D-01, D-02, D-03, D-04, D-05, and D-06.
</objective>

<must_haves>
- D-01: MUST encrypt Typebot credentials in the `integrations` table `config` BYTEA column using AES-256-GCM.
- D-02: MUST store multiple bot configurations inside the encrypted JSON `config` envelope in a single `typebot` integration row.
- D-03: MUST map each Typebot bot configuration specifically to a connection channel (using connection ID) with optional trigger keywords and a fallback default bot flag.
- D-04: MUST ignore trigger keywords if a customer sends a trigger keyword while already in an active bot session and route the message to the active session.
- D-05: MUST track active sessions in a dedicated `typebot_sessions` database table using a composite primary key `(workspace_id, contact_id, connection_id)`, including `updated_at` to enforce inactivity timeouts, and delete mapping to restart sessions on remote 404/expired API responses.
- D-06: MUST use a Hybrid Flow where customer replies are enqueued synchronously when returned in the HTTP response of the Typebot execution API (`startChat`/`continueChat`).
</must_haves>

<tasks>
  <task>
    <read_first>
      - internal/platform/postgres/migrations/027_create_integrations_and_chatwoot_mappings.sql
      - internal/repository/integration.go
    </read_first>
    <action>
      Create a new migration file `internal/platform/postgres/migrations/028_create_typebot_sessions.sql` using goose format.
      Create the `typebot_sessions` table with columns: `workspace_id` (UUID, NOT NULL, REFERENCES workspaces(id) ON DELETE CASCADE), `contact_id` (UUID, NOT NULL, REFERENCES contacts(id) ON DELETE CASCADE), `connection_id` (UUID, NOT NULL, REFERENCES connections(id) ON DELETE CASCADE), `typebot_session_id` (TEXT, NOT NULL), `created_at` (TIMESTAMPTZ, NOT NULL, DEFAULT now()), `updated_at` (TIMESTAMPTZ, NOT NULL, DEFAULT now()).
      Set the primary key to `(workspace_id, contact_id, connection_id)` (D-05).
      
      Create `internal/repository/typebot_session.go` with a `TypebotSessionRepository` struct implementing `GetSession`, `UpsertSession`, and `DeleteSession`.
      Ensure Typebot credentials are encrypted (D-01) and support multiple bots in a single row configuration JSON (D-02).
    </action>
    <acceptance_criteria>
      - The `028_create_typebot_sessions.sql` file exists and has `+goose Up` and `+goose Down` sections.
      - The table `typebot_sessions` has the correct columns and primary key constraint.
      - The `TypebotSessionRepository` provides `GetSession`, `UpsertSession`, and `DeleteSession` methods that compile successfully.
    </acceptance_criteria>
  </task>

  <task>
    <read_first>
      - internal/integration/typebot/client.go (new file)
    </read_first>
    <action>
      Create `internal/integration/typebot/client.go`. Implement a `Client` struct for Typebot.
      Add a `StartChat(ctx context.Context, apiURL, botID, publicToken string, prefilledVariables map[string]string) (string, []Message, error)` method. The endpoint is `POST {apiURL}/api/v1/typebots/{botID}/startChat`. If `publicToken` is provided, set `Authorization: Bearer {publicToken}`.
      Add a `ContinueChat(ctx context.Context, apiURL, sessionID, publicToken string, messageText string) ([]Message, error)` method. The endpoint is `POST {apiURL}/api/v1/sessions/{sessionID}/continueChat`. The payload is `{"message": {"type": "text", "text": messageText}}`.
      Define necessary structs to parse the Typebot `messages` response.
    </action>
    <acceptance_criteria>
      - `client.go` compiles and exports `Client` with `StartChat` and `ContinueChat`.
      - `StartChat` builds a POST request to `/api/v1/typebots/{botID}/startChat` and returns a session string and messages array.
      - `ContinueChat` builds a POST request to `/api/v1/sessions/{sessionID}/continueChat` and handles HTTP 404 for expired sessions gracefully.
    </acceptance_criteria>
  </task>

  <task>
    <read_first>
      - internal/inbound/processor.go
      - internal/integration/typebot/forwarder.go (new file)
    </read_first>
    <action>
      Create `internal/integration/typebot/forwarder.go`. Implement a `Forwarder` struct initialized with `TypebotSessionRepository` and `IntegrationRepository` and `publisher`.
      Add `SyncInboundMessage(ctx context.Context, contact domain.Contact, event domain.InboundEvent, connection domain.Connection) error`. 
      Inside `SyncInboundMessage`, look up if the workspace has an active typebot integration in `integrations` table. Decrypt the `config` block using `IntegrationRepository`.
      Check for an active session in `typebot_sessions` (D-05). 
      If active session exists, use `Client.ContinueChat` (D-06). If not, check if `event.Body` matches any bot trigger keyword or default fallback bot (D-03). If matched, use `Client.StartChat` and save the new `typebot_session_id`.
      If a customer sends a trigger keyword while already in an active session, ignore it and route to the active session (D-04).
      If `Client.ContinueChat` returns a 404, delete the session and retry with `StartChat` (D-05).
      Publish returned messages from Typebot to `messages.outbound` NATS JetStream subject (D-06).
      
      In `internal/inbound/processor.go`, inject `TypebotForwarder` into `Processor`. Inside `Processor.Process()`, launch a goroutine with a timeout context (e.g., 10 seconds) that calls `TypebotForwarder.SyncInboundMessage`.
    </action>
    <acceptance_criteria>
      - `TypebotForwarder` retrieves integration credentials, accesses sessions, and calls Typebot API.
      - Expired sessions (HTTP 404) trigger a deletion of the old session and a new `StartChat`.
      - Output bot messages are parsed and enqueued to `messages.outbound` via `publisher.Publish()`.
      - `InboundProcessor.Process()` executes the forwarder asynchronously with a timeout context.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- `go test ./internal/integration/typebot/...` must pass and cover the HTTP client and forwarder logic.
- `go test ./internal/inbound/...` must pass and ensure `InboundProcessor` still functions correctly with the async Typebot hook.
</verification>

<artifacts>
- File: internal/platform/postgres/migrations/028_create_typebot_sessions.sql
- File: internal/repository/typebot_session.go
- Struct: TypebotSessionRepository
- File: internal/integration/typebot/client.go
- Struct: Client (Typebot)
- File: internal/integration/typebot/forwarder.go
- Struct: Forwarder (Typebot)
</artifacts>
