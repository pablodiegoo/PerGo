---
phase: 22
plan: 02
wave: 2
depends_on: "22-01"
files_modified:
  - internal/api/handler/typebot_webhook.go
  - internal/api/handler/admin/typebot.go
  - templates/pages/typebot_settings.templ
  - templates/layout/sidebar.templ
  - cmd/pergo/main.go
autonomous: false
requirements:
  - TYPE-02
  - TYPE-03
---

<objective>
Implement the Typebot webhook receiver for inbound asynchronous messages, the admin settings UI for configuring the Typebot connection, and register the necessary HTTP routes. Addresses decision D-07.
</objective>

<must_haves>
- D-07: MUST protect the `POST /api/integrations/typebot` webhook with `AuthMiddleware` requiring query-parameter token validation.
- MUST decrypt Typebot connection credentials via `IntegrationRepository` in the admin settings handler before rendering the page.
- MUST encrypt Typebot credentials before saving in the admin settings POST handler.
- MUST construct a valid `domain.QueueMessage` and publish it to NATS JetStream `messages.outbound` when the webhook receives a message.
</must_haves>

<tasks>
  <task>
    <read_first>
      - internal/api/handler/chatwoot_webhook.go
      - internal/api/handler/typebot_webhook.go (new file)
    </read_first>
    <action>
      Create `internal/api/handler/typebot_webhook.go` containing `TypebotWebhookHandler` struct.
      Implement the `Handle(c *echo.Context) error` method (D-07).
      Parse the incoming JSON payload from Typebot.
      Construct a `domain.QueueMessage` matching the recipient connection/contact.
      Use `h.publisher.Publish(ctx, "messages.outbound", payload, traceID)` to push the outbound message into NATS JetStream.
      Ensure the handler is secured using the standard API key query parameter token validation middleware.
    </action>
    <acceptance_criteria>
      - `TypebotWebhookHandler` correctly decodes a mocked Typebot webhook payload.
      - The handler publishes the message to `messages.outbound` without error and returns HTTP 200.
    </acceptance_criteria>
  </task>

  <task>
    <read_first>
      - internal/api/handler/admin/integration.go
      - internal/api/handler/admin/typebot.go (new file)
    </read_first>
    <action>
      Create `internal/api/handler/admin/typebot.go` containing `TypebotSettingsHandler`.
      Implement `GetSettings(c *echo.Context) error` and `PostSettings(c *echo.Context) error`.
      `GetSettings` should query the `IntegrationRepository` for `provider="typebot"`, decrypt the config block (if it exists), and render the `TypebotSettingsPage` templ component.
      `PostSettings` should parse the form data (API URL, Bot ID, Public Token, Active flag, Trigger keywords), marshal it into JSON, encrypt it, and save/upsert it via `IntegrationRepository`.
    </action>
    <acceptance_criteria>
      - `TypebotSettingsHandler` fetches and decrypts settings successfully.
      - `PostSettings` securely encrypts and stores the configuration in the `integrations` table.
      - Handlers return proper HTTP responses or redirect upon success.
    </acceptance_criteria>
  </task>

  <task>
    <read_first>
      - templates/pages/integrations.templ
      - templates/pages/typebot_settings.templ (new file)
      - templates/layout/sidebar.templ
    </read_first>
    <action>
      Create `templates/pages/typebot_settings.templ` defining the HTML UI for configuring Typebot bots. It should use `layout.Base()` and include a form with inputs for API URL, Bot ID, Public Token, Trigger Keywords, and an Active checkbox.
      Modify `templates/layout/sidebar.templ` to add a new navigation link to `/workspaces/{workspace_id}/integrations/typebot` under the Integrations section or main settings section.
    </action>
    <acceptance_criteria>
      - The `typebot_settings.templ` compiles with `templ generate`.
      - The Typebot page correctly renders form fields with existing configuration values.
      - The sidebar includes a visible, clickable link to the Typebot configuration page.
    </acceptance_criteria>
  </task>

  <task>
    <read_first>
      - cmd/pergo/main.go
    </read_first>
    <action>
      Update `cmd/pergo/main.go`. 
      Initialize the `TypebotWebhookHandler` and `TypebotSettingsHandler`.
      Register `POST /api/integrations/typebot` using `AuthMiddleware` for API Key validation.
      Register `GET /workspaces/:id/integrations/typebot` and `POST /workspaces/:id/integrations/typebot` in the admin UI router group.
    </action>
    <acceptance_criteria>
      - `cmd/pergo/main.go` compiles successfully.
      - HTTP GET/POST to `/workspaces/:id/integrations/typebot` route to the correct admin handler methods.
      - HTTP POST to `/api/integrations/typebot` is protected by API key middleware and routes to the webhook handler.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- `go test ./internal/api/handler/...` must pass and cover the Typebot webhook parsing and NATS publishing.
- UI forms should load and save config successfully during manual inspection.
</verification>

<artifacts>
- File: internal/api/handler/typebot_webhook.go
- Struct: TypebotWebhookHandler
- File: internal/api/handler/admin/typebot.go
- Struct: TypebotSettingsHandler
- File: templates/pages/typebot_settings.templ
- Component: TypebotSettingsPage
</artifacts>
