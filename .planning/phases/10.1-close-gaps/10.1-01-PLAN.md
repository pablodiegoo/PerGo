---
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - internal/api/handler/admin/device_test.go
  - internal/api/handler/admin/inbox_test.go
autonomous: true
requirements:
  - GAP-01
  - GAP-02
  - GAP-03
  - GAP-04
  - GAP-05
---

# Plan 10.1-01: CI/CD Pipeline & Evaluation Gaps Closure

This plan closes the testing, security, and verification gaps identified in Phase 10's Evaluation Review by implementing GitHub Actions CI, route access controls validation, and deterministic database integration testing using testcontainers.

## Tasks

<tasks>
<task id="1">
  <name>Create GitHub Actions CI workflow</name>
  <read_first>
    <file>/home/pablo/Coding/PerGo/Makefile</file>
  </read_first>
  <action>
    Create `.github/workflows/ci.yml` that:
    1. Triggers on pushes and pull requests to master/main.
    2. Runs a job with postgres:16 and nats:2.10-alpine (with JetStream enabled) service containers.
    3. Installs Go 1.26.x.
    4. Runs tests with race detector (`go test ./... -race -count=1`).
    5. Runs golangci-lint static check.
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` is present in the repository.
    - Local validation of syntax matches GitHub Actions schema.
  </acceptance_criteria>
</task>

<task id="2">
  <name>Verify decommissioned playground route returns 404</name>
  <read_first>
    <file>/home/pablo/Coding/PerGo/internal/api/handler/admin/device_test.go</file>
  </read_first>
  <action>
    Add `TestPlaygroundRouteDecommissioned` to `internal/api/handler/admin/device_test.go` or `cmd/pergo/admin_test.go` to:
    1. Fire a GET request to `/admin/playground`.
    2. Assert that the response returns HTTP status `404 Not Found`.
  </action>
  <acceptance_criteria>
    - Test executes and passes.
  </acceptance_criteria>
</task>

<task id="3">
  <name>Verify WebSocket test endpoint requires authentication</name>
  <read_first>
    <file>/home/pablo/Coding/PerGo/internal/api/handler/admin/device_test.go</file>
  </read_first>
  <action>
    Add `TestDeviceHandler_WS_RequiresAuth` to `internal/api/handler/admin/device_test.go`:
    1. Send a request to `/admin/devices/test/ws` without a valid session cookie.
    2. Assert that the request does not succeed with protocol upgrade (101) but is instead rejected with `401 Unauthorized` or redirect.
  </action>
  <acceptance_criteria>
    - Test executes and passes.
  </acceptance_criteria>
</task>

<task id="4">
  <name>Implement HTTP handler test for template sending</name>
  <read_first>
    <file>/home/pablo/Coding/PerGo/internal/api/handler/admin/inbox_test.go</file>
  </read_first>
  <action>
    Add `TestInboxHandler_NewMessageSend_HTTP` to `internal/api/handler/admin/inbox_test.go`:
    1. Use Echo's test request utility to POST to `/admin/inbox/new-message-send`.
    2. Inject a mock NATS publisher into the handler.
    3. Assert the route handles request parsing and invokes the publisher with the correctly formatted template parameters.
  </action>
  <acceptance_criteria>
    - Test executes and passes.
  </acceptance_criteria>
</task>

<task id="5">
  <name>Refactor integration tests to use testcontainers-go</name>
  <read_first>
    <file>/home/pablo/Coding/PerGo/internal/api/handler/admin/inbox_test.go</file>
    <file>/home/pablo/Coding/PerGo/internal/api/handler/admin/device_test.go</file>
  </read_first>
  <action>
    Update test setups in `internal/api/handler/admin`:
    1. Replace fallback logic (`5433` vs `5432`) with `testcontainers-go` to dynamically spin up a clean Postgres container and NATS container for tests.
    2. Configure the test suite's connection pools/NATS clients to target these containers.
  </action>
  <acceptance_criteria>
    - Database integration tests run and pass deterministically.
    - No silent skips when local Postgres is not running.
  </acceptance_criteria>
</task>
</tasks>

## Verification Criteria

### Automated Tests
- Run `go test -v ./internal/api/handler/admin` to verify that all new handler tests (playground 404, WS auth, template handler test, testcontainers database integration) pass successfully.

### Manual Verification
1. Verify the CI/CD workflow configuration exists.
2. Confirm testcontainers spins up containers correctly.

## Must Haves (Goal-Backward Verification)
- [ ] CI/CD configuration file `.github/workflows/ci.yml` is present.
- [ ] GET `/admin/playground` explicitly returns 404 in handler tests.
- [ ] GET `/admin/devices/test/ws` rejects unauthenticated connection attempts.
- [ ] `TestInboxHandler_NewMessageSend_HTTP` asserts that NATS publisher receives template payload.
- [ ] Integration tests use `testcontainers-go` for Postgres/NATS dependencies.
