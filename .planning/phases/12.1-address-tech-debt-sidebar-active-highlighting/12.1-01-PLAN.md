---
wave: 1
depends_on: []
files_modified:
  - templates/layout/sidebar.templ
  - templates/layout/sidebar_templ.go
  - templates/layout/sidebar_test.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "Workspace-scoped campaigns URLs (/admin/workspaces/:id/campaigns) correctly highlight 'Campanhas' menu item and do not expand Settings accordion"
    - "Workspace-scoped webhooks URLs (/admin/workspaces/:id/webhooks) correctly expand Settings accordion and highlight 'Webhooks' submenu item without highlighting 'Workspace' submenu item"
    - "Unit tests cover all active highlighting scenarios and pass successfully"
  artifacts:
    - path: "templates/layout/sidebar_test.go"
      provides: "Unit tests for sidebar path matching functions"
---

<objective>
Refactor and fix the navigation sidebar active highlighting logic to properly handle workspace-scoped URLs for Campaigns and Webhooks, preventing incorrect Settings accordion expansion and incorrect submenu highlighting.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@templates/layout/sidebar.templ
@templates/layout/sidebar_templ.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modify Sidebar Path-matching Helpers and Implement Unit Tests</name>
  <files>templates/layout/sidebar.templ, templates/layout/sidebar_test.go</files>
  <read_first>templates/layout/sidebar.templ</read_first>
  <action>
    Update path-matching logic inside `templates/layout/sidebar.templ` and add a new test file:
    1. Modify `isSettingsActive` in `templates/layout/sidebar.templ` to immediately return `false` if `activePath` contains `/campaigns`.
    2. Modify `isSubmenuActive` in `templates/layout/sidebar.templ`:
       - For `target == "/admin/workspace"`, return true only if path starts with `/admin/workspace` or `/admin/workspaces` AND does not contain `/webhooks` and does not contain `/campaigns`.
       - For `target == "/admin/webhooks"`, return true if path starts with `/admin/webhooks` or contains `/webhooks`.
    3. Modify `isTopLevelActive` in `templates/layout/sidebar.templ`:
       - For `target == "/admin/campaigns"`, return true if path starts with `/admin/campaigns` or contains `/campaigns`.
    4. Create `templates/layout/sidebar_test.go` with table-driven tests:
       - `TestIsSettingsActive` testing `/admin/connections` (true), `/admin/workspaces/123/webhooks` (true), `/admin/workspaces/123/campaigns` (false), `/admin/campaigns` (false), `/admin/inbox` (false).
       - `TestIsTopLevelActive` testing `/admin/campaigns` with target `/admin/campaigns` (true), `/admin/workspaces/123/campaigns` with target `/admin/campaigns` (true), `/admin/workspaces/123/webhooks` with target `/admin/campaigns` (false).
       - `TestIsSubmenuActive` testing `/admin/workspace` with target `/admin/workspace` (true), `/admin/workspaces/123` with target `/admin/workspace` (true), `/admin/workspaces/123/webhooks` with target `/admin/workspace` (false), `/admin/workspaces/123/webhooks` with target `/admin/webhooks` (true), `/admin/workspaces/123/campaigns` with target `/admin/workspace` (false).
    5. Run `PATH=$PATH:/home/pablo/go/bin templ generate ./...` to regenerate `templates/layout/sidebar_templ.go`.
    6. Run tests with `go test -v ./templates/layout/...`.
  </action>
  <verify>
    Execute unit tests via `go test -v ./templates/layout/...` and verify they pass.
  </verify>
  <acceptance_criteria>
    - `isSettingsActive` returns `false` for any path containing `/campaigns`.
    - `isSubmenuActive` for target `/admin/workspace` returns `false` for any path containing `/webhooks` or `/campaigns`.
    - `isSubmenuActive` for target `/admin/webhooks` returns `true` for any path containing `/webhooks`.
    - `isTopLevelActive` for target `/admin/campaigns` returns `true` for any path containing `/campaigns`.
    - All tests in `templates/layout/sidebar_test.go` compile and pass when running `go test -v ./templates/layout/...`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Verify all functions match the criteria via:
- [ ] Run `PATH=$PATH:/home/pablo/go/bin templ generate ./...` to ensure clean generation.
- [ ] Run `go test -v ./templates/layout/...` to assert correctness of the helpers.
- [ ] Run the full test suite `go test ./...` to verify no regressions.
</verification>

<success_criteria>
- The Settings accordion is correctly expanded only for Settings pages (excluding Campaigns).
- Webhooks sub-menu item is correctly highlighted on workspace-scoped Webhook pages.
- Campaigns menu item is correctly highlighted on workspace-scoped Campaign pages.
- All unit tests for sidebar active highlighting path matching are passing.
</success_criteria>

## Artifacts this phase produces

### New File Paths
- [templates/layout/sidebar_test.go](file:///home/pablo/Coding/OmniGo/templates/layout/sidebar_test.go)

### Modified File Paths
- [templates/layout/sidebar.templ](file:///home/pablo/Coding/OmniGo/templates/layout/sidebar.templ)
- [templates/layout/sidebar_templ.go](file:///home/pablo/Coding/OmniGo/templates/layout/sidebar_templ.go)

### Functions Modified
- `isSettingsActive` in [templates/layout/sidebar.templ](file:///home/pablo/Coding/OmniGo/templates/layout/sidebar.templ#L30)
- `isSubmenuActive` in [templates/layout/sidebar.templ](file:///home/pablo/Coding/OmniGo/templates/layout/sidebar.templ#L49)
- `isTopLevelActive` in [templates/layout/sidebar.templ](file:///home/pablo/Coding/OmniGo/templates/layout/sidebar.templ#L68)

### Functions Created
- `TestIsSettingsActive` in [templates/layout/sidebar_test.go](file:///home/pablo/Coding/OmniGo/templates/layout/sidebar_test.go)
- `TestIsTopLevelActive` in [templates/layout/sidebar_test.go](file:///home/pablo/Coding/OmniGo/templates/layout/sidebar_test.go)
- `TestIsSubmenuActive` in [templates/layout/sidebar_test.go](file:///home/pablo/Coding/OmniGo/templates/layout/sidebar_test.go)
