---
phase: 07-media-inbound
plan: "07-03"
type: tdd
wave: 3
depends_on: ["07-02"]
files_modified:
  - internal/platform/postgres/migrations/009_media_and_inbound.sql
  - internal/repository/inbound_dedup.go
  - internal/repository/inbound_dedup_test.go
  - internal/api/handler/waba_webhook.go
  - internal/api/handler/waba_webhook_test.go
  - internal/api/handler/telegram_webhook.go
  - internal/api/handler/telegram_webhook_test.go
  - internal/session/manager.go
  - cmd/omnigo/main.go
autonomous: true
requirements: [INBD-01, INBD-03]
must_haves:
  truths:
    - "WABA webhook implements verification GET challenge (returns hub.challenge)"
    - "WABA status objects ignored"
    - "WABA, Telegram, and WhatsApp Web inbound messages parse content: text, media, location, contacts"
    - "Empty text with media/location/contacts forwarded normally"
    - "Duplicate inbound messages are deduplicated using provider message ID within dedup window (Postgres insert ON CONFLICT DO NOTHING)"
    - "Dedup key is provider-specific"
    - "Inbound events publish to NATS stream INBOUND on subject inbound.events.{workspace_id} with Trace-ID headers"
  prohibitions: []
---

## Artifacts this phase produces

| File Path | Symbol/Artifact | Type | Description |
|-----------|-----------------|------|-------------|
| [009_media_and_inbound.sql](file:///home/pablo/Coding/OmniGo/internal/platform/postgres/migrations/009_media_and_inbound.sql) | `009_media_and_inbound.sql` | Migration | Migration script creating `inbound_dedups` table and workspace `pii_opt_in` flag |
| [inbound_dedup.go](file:///home/pablo/Coding/OmniGo/internal/repository/inbound_dedup.go) | `repository.InboundDedupRepository` | Struct | Postgres repository layer for inbound message deduplication |
| [inbound_dedup.go](file:///home/pablo/Coding/OmniGo/internal/repository/inbound_dedup.go) | `repository.InboundDedupRepository.InsertAndCheck` | Method | Atomically inserts provider message IDs to check for duplicates |
| [waba_webhook.go](file:///home/pablo/Coding/OmniGo/internal/api/handler/waba_webhook.go) | `handler.WABAWebhookHandler` | Struct | Echo handler for incoming Meta WABA webhooks |
| [telegram_webhook.go](file:///home/pablo/Coding/OmniGo/internal/api/handler/telegram_webhook.go) | `handler.TelegramWebhookHandler` | Struct | Modified to extract inbound Telegram media, deduplicate, and NATS queue |
| [manager.go](file:///home/pablo/Coding/OmniGo/internal/session/manager.go) | `session.Manager` | Struct | Modified to handle whatsmeow inbound messages and forward to NATS |

<objective>
Phase 7 Wave 3 sets up database migrations, the deduplication layer, the new WABA inbound verification/ingestion webhook, and extends Telegram and WhatsApp Web adapters to extract inbound media/data payloads and publish events to NATS.
</objective>

<execution_context>
@/home/pablo/Coding/OmniGo/.opencode/gsd-core/workflows/execute-plan.md
@/home/pablo/Coding/OmniGo/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-media-inbound/07-CONTEXT.md
@.planning/phases/07-media-inbound/07-SPEC.md
@.planning/phases/07-media-inbound/07-RESEARCH.md
@internal/repository/recipient_session.go
@internal/api/handler/telegram_webhook.go
@internal/session/manager.go
@cmd/omnigo/main.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Database migrations for workspace PII opt-in and inbound deduplication</name>
  <files>internal/platform/postgres/migrations/009_media_and_inbound.sql</files>
  <requirements>[INBD-01]</requirements>
  <read_first>
Files:
- internal/platform/postgres/migrations/009_media_and_inbound.sql (new migration file)
Sources of truth:
- .planning/phases/07-media-inbound/07-SPEC.md (Requirements 4, 7, and PII opt-in)
- .planning/phases/07-media-inbound/07-RESEARCH.md (Database schema decisions)
  </read_first>
  <acceptance_criteria>
- Goose migration script is created.
- Migration adds pii_opt_in BOOLEAN column to workspaces table, defaulting to FALSE.
- Migration creates inbound_dedups table with composite primary key (workspace_id, channel, provider_message_id) and index on created_at.
- Running goose validate reports no issues.
  </acceptance_criteria>
  <action>
Create internal/platform/postgres/migrations/009_media_and_inbound.sql. Write +goose Up queries: ALTER TABLE workspaces ADD COLUMN pii_opt_in BOOLEAN NOT NULL DEFAULT FALSE; CREATE TABLE inbound_dedups (workspace_id UUID NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE, channel VARCHAR(50) NOT NULL, provider_message_id VARCHAR(255) NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (workspace_id, channel, provider_message_id)); CREATE INDEX idx_inbound_dedups_created_at ON inbound_dedups(created_at);. Write +goose Down queries: DROP TABLE IF EXISTS inbound_dedups; ALTER TABLE workspaces DROP COLUMN IF EXISTS pii_opt_in;. Run goose validate/up to check syntax and database execution.
  </action>
  <verify>
    <automated>go build ./...</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inbound deduplication repository & database implementation</name>
  <files>internal/repository/inbound_dedup.go, internal/repository/inbound_dedup_test.go</files>
  <requirements>[INBD-01]</requirements>
  <read_first>
Files:
- internal/repository/inbound_dedup.go (new repository file)
- internal/repository/inbound_dedup_test.go (new test file)
Sources of truth:
- .planning/phases/07-media-inbound/07-SPEC.md (Requirement 7, Inbound deduplication)
  </read_first>
  <acceptance_criteria>
- InboundDedupRepository exposes InsertAndCheck method.
- Performs atomic insert with pgx ON CONFLICT DO NOTHING.
- Returns true if the message is unique and was inserted, false if duplicate.
- Unit test verifies thread-safe at-most-once logic (only one concurrent transaction inserts).
  </acceptance_criteria>
  <action>
Create internal/repository/inbound_dedup.go defining InboundDedupRepository with a pgxpool.Pool dependency. Write InsertAndCheck(ctx, workspaceID, channel, providerMessageID) method. Implement INSERT INTO inbound_dedups (workspace_id, channel, provider_message_id) VALUES ($1, $2, $3) ON CONFLICT DO NOTHING. Check rows affected: return true if RowsAffected == 1, false otherwise. Create internal/repository/inbound_dedup_test.go and write table-driven tests checking insert success, duplicate insert return value, and concurrent execution checks.
  </action>
  <verify>
    <automated>go test -run TestInboundDeduplicate ./internal/repository -v -count=1</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WABA Inbound Webhook handler (Meta verification & ingestion)</name>
  <files>internal/api/handler/waba_webhook.go, internal/api/handler/waba_webhook_test.go, cmd/omnigo/main.go</files>
  <requirements>[INBD-01, INBD-03]</requirements>
  <read_first>
Files:
- cmd/omnigo/main.go (file being modified)
- internal/api/handler/waba_webhook.go (new handler file)
- internal/api/handler/waba_webhook_test.go (new test file)
Sources of truth:
- .planning/phases/07-media-inbound/07-SPEC.md (Requirement 4, WABA inbound webhook & Meta verification challenge)
- .planning/phases/07-media-inbound/07-RESEARCH.md (WABA webhook token validation)
  </read_first>
  <acceptance_criteria>
- GET /webhooks/waba/:workspace_id verifies hub.verify_token and returns hub.challenge plain text.
- POST /webhooks/waba/:workspace_id parses inbound WABA message payload.
- WABA status updates (sent, delivered, read) are ignored and do not generate inbound events.
- Extracts text, location, contact, and media. For media, calls Graph API to fetch download URL, retrieves bytes, stores in S3, and sets the local proxy URL.
- Uses InboundDedupRepository to reject duplicates.
- Generates a trace ID, writes inbound audit log row, and publishes InboundEventPayload JSON to NATS stream INBOUND on subject inbound.events.{workspace_id} with Trace-ID headers.
- Handler tests verify challenge verification and correct payload parsing.
  </acceptance_criteria>
  <action>
Create internal/api/handler/waba_webhook.go. Define WABAWebhookHandler with db pool, InboundDedupRepository, S3Client, and publisher. In GET method, fetch workspace credentials, compare hub.verify_token with the workspace verification token, and return hub.challenge plain text. In POST method, parse WABA payload structure. Ignore status messages. If the payload contains messages, verify uniqueness using InboundDedupRepository. Extract fields (text, contacts, location, media). For media, fetch download URL using Meta Graph API with workspace credentials token, download bytes, write to S3, and compute proxy URL. Marshal to InboundEventPayload, generate trace ID, insert audit_logs row (direction: inbound, trace_id, payload), and publish to NATS inbound.events.{workspace_id} subject. Register handlers in cmd/omnigo/main.go. Write waba_webhook_test.go.
  </action>
  <verify>
    <automated>go test -run TestWABAWebhook_Inbound ./internal/api/handler -v -count=1</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Telegram Inbound Webhook handler extension</name>
  <files>internal/api/handler/telegram_webhook.go, internal/api/handler/telegram_webhook_test.go</files>
  <requirements>[INBD-01, INBD-03]</requirements>
  <read_first>
Files:
- internal/api/handler/telegram_webhook.go (file being modified)
- internal/api/handler/telegram_webhook_test.go (test file being modified)
Sources of truth:
- .planning/phases/07-media-inbound/07-SPEC.md (Requirement 4, Telegram webhook handler updates)
- .planning/phases/07-media-inbound/07-RESEARCH.md (Telegram file ID to download path flow)
  </read_first>
  <acceptance_criteria>
- Telegram webhook parses message payload text, photo, document, audio, video, location, contact.
- Downloads media by calling Telegram bot getFile endpoint, downloading bytes from the retrieved file path, storing in S3, and mapping to proxy URL.
- Uses InboundDedupRepository to deduplicate using update_id.
- Publishes event payload to NATS subject inbound.events.{workspace_id} with Trace-ID NATS header.
- Writes inbound audit log with trace ID.
- Unit/integration tests pass.
  </acceptance_criteria>
  <action>
Modify internal/api/handler/telegram_webhook.go. Add InboundDedupRepository, S3Client, and publisher dependencies. Update the Handle function. Parse incoming telegram update. Validate X-Telegram-Bot-Api-Secret-Token. Check update_id uniqueness using InboundDedupRepository. Extract message content. For photo, document, audio, video, parse file_id. Fetch filepath from Telegram getFile API using workspace bot token, retrieve file bytes, upload to S3, and set proxy URL. Generate trace ID. Create InboundEventPayload, publish to NATS, and write inbound audit log row. Update tests in telegram_webhook_test.go.
  </action>
  <verify>
    <automated>go test -run TestTelegramWebhook_Inbound ./internal/api/handler -v -count=1</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 5: WhatsApp Web Inbound handler (whatsmeow event listener)</name>
  <files>internal/session/manager.go</files>
  <requirements>[INBD-01, INBD-03]</requirements>
  <read_first>
Files:
- internal/session/manager.go (file being modified)
Sources of truth:
- .planning/phases/07-media-inbound/07-SPEC.md (Requirement 4, WhatsApp Web events)
- .planning/phases/07-media-inbound/07-RESEARCH.md (whatsmeow message download method)
  </read_first>
  <acceptance_criteria>
- Active session listener catches *waEvents.Message events.
- Ignores self-sent messages (v.Info.IsFromMe is true).
- Deduplicates using provider message ID v.Info.ID.
- Extracts text, location, contact, and media. For media, calls whatsmeow client Download method, uploads bytes to S3, and links to proxy URL.
- Publishes InboundEventPayload to NATS inbound.events.{workspace_id} subject.
- Creates inbound audit log entry with trace ID.
  </acceptance_criteria>
  <action>
Inject InboundDedupRepository, S3Client, and publisher into session.Manager. In internal/session/manager.go, locate setupInboundHandler (or the whatsmeow AddEventHandler logic). Inspect incoming waEvents.Message. Assert IsFromMe is false. Check uniqueness using InboundDedupRepository. Parse text, location, contact, and media fields. If media is present, decrypt and download the attachment using whatsmeow's client.Download API. Upload retrieved bytes to S3, generating the proxy URL. Format InboundEventPayload, generate trace ID, write audit log row (direction: inbound, trace_id, payload), and publish to NATS inbound.events.{workspace_id} subject.
  </action>
  <verify>
    <automated>go test -run TestWhatsAppInbound ./internal/session -v -count=1</automated>
  </verify>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register & ASVS Level 1 Compliance

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Spoofing | Webhook event verification | high | mitigate | WABA webhook validates hub.verify_token dynamically per workspace. Telegram webhook validates X-Telegram-Bot-Api-Secret-Token. |
| T-07-05 | Denial of Service | Media download SSRF / resource exhaustion | high | mitigate | Enforce 30s timeout on media download. Reject downloads > 25MB. Buffer download to memory up to 25MB, prevent disk space exhaustion. |
</threat_model>

<verification>
- Inbound dedup repository tests: `go test -run TestInboundDeduplicate ./internal/repository -v -count=1`
- Telegram webhook tests: `go test -run TestTelegramWebhook_Inbound ./internal/api/handler -v -count=1`
- WABA webhook tests: `go test -run TestWABAWebhook_Inbound ./internal/api/handler -v -count=1`
- WhatsApp session tests: `go test -run TestWhatsAppInbound ./internal/session -v -count=1`
- Full test suite compiles: `go build ./...`
- Vet passes: `go vet ./...`
</verification>

<success_criteria>
- WABA webhook implements verification GET challenge (returns hub.challenge)
- WABA, Telegram, and WhatsApp Web inbound messages parse content: text, media, location, contacts
- Duplicate inbound messages are deduplicated using provider message ID within dedup window (Postgres insert ON CONFLICT DO NOTHING)
- Inbound events publish to NATS stream INBOUND on subject inbound.events.{workspace_id} with Trace-ID headers
</success_criteria>

<output>
Create `.planning/phases/07-media-inbound/07-03-SUMMARY.md` when done
</output>
