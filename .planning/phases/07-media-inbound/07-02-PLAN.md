---
phase: 07-media-inbound
plan: "07-02"
type: tdd
wave: 2
depends_on: ["07-01"]
files_modified:
  - internal/channel/dispatcher.go
  - internal/channel/whatsapp/adapter.go
  - internal/channel/whatsapp/adapter_test.go
  - internal/channel/whatsapp/waba.go
  - internal/channel/whatsapp/waba_test.go
  - internal/channel/telegram/telegram.go
  - internal/channel/telegram/telegram_test.go
autonomous: true
requirements: [MEDIA-02, MEDIA-08]
must_haves:
  truths:
    - "WhatsApp Web sends media as native waE2E message using downloaded bytes"
    - "WABA sends media payload using the public media proxy URL"
    - "Telegram sends media via multipart request to sendPhoto/sendDocument/sendAudio/sendVideo"
    - "Media without caption delivers no caption text"
    - "Test concurrent adapter media deliveries for the same message don't interfere (requires a held-out test)"
  prohibitions: []
---

## Artifacts this phase produces

| File Path | Symbol/Artifact | Type | Description |
|-----------|-----------------|------|-------------|
| [dispatcher.go](file:///home/pablo/Coding/PerGo/internal/channel/dispatcher.go) | `dispatcher.MessagePayload` | Struct | Modified to include `Media` field |
| [adapter.go](file:///home/pablo/Coding/PerGo/internal/channel/whatsapp/adapter.go) | `whatsapp.Adapter.Dispatch` | Method | Modified to download from S3 and upload to WhatsApp CDN for native sending |
| [waba.go](file:///home/pablo/Coding/PerGo/internal/channel/whatsapp/waba.go) | `whatsapp.WABAAdapter.Dispatch` | Method | Modified to format outbound REST payloads for WhatsApp Business API media |
| [telegram.go](file:///home/pablo/Coding/PerGo/internal/channel/telegram/telegram.go) | `telegram.TelegramAdapter.Dispatch` | Method | Modified to compile and post multipart requests for Telegram Bot media API |

<objective>
Phase 7 Wave 2 updates outbound adapters (WhatsApp Web, WABA, and Telegram) to deliver media natively through their respective APIs with caption support.
</objective>

<execution_context>
@/home/pablo/Coding/PerGo/.opencode/gsd-core/workflows/execute-plan.md
@/home/pablo/Coding/PerGo/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-media-inbound/07-CONTEXT.md
@.planning/phases/07-media-inbound/07-SPEC.md
@.planning/phases/07-media-inbound/07-RESEARCH.md
@internal/channel/dispatcher.go
@internal/channel/whatsapp/adapter.go
@internal/channel/whatsapp/waba.go
@internal/channel/telegram/telegram.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update channel dispatcher message payload structure</name>
  <files>internal/channel/dispatcher.go</files>
  <requirements>[MEDIA-02]</requirements>
  <read_first>
Files:
- internal/channel/dispatcher.go (file being modified)
Sources of truth:
- .planning/phases/07-media-inbound/07-SPEC.md (Requirement 2, per-channel media delivery)
  </read_first>
  <acceptance_criteria>
- dispatcher.MessagePayload includes the Media struct matching domain message structure.
- Correctly parses NATS JSON messages containing media payloads.
  </acceptance_criteria>
  <action>
Modify internal/channel/dispatcher.go. Import the internal/domain package if needed, or define a local Media struct equivalent. Add Media pointer to the MessagePayload structure.
  </action>
  <verify>
    <automated>go build ./...</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement and verify media support in WhatsApp Web adapter</name>
  <files>internal/channel/whatsapp/adapter.go, internal/channel/whatsapp/adapter_test.go</files>
  <requirements>[MEDIA-02, MEDIA-08]</requirements>
  <read_first>
Files:
- internal/channel/whatsapp/adapter.go (file being modified)
- internal/channel/whatsapp/adapter_test.go (test file being modified)
Sources of truth:
- .planning/phases/07-media-inbound/07-SPEC.md (Requirement 2, WhatsApp Web media message structure)
- .planning/phases/07-media-inbound/07-RESEARCH.md (whatsmeow upload procedure)
  </read_first>
  <acceptance_criteria>
- WhatsApp Web adapter downloads file bytes from S3.
- Calls whatsmeow client Upload method to upload bytes to WhatsApp CDN.
- Formats proper native Image/Document/Audio/Video waE2E messages.
- If Media.Caption is specified, it is mapped to the message caption.
- Unit/integration tests pass.
  </acceptance_criteria>
  <action>
Inject S3Client into WhatsApp Web Adapter in internal/channel/whatsapp/adapter.go. In the Dispatch method, check if payload.Media is not nil. If present, use S3Client to read the stored file bytes. Call the whatsmeow client's Upload method passing the raw bytes and media type. Construct the waE2E message structure based on the media type (ImageMessage, DocumentMessage, AudioMessage, or VideoMessage), mapping the Caption to the target message structure. Send the message. Update adapter_test.go to verify media dispatch logic with mocked S3 downloads and whatsmeow uploads.
  </action>
  <verify>
    <automated>go test -run TestWhatsAppAdapter_Media ./internal/channel/whatsapp -v -count=1</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement and verify media support in WABA adapter</name>
  <files>internal/channel/whatsapp/waba.go, internal/channel/whatsapp/waba_test.go</files>
  <requirements>[MEDIA-02, MEDIA-08]</requirements>
  <read_first>
Files:
- internal/channel/whatsapp/waba.go (file being modified)
- internal/channel/whatsapp/waba_test.go (test file being modified)
Sources of truth:
- .planning/phases/07-media-inbound/07-SPEC.md (Requirement 2, WABA JSON media formats)
  </read_first>
  <acceptance_criteria>
- Constructs WABA outbound media payload using the public proxy URL.
- Matches type image, document, audio, video.
- Handles caption and filename properties correctly.
- Unit/integration tests pass.
  </acceptance_criteria>
  <action>
Modify internal/channel/whatsapp/waba.go's Dispatch function. If payload.Media is present, construct the payload for Meta Graph Cloud API. Since WABA API takes a direct URL, use payload.Media.MediaURL (which was already rewritten to the proxy URL in Wave 1). Set the type parameter to payload.Media.MediaType and construct the corresponding media object (e.g. image object with link, caption). Update waba_test.go.
  </action>
  <verify>
    <automated>go test -run TestWABAAdapter_Media ./internal/channel/whatsapp -v -count=1</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement and verify media support in Telegram adapter</name>
  <files>internal/channel/telegram/telegram.go, internal/channel/telegram/telegram_test.go</files>
  <requirements>[MEDIA-02, MEDIA-08]</requirements>
  <read_first>
Files:
- internal/channel/telegram/telegram.go (file being modified)
- internal/channel/telegram/telegram_test.go (test file being modified)
Sources of truth:
- .planning/phases/07-media-inbound/07-SPEC.md (Requirement 2, Telegram Bot API media endpoints)
  </read_first>
  <acceptance_criteria>
- Constructs multipart payload for Telegram.
- Calls sendPhoto, sendDocument, sendAudio, or sendVideo based on type.
- Maps caption and filename correctly.
- Unit/integration tests pass.
  </acceptance_criteria>
  <action>
Inject S3Client into TelegramAdapter in internal/channel/telegram/telegram.go. In Dispatch, if payload.Media is present, fetch the file bytes from S3Client. Create a multipart form-data payload containing the binary bytes with correct form name (photo, document, audio, video), filename, and optional caption field. POST the request to the respective Telegram bot endpoint (sendPhoto, sendDocument, sendAudio, or sendVideo). Update telegram_test.go.
  </action>
  <verify>
    <automated>go test -run TestTelegramAdapter_Media ./internal/channel/telegram -v -count=1</automated>
  </verify>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register & ASVS Level 1 Compliance

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | Outbound message adapter | medium | mitigate | Validate that S3 file retrieval resolves locally before initiating channel API uploads. |
</threat_model>

<verification>
- WhatsApp Web adapter tests: `go test -run TestWhatsAppAdapter_Media ./internal/channel/whatsapp -v -count=1`
- WABA adapter tests: `go test -run TestWABAAdapter_Media ./internal/channel/whatsapp -v -count=1`
- Telegram adapter tests: `go test -run TestTelegramAdapter_Media ./internal/channel/telegram -v -count=1`
- Full test suite compiles: `go build ./...`
- Vet passes: `go vet ./...`
</verification>

<success_criteria>
- WhatsApp Web delivers media as native waE2E.ImageMessage (not a text link)
- WABA delivers media with type: "image" payload (not text)
- Telegram delivers media via sendPhoto API call (not sendMessage with URL)
- Caption text appears in channel-native caption rendering
</success_criteria>

<output>
Create `.planning/phases/07-media-inbound/07-02-SUMMARY.md` when done
</output>
