---
phase: 07-media-inbound
plan: "07-01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - cmd/omnigo/main.go
  - internal/config/config.go
  - internal/domain/message.go
  - internal/domain/message_test.go
  - internal/platform/storage/s3.go
  - internal/platform/storage/s3_test.go
  - internal/api/handler/media.go
  - internal/api/handler/message.go
  - internal/api/handler/message_test.go
autonomous: true
requirements: [MEDIA-01, MEDIA-03]
must_haves:
  truths:
    - "POST /messages with media payload validates type, url, caption, and filename"
    - "Boundary checks are inclusive (file <= 25,000,000 bytes accepted, > 25,000,000 bytes rejected with 422 'media_size_exceeded')"
    - "Media download failure (404, timeout, DNS) rejects message with 422 before NATS enqueue"
    - "Media stored in S3 at {workspace_id}/{content_hash}.{ext}"
    - "GET /media/:workspace_id/:hash endpoint streams file from S3"
    - "Test that duplicate POST /messages with same media_url doesn't create duplicate S3 objects (requires a held-out test)"
    - "Test concurrent media downloads for the same URL don't corrupt the stored object (requires a held-out test)"
    - "Test re-uploading same content produces same S3 key (content-addressed hash) (requires a held-out test)"
  prohibitions: []
---

## Artifacts this phase produces

| File Path | Symbol/Artifact | Type | Description |
|-----------|-----------------|------|-------------|
| [message.go](file:///home/pablo/Coding/OmniGo/internal/domain/message.go) | `domain.Media` | Struct | Data structure representing media payloads (URL, type, filename, caption) |
| [message.go](file:///home/pablo/Coding/OmniGo/internal/domain/message.go) | `domain.CreateMessageRequest` | Struct | Modified to include `Media` field and validation rules |
| [message.go](file:///home/pablo/Coding/OmniGo/internal/domain/message.go) | `domain.QueueMessage` | Struct | Modified to include `Media` field |
| [config.go](file:///home/pablo/Coding/OmniGo/internal/config/config.go) | `config.Config` | Struct | Modified to parse and store S3 connection parameters |
| [s3.go](file:///home/pablo/Coding/OmniGo/internal/platform/storage/s3.go) | `storage.S3Client` | Struct | Standard S3 client wrapper for object storage operations |
| [s3.go](file:///home/pablo/Coding/OmniGo/internal/platform/storage/s3.go) | `storage.DownloadAndValidate` | Method | Utility to download remote files with size limits and timeouts |
| [media.go](file:///home/pablo/Coding/OmniGo/internal/api/handler/media.go) | `handler.MediaHandler` | Struct | Echo handler serving stored S3 objects securely via proxy URL |
| [message.go](file:///home/pablo/Coding/OmniGo/internal/api/handler/message.go) | `handler.MessageHandler` | Struct | Modified to download and validate media before NATS enqueue |

<objective>
Phase 7 Wave 1 defines the schema for unified media payload delivery, validates and downloads files within the 25MB boundary, stores them in S3-compatible storage, and exposes a proxy HTTP route for streaming.
</objective>

<execution_context>
@/home/pablo/Coding/OmniGo/.opencode/gsd-core/workflows/execute-plan.md
@/home/pablo/Coding/OmniGo/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-media-inbound/07-CONTEXT.md
@.planning/phases/07-media-inbound/07-SPEC.md
@.planning/phases/07-media-inbound/07-RESEARCH.md
@internal/domain/message.go
@internal/config/config.go
@internal/api/handler/message.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Media struct to domain message models & update request validation</name>
  <files>internal/domain/message.go, internal/domain/message_test.go</files>
  <requirements>[MEDIA-01]</requirements>
  <read_first>
Files:
- internal/domain/message.go (file being modified)
- internal/domain/message_test.go (test file being modified)
Sources of truth:
- .planning/phases/07-media-inbound/07-SPEC.md (Requirement 1, unified media field)
  </read_first>
  <acceptance_criteria>
- CreateMessageRequest, QueueMessage, and MessagePayload structs include a Media pointer field.
- Message validation requires at least Body or Media (or both) to be present.
- Enforce validation: MediaType must be one of image, document, audio, video.
- Enforce validation: MediaURL must be a valid HTTP/HTTPS URL.
- Enforce validation: If MediaType is document, Filename is required.
- Invalid requests return invalid_payload code and field-level validation details.
- All unit tests pass successfully.
  </acceptance_criteria>
  <action>
Create a new Media struct in internal/domain/message.go containing MediaURL (string, json tag media_url), MediaType (string, json tag media_type), Filename (string, json tag filename), and Caption (string, json tag caption). Add the Media pointer field to CreateMessageRequest and QueueMessage. In internal/domain/message.go:ValidateMessage, assert that if both Body and Media are empty, a FieldError is appended to details. Verify that if Media is provided, MediaURL is not empty and has an http or https prefix, and MediaType matches one of the allowed values: image, document, audio, video. If MediaType is document, verify that Filename is not empty. In internal/domain/message_test.go, add table-driven tests checking for valid and invalid combinations (e.g. valid document with filename, document without filename, invalid media type, missing url, valid caption). Run the unit tests to verify.
  </action>
  <verify>
    <automated>go test -run TestCreateMessageRequest_Validate ./internal/domain -v -count=1</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create S3 storage client and download helper with size limits & timeout</name>
  <files>internal/platform/storage/s3.go, internal/platform/storage/s3_test.go, internal/config/config.go</files>
  <requirements>[MEDIA-03]</requirements>
  <read_first>
Files:
- internal/config/config.go (file being modified)
- internal/platform/storage/s3.go (new storage file)
- internal/platform/storage/s3_test.go (new test file)
Sources of truth:
- .planning/phases/07-media-inbound/07-SPEC.md (Requirement 3, S3 storage size boundaries)
- .planning/phases/07-media-inbound/07-RESEARCH.md (S3 config parameters & size checks)
  </read_first>
  <acceptance_criteria>
- Config parses S3_ENDPOINT, S3_BUCKET, S3_ACCESS_KEY, S3_SECRET_KEY, S3_REGION, S3_USE_PATH_STYLE from environment.
- S3Client initializes using aws-sdk-go-v2 and correctly sets BaseEndpoint and UsePathStyle for MinIO compatibility.
- DownloadAndValidate downloads file with 30s timeout, limits reading to 25,000,001 bytes, and returns media_size_exceeded if length is > 25,000,000 bytes.
- Detects MIME type using first 512 bytes with http.DetectContentType.
- Key format is {workspace_id}/{content_hash}.{ext}.
- Upload stores bytes in S3.
- Unit/integration tests pass.
  </acceptance_criteria>
  <action>
Add S3 environment variables to internal/config/config.go. Create internal/platform/storage/s3.go. Define Config, S3Client, and DownloadResult structures. Initialize S3 client using LoadDefaultConfig and NewFromConfig. In DownloadAndValidate, instantiate an HTTP client with a 30s timeout, issue an HTTP GET to the source url, and wrap the response body in an io.LimitReader for 25,000,001 bytes. If the returned bytes exceed 25,000,000 bytes, return a custom media_size_exceeded error. Detect MIME type and map to extension. Hash contents using SHA-256 to produce content_hash. Write Upload method using PutObject with calculated key and content-type. Create internal/platform/storage/s3_test.go with mock HTTP server and verification loops. Use go get to fetch the AWS SDK v2 packages. Run test suite.
  </action>
  <verify>
    <automated>go test -run TestDownloadAndValidate ./internal/platform/storage -v -count=1</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create Media proxy Echo route to serve stored S3 files</name>
  <files>internal/api/handler/media.go, cmd/omnigo/main.go</files>
  <requirements>[MEDIA-03]</requirements>
  <read_first>
Files:
- cmd/omnigo/main.go (file being modified)
- internal/api/handler/media.go (new handler file)
Sources of truth:
- .planning/phases/07-media-inbound/07-SPEC.md (Requirement 3, serves via proxy URL)
  </read_first>
  <acceptance_criteria>
- GET /media/:workspace_id/:hash endpoint streams stored bytes from S3.
- Authenticates using existing auth middleware (API Key or dashboard session auth).
- Streams from S3 directly to HTTP response writer.
- Returns 404 if file does not exist in S3.
- Sets correct Content-Type header on stream.
  </acceptance_criteria>
  <action>
Create internal/api/handler/media.go defining MediaHandler with S3Client. Define Handle function for GET /media/:workspace_id/:hash. Perform S3 GetObject call using the workspace_id and hash parameters. Read Content-Type from S3 output, set header on Echo context response, and stream GetObject body using io.Copy. Return 404 if S3 returns NoSuchKey error. In cmd/omnigo/main.go, instantiate S3Client, instantiate MediaHandler, and register route e.GET("/media/:workspace_id/:hash", mediaHandler.Handle) protected by AuthMiddleware. Verify handler works.
  </action>
  <verify>
    <automated>go build ./...</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Hook download-and-store pipeline into message ingestion</name>
  <files>internal/api/handler/message.go, internal/api/handler/message_test.go</files>
  <requirements>[MEDIA-01, MEDIA-03]</requirements>
  <read_first>
Files:
- internal/api/handler/message.go (file being modified)
- internal/api/handler/message_test.go (test file being modified)
Sources of truth:
- .planning/phases/07-media-inbound/07-SPEC.md (Requirement 3, size limit, download failure 422 rejection)
  </read_first>
  <acceptance_criteria>
- MessageHandler accepts S3Client as parameter.
- On ingestion, if Media field exists, calls DownloadAndValidate.
- If download fails (timeout, 404, DNS error), returns 422 with media_download_failed.
- If download exceeds 25MB, returns 422 with media_size_exceeded.
- Stores downloaded media in S3 and rewires the message payload's MediaURL to the internal proxy URL.
- Test suite verifies download failure rejection.
  </acceptance_criteria>
  <action>
Modify internal/api/handler/message.go's MessageHandler to contain S3Client. In the Create handler, check if req.Media is not nil. If present, call storage.DownloadAndValidate with the original MediaURL and max 25MB limit. If validation/download fails, return HTTP 422 with structured ErrorResponse containing media_size_exceeded or media_download_failed details. Otherwise, write the bytes to S3 using S3Client.Upload, rewrite req.Media.MediaURL to /media/{workspace_id}/{hash}.{ext}, and proceed to publish to NATS queue. Update internal/api/handler/message_test.go to mock storage downloads and verify happy and error paths.
  </action>
  <verify>
    <automated>go test -run TestMessageHandler_Create ./internal/api/handler -v -count=1</automated>
  </verify>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register & ASVS Level 1 Compliance

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | Inbound message payload | high | mitigate | Input validation on media type, URL scheme, and size bounds (max 25MB). Use io.LimitReader for all downloads. |
| T-07-02 | Elevation of Privilege | media endpoint | high | mitigate | Protect GET /media/:workspace_id/:hash with workspace API key / session authentication. Enforce workspace scoping. |
| T-07-05 | Denial of Service | Media download SSRF / resource exhaustion | high | mitigate | Enforce 30s timeout on media download. Reject downloads > 25MB. Buffer download to memory up to 25MB, prevent disk space exhaustion. |
| T-07-SC | Tampering | S3 Client Dependency | medium | mitigate | Validate imported Go module checksums and verify go.sum updates for aws-sdk-go-v2 packages. |
</threat_model>

<verification>
- Domain tests: `go test -run TestCreateMessageRequest_Validate ./internal/domain -v -count=1`
- Storage client tests: `go test ./internal/platform/storage/... -v -count=1`
- Message ingestion tests: `go test -run TestMessageHandler_Create ./internal/api/handler -v -count=1`
- Full test suite compiles: `go build ./...`
- Vet passes: `go vet ./...`
</verification>

<success_criteria>
- POST /messages with media payload validates type, url, caption, and filename
- Boundary checks are inclusive (file <= 25,000,000 bytes accepted, > 25,000,000 bytes rejected with 422 'media_size_exceeded')
- Media download failure (404, timeout, DNS) rejects message with 422 before NATS enqueue
- Media stored in S3 at {workspace_id}/{content_hash}.{ext}
- GET /media/:workspace_id/:hash endpoint streams file from S3
</success_criteria>

<output>
Create `.planning/phases/07-media-inbound/07-01-SUMMARY.md` when done
</output>
