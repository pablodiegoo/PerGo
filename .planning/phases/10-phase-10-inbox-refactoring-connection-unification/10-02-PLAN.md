---
wave: 2
depends_on:
  - 10-01-PLAN.md
files_modified:
  - internal/api/handler/admin/device.go
  - templates/pages/devices.templ
  - templates/layout/sidebar.templ
  - cmd/pergo/main.go
  - internal/api/handler/admin/playground.go
  - internal/api/handler/admin/device_test.go
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-03
  - ADMIN-04
  - ADMIN-06
---

# Plan 10-02: Unified Connections Dashboard & Playground Decommission

Consolidate channel connection management under a single screen inside the admin panel. Enable operators to configure WhatsApp Web, Meta WABA, and Telegram Bot connections in one place. Decommission the standalone Developer Playground by integrating connectivity test dispatchers directly into connection table rows.

## Tasks

<tasks>
<task id="1">
  <name>Repurpose DeviceHandler to support ConnectionRepository and creation forms</name>
  <read_first>
    <file>/home/pablo/Coding/PerGo/internal/api/handler/admin/device.go</file>
    <file>/home/pablo/Coding/PerGo/internal/repository/connection.go</file>
  </read_first>
  <action>
    Modify `internal/api/handler/admin/device.go`:
    1. Update `DeviceHandler` struct to include `Connections *repository.ConnectionRepository` and other necessary dependencies (e.g. `Publisher *queue.JetStreamPublisher`).
    2. Update `List` method to resolve the active `workspace_id` from the cookie (using `resolveWorkspaceID` helper similar to `inbox.go`) and query `h.Connections.ListByWorkspace` to load all connections.
    3. Update `StartPairing` to handle registration and association with the correct workspace connection.
    4. Implement new endpoint/handler methods:
       - `POST /admin/devices/create` to handle creating WABA and Telegram Bot connections. Validate credentials (e.g. by fetching bot identity via Telegram Bot API or triggering Meta template synchronization). Store encrypted credentials via `Connections.SaveCredentials`.
       - `GET /admin/devices/test` to render the connectivity test modal.
       - `POST /admin/devices/test` to publish a test outbound message to `messages.outbound` JetStream subject.
       - `GET /admin/devices/test/ws` (or `/admin/connections/test/ws`) WebSocket endpoint that streams NATS outbound, inbound, and webhook dispatch events live to the client.
    5. Update the `Disconnect` (or delete) handler (e.g. `DELETE /admin/devices/:id` or `/admin/connections/:id`) to handle disconnecting by JID for WhatsApp Web, and make sure it calls `h.Repo.Delete(ctx, id)` (where `h.Repo` is `ConnectionRepository` / `session.DeviceRepository`) to completely purge connection records from the database.
  </action>
  <acceptance_criteria>
    - Handlers compile without errors.
    - list endpoint queries connection records from the database instead of only active whatsmeow devices.
    - `/admin/devices/test` triggers enqueuing a test outbound message via NATS.
  </acceptance_criteria>
</task>

<task id="2">
  <name>Refactor templates/pages/devices.templ for unified connections</name>
  <read_first>
    <file>/home/pablo/Coding/PerGo/templates/pages/devices.templ</file>
    <file>/home/pablo/Coding/PerGo/templates/components/device_card.templ</file>
    <file>/home/pablo/Coding/PerGo/.planning/spikes/007-inbox-polling-stability/main.go</file>
  </read_first>
  <action>
    Modify `templates/pages/devices.templ` and update or add necessary templ components:
    1. Repurpose `DeviceListPage` to accept `[]*repository.Connection` instead of `[]*session.Device`.
    2. Update the main table layout to show Connection Name, Channel Icon, Status Badge, Sender Identity (Phone / Bot username), and Connected Since timestamp.
    3. Add a "Nova Conexão" button that opens an HTMX-swapped modal containing:
       - Channel Type select dropdown (WhatsApp Web, WABA Cloud, Telegram).
       - Dynamic fields updated via vanilla JavaScript when selection changes.
       - Ban warning displayed prominently for WhatsApp Web option.
    4. Render a "Testar" button in each connection row that opens the test send modal (`/admin/devices/test?id={id}`).
    5. Render an "Excluir" button that triggers `DELETE /admin/devices/{id}` (or `/admin/connections/{id}`).
  </action>
  <acceptance_criteria>
    - `templates/pages/devices_templ.go` compiles successfully.
    - Main Connections table renders WABA, Telegram, and WhatsApp Web records in a single list.
    - Clicking "Nova Conexão" opens the creation modal with dynamic channel inputs and warning banners.
    - Test modal renders receiver input and test textarea correctly.
  </acceptance_criteria>
</task>

<task id="3">
  <name>Decommission the Developer Playground</name>
  <read_first>
    <file>/home/pablo/Coding/PerGo/cmd/pergo/main.go</file>
    <file>/home/pablo/Coding/PerGo/templates/layout/sidebar.templ</file>
  </read_first>
  <action>
    Perform cleanup steps:
    1. Remove route registrations for `/admin/playground*` and `playgroundHandler` initialization inside `cmd/pergo/main.go`.
    2. Remove the "Playground" `<li>` link element from `templates/layout/sidebar.templ`.
    3. Delete the file `internal/api/handler/admin/playground.go`.
    4. Register the new WebSocket test endpoint `/admin/devices/test/ws` (or `/admin/connections/test/ws`) and the delete endpoint `/admin/devices/:id` (or `/admin/connections/:id`) on `DeviceHandler` in `cmd/pergo/main.go`.
  </action>
  <acceptance_criteria>
    - Server builds successfully without any references to `PlaygroundHandler`.
    - Sidebar does not show the Playground link.
    - `internal/api/handler/admin/playground.go` is removed from the codebase.
  </acceptance_criteria>
</task>

<task id="4">
  <name>Update device/connection handler tests</name>
  <read_first>
    <file>/home/pablo/Coding/PerGo/internal/api/handler/admin/device_test.go</file>
  </read_first>
  <action>
    Update tests in `internal/api/handler/admin/device_test.go` to match the connection-based handler signature and mock database calls for unified listing, creation, and test sending.
  </action>
  <acceptance_criteria>
    - All tests in `device_test.go` pass successfully.
  </acceptance_criteria>
</task>
</tasks>

## Verification Criteria

### Automated Tests
- Run `go test -v ./internal/api/handler/admin -run TestDevice` to ensure list, pairing, and test routes verify properly.

### Manual Verification
1. Run PerGo and navigate to the admin console.
2. Verify that the "Playground" link is missing from the sidebar.
3. Open the "Conexões" tab.
4. Verify that:
   - Clicking "Nova Conexão" opens a modal.
   - Changing the channel type updates input fields dynamically (e.g. showing Token inputs for WABA/Telegram).
   - Selecting WhatsApp Web displays the ban warning.
   - For an active connection, clicking "Testar" opens the sending tester modal. Submitting a test dispatch triggers NATS queue routing successfully.
   - Clicking "Excluir" removes the connection and hides its table row.

## Must Haves (Goal-Backward Verification)
- [ ] Playground sidebar link and route registrations are completely removed.
- [ ] Creating WhatsApp Web starts pairing QR flow inside the modal, while WABA/Telegram Bot saves synchronously.
- [ ] Connections row contains a "Testar" button that opens a test send modal.

## Artifacts this phase produces
- **Functions/Methods modified**:
  - `admin.DeviceHandler.List`
  - `admin.DeviceHandler.StartPairing`
  - `admin.DeviceHandler.Disconnect`
- **Functions/Methods added**:
  - `admin.DeviceHandler.Create` (POST `/admin/devices/create`)
  - `admin.DeviceHandler.TestForm` (GET `/admin/devices/test`)
  - `admin.DeviceHandler.RunTest` (POST `/admin/devices/test`)
  - `admin.DeviceHandler.WS` (GET `/admin/devices/test/ws`)
- **Tests modified**:
  - `internal/api/handler/admin/device_test.go`
- **Files deleted**:
  - `internal/api/handler/admin/playground.go`
