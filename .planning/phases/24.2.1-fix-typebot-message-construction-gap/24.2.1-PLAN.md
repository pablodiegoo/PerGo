# Phase 24.2.1: fix-typebot-message-construction-gap - Plan

## Task 1: Update Typebot Client `StartChatRequest` Model
<read_first>
- internal/integration/typebot/client.go
</read_first>
<acceptance_criteria>
- `StartChatRequest` struct includes `Message` (string, omitempty) and `SessionID` (string, omitempty).
- `StartChatRequest.PrefilledVariables` type is changed from `map[string]string` to `map[string]any`.
- `Client.StartChat` method signature accepts `req StartChatRequest` instead of `prefilledVariables`.
- `Client.StartChat` properly marshals `req` directly instead of manually building it inside the method.
</acceptance_criteria>
<action>
1. Open `internal/integration/typebot/client.go`.
2. Update the `StartChatRequest` struct as described.
3. Modify the `StartChat` function signature to accept `req StartChatRequest`.
4. Remove the local `reqBody` struct construction inside `StartChat` and marshal the provided `req` parameter instead.
</action>

## Task 2: Update Typebot Client Tests
<read_first>
- internal/integration/typebot/client_test.go
</read_first>
<acceptance_criteria>
- `TestTypebotClient_StartChat` is updated to call `StartChat` with a populated `StartChatRequest`.
- The test verifies successful execution with the new signature.
</acceptance_criteria>
<action>
1. Open `internal/integration/typebot/client_test.go`.
2. Update the `StartChat` call in `TestTypebotClient_StartChat` to pass a valid `StartChatRequest` object containing `Message`, `SessionID`, and nested metadata in `PrefilledVariables` instead of just a map or `nil`.
</action>

## Task 3: Implement Message Construction Logic in Forwarder
<read_first>
- internal/integration/typebot/forwarder.go
</read_first>
<acceptance_criteria>
- `SyncInboundMessage` computes a deterministic `sessionId` as `<ConnectionID>:<SenderIdentity>` (D-03, D-04).
- `SyncInboundMessage` computes `messageText`. It sets `[Media Attachment]` (D-07) for messages with media, `[Unsupported message: location]` for locations, and `[Unsupported message: contact]` for contacts (D-09). Falls back to `event.Body` otherwise (D-02).
- `pergoMetadata` map is generated with `media_url` (D-05, D-06) and `media_type` (D-08) when media is present, nested under `pergo_metadata` (D-01).
- `ContinueChat` uses the computed `messageText` instead of raw `event.Body`.
- `StartChat` uses a newly populated `StartChatRequest` containing `Message`, `SessionID`, and `PrefilledVariables` (which includes `pergo_metadata` if populated).
</acceptance_criteria>
<action>
1. Open `internal/integration/typebot/forwarder.go`.
2. Inside `SyncInboundMessage`, just before the session check (`session, err := f.sessionRepo...`), add logic to compute `messageText` based on `event.Media`, `event.Location`, `event.Contacts`, and `event.Body`.
3. Compute `pergoMetadata` conditionally if media is present.
4. Compute `sessionId := fmt.Sprintf("%s:%s", event.ConnectionID.String(), event.From)`.
5. Update the `ContinueChat` call to pass `messageText`.
6. Update the `StartChat` payload construction to build a `map[string]any` for `prefilled`, adding `pergo_metadata` to it if not nil.
7. Call `f.client.StartChat` passing a constructed `StartChatRequest`.
</action>

## Task 4: Update Forwarder Tests to Assert Message Construction
<read_first>
- internal/integration/typebot/forwarder_test.go
</read_first>
<acceptance_criteria>
- `TestTypebotForwarder_PopulatesRoutingFields` simulates an inbound event with a Media payload.
- The mocked Typebot API server validates that the decoded request body has the expected `message` (`[Media Attachment]`), `sessionId`, and `prefilledVariables` (containing `pergo_metadata.media_url`).
</acceptance_criteria>
<action>
1. Open `internal/integration/typebot/forwarder_test.go`.
2. In `TestTypebotForwarder_PopulatesRoutingFields`, add an `inbound.InboundMedia` struct to the mocked `ev` (with a dummy `MediaURL` and `MediaType`).
3. Update the HTTP mock server for the `startChat` endpoint to read the request body, unmarshal it into a map or struct, and explicitly assert that `sessionId`, `message`, and `prefilledVariables.pergo_metadata` match expectations. Return an error or fail the test if the assertions fail inside the handler.
</action>
