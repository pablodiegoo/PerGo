---
wave: 1
depends_on: []
files_modified:
  - internal/platform/postgres/migrations/021_create_user_action_logs.sql
  - internal/repository/user_action_log.go
  - internal/repository/user_action_log_test.go
  - internal/api/middleware/audit.go
  - internal/api/middleware/audit_test.go
  - cmd/pergo/main.go
  - internal/api/handler/admin/user_logs.go
  - templates/pages/user_logs.templ
  - templates/layout/sidebar.templ
autonomous: true
---

# Plan: User & API Action Logs

## Objective
Implement administrative action audit logging in PerGo. This tracks all operations performed via the public API (using API Keys) and the administrator dashboard (using operator sessions), recording IP, User-Agent, and detailed JSONB metadata.

## Tasks

<tasks>
<task id="T1">
  <read_first>
    - internal/platform/postgres/migrations/001_create_schema.sql
  </read_first>
  <acceptance_criteria>
    - Migration file `internal/platform/postgres/migrations/021_create_user_action_logs.sql` is created.
    - Runs `go test ./...` or `make build` and migrations compile successfully.
    - Schema contains `user_action_logs` table with indices on `(workspace_id, created_at DESC)`.
  </acceptance_criteria>
  <action>
    Create a new goose migration file in `internal/platform/postgres/migrations/` defining the `user_action_logs` table, its columns, foreign key to workspaces, and composite index.
  </action>
</task>

<task id="T2">
  <read_first>
    - internal/repository/repository.go
    - internal/repository/connection.go
  </read_first>
  <acceptance_criteria>
    - Repository struct `UserActionLogRepository` is defined in `internal/repository/user_action_log.go`.
    - Methods `Insert(ctx, log)` and `ListByWorkspace(ctx, workspaceID, limit, offset)` are implemented.
    - Unit tests in `internal/repository/user_action_log_test.go` compile and pass.
  </acceptance_criteria>
  <action>
    Implement `UserActionLogRepository` exposing insertion and paginated workspace lookup methods. Create unit tests checking polymorphic insertions and verify that JSONB metadata decodes cleanly.
  </action>
</task>

<task id="T3">
  <read_first>
    - internal/api/middleware/auth.go
  </read_first>
  <acceptance_criteria>
    - Middleware `AuditMiddleware` is defined in `internal/api/middleware/audit.go`.
    - Automatically skips bypass routes (`/`, `/healthz`, `/readyz`, `/static/*`).
    - Capture IP, User-Agent, action name, and authenticated actor details, logging them asynchronously.
    - Unit tests in `internal/api/middleware/audit_test.go` pass.
  </acceptance_criteria>
  <action>
    Create `AuditMiddleware` to capture request info, extract key prefix/name from context, and trigger asynchronous database log inserts. Write unit tests mocking repository interfaces to check correct trigger behavior.
  </action>
</task>

<task id="T4">
  <read_first>
    - internal/api/handler/admin/dashboard.go
    - cmd/pergo/main.go
  </read_first>
  <acceptance_criteria>
    - `AuditMiddleware` is registered globally or on the API group (`/api/v1/*`) in `cmd/pergo/main.go`.
    - Admin handlers (e.g. creating campaigns or connections) trigger manual `user_action_logs` repository writes with type `'user'` and source `'dashboard'`.
  </acceptance_criteria>
  <action>
    Register the audit middleware in `main.go` and inject `UserActionLogRepository` into dashboard handlers. Update action endpoints to record operations (like pairing a connection or scheduling a campaign) to the action logs repository.
  </action>
</task>

<task id="T5">
  <read_first>
    - templates/layout/sidebar.templ
  </read_first>
  <acceptance_criteria>
    - `templates/pages/user_logs.templ` is created with table, filter controls, and modal details.
    - Echo handler `internal/api/handler/admin/user_logs.go` is created.
    - Routing in `cmd/pergo/main.go` maps `/admin/workspaces/:workspace_id/settings/user-logs` to render the template.
    - Sidebar accordion shows active sub-option and highlights correctly.
  </acceptance_criteria>
  <action>
    Create `user_logs.templ` template and `user_logs.go` handlers. Wire settings route in `main.go` and update the sidebar navigation list to link to the new logs page.
  </action>
</task>

<task id="T6">
  <read_first>
    - Makefile
  </read_first>
  <acceptance_criteria>
    - Code generation via `templ generate` compiles without errors.
    - `go test ./...` passes project-wide.
  </acceptance_criteria>
  <action>
    Compile templates using `templ generate` and run the full test suite to guarantee correctness.
  </action>
</task>
</tasks>

## Must-Haves for Goal-Backward Verification
- [ ] User action logs table stores ip_address, user_agent, actor_name, action, source, and JSONB metadata.
- [ ] Public API requests (`POST /api/v1/messages`) are recorded automatically via middleware.
- [ ] Operator panel actions are recorded using current session email.
- [ ] "Users" logs tab is visible in settings sidebar and renders records with modal metadata inspector.
- [ ] All tests compile and pass.

## Artifacts this phase produces
- Migration file `internal/platform/postgres/migrations/021_create_user_action_logs.sql`.
- Repository file `internal/repository/user_action_log.go`.
- Middleware file `internal/api/middleware/audit.go`.
- Handler file `internal/api/handler/admin/user_logs.go`.
- Template file `templates/pages/user_logs.templ`.
- Settings route in `cmd/pergo/main.go`.
