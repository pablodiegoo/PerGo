---
phase: 11-settings-sidebar-layout-unification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/layout/sidebar.templ
  - cmd/pergo/main.go
  - templates/pages/workspaces.templ
  - templates/pages/webhooks.templ
  - templates/pages/telemetry.templ
  - templates/pages/devices.templ
  - templates/pages/audit.templ
  - cmd/pergo/admin_test.go
autonomous: false
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-05
user_setup: []

must_haves:
  truths:
    - "Left sidebar has an expandable/collapsible 'Configurações' menu under which Connections (Conexões), Logs, Workspace, Webhooks, and Telemetry links are nested (D-01, D-02)"
    - "Clicking 'Configurações' parent toggles the submenu using Tailwind CSS transition animations with chevrons rotating smoothly (D-03)"
    - "The submenu automatically expands on load and highlights the active child item when visiting any sub-page within Configurações (D-04)"
    - "Old top tab switchers on the Workspace, Webhooks, and Telemetry settings pages are removed to clean up visual clutter (D-05)"
    - "All configurations sub-pages use the unified page header design with clean spacing, title headings, and descriptive text (D-06)"
  artifacts:
    - "templates/layout/sidebar.templ"
    - "cmd/pergo/main.go"
    - "templates/pages/workspaces.templ"
    - "templates/pages/webhooks.templ"
    - "templates/pages/telemetry.templ"
    - "templates/pages/devices.templ"
    - "templates/pages/audit.templ"
  key_links:
    - "Active path context propagation from request middleware to the Templ template renders"
    - "URL routing alias mapping for new settings URLs like /admin/connections, /admin/workspace, and /admin/logs in cmd/pergo/main.go"
---

# User Story
**As a** system operator, **I want to** navigate the system configuration through a nested accordion sidebar and use a unified settings layout, **so that** my administration interface is clean, uncluttered, and efficient.

<objective>
Implement a collapsible configurations left sidebar submenu with server-side active route state persistence and standard layout page headers, removing all old top tab switchers from Workspace, Webhooks, and Telemetry settings.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-settings-sidebar-layout-unification/11-CONTEXT.md
@.planning/phases/11-settings-sidebar-layout-unification/11-UI-SPEC.md
@.agents/skills/spike-findings-pergo/references/settings-ui.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Route Aliases Registration & Request Path Middleware</name>
  <files>
    cmd/pergo/main.go
  </files>
  <read_first>
    cmd/pergo/main.go
  </read_first>
  <action>
    Define a Go request middleware (e.g. `PathMiddleware`) or inline handler inside `cmd/pergo/main.go` that extracts the current request path (e.g. `c.Request().URL.Path`) and injects it into the request's context (e.g., using key `"active_path"`). Ensure this middleware is applied to the protected `adminGroup` router group.
    
    Register the route aliases in `cmd/pergo/main.go` inside the protected `adminGroup` router block to support the submenu links (per D-02):
    - Mapped route `/admin/connections` to point to `deviceHandler.List`.
    - Mapped route `/admin/workspace` to point to `workspaceHandler.List`.
    - Mapped route `/admin/workspace/:id` to point to `workspaceHandler.Detail`.
    - Mapped route `/admin/logs` to redirect to `/admin/logs/outbound`.
    - Mapped route `/admin/logs/outbound` to point to `auditHandler.ListOutbound`.
    - Mapped route `/admin/logs/inbound` to point to `auditHandler.ListInbound`.
    - Mapped route `/admin/logs/outbound/export` to point to `auditHandler.ExportOutboundCSV`.
    - Mapped route `/admin/logs/inbound/export` to point to `auditHandler.ExportInboundCSV`.
    
    Ensure original routes (like `/admin/devices`, `/admin/workspaces`, `/admin/audit/*`) remain active for compatibility.
  </action>
  <verify>
    Run `go test ./...` to verify compiling, and execute curl requests to `/admin/connections`, `/admin/workspace`, `/admin/logs` with correct auth to verify they match handlers.
  </verify>
  <done>
    New paths `/admin/connections`, `/admin/workspace`, and `/admin/logs` route structures are registered and functional, and path middleware successfully injects the current route URL into context.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Collapsible Sidebar Accordion with Active State Persistence</name>
  <files>
    templates/layout/sidebar.templ
  </files>
  <read_first>
    templates/layout/sidebar.templ
    .agents/skills/spike-findings-pergo/references/settings-ui.md
  </read_first>
  <action>
    Modify `templates/layout/sidebar.templ` to:
    - Replace the static "Configurações" link with a collapsible configurations nested submenu button and list container (per D-01).
    - Submenu links: **Conexões** (`/admin/connections`), **Logs** (`/admin/logs`), **Workspace** (`/admin/workspace`), **Webhooks** (`/admin/webhooks`), and **Telemetry** (`/admin/telemetry`) (per D-02).
    - Implement a small vanilla JS toggler `toggleSettingsSubmenu` to expand/collapse heights and rotate the chevron icon using Tailwind classes (e.g. `max-h-0` to `max-h-[240px]`, `rotate-180`, `transition-all duration-200`) (per D-03).
    - Extract the active path inside the template using a helper to fetch `"active_path"` from the context. If the current route prefix matches `/admin/connections`, `/admin/devices`, `/admin/logs`, `/admin/audit`, `/admin/workspace`, `/admin/workspaces`, `/admin/webhooks`, or `/admin/telemetry`:
      - Force the settings submenu `<ul>` list to render as expanded (`max-h-[240px]`, `opacity-100`) and the chevron rotated by default (per D-04).
      - Highlight the corresponding active sub-item using the accent background styling.
      - Apply parent highlight styling to the "Configurações" main button (`bg-zinc-200/60`, `text-zinc-900`, `font-semibold`).
  </action>
  <verify>
    Run `make generate` to compile templates, and assert that the generated HTML has configurations submenu elements and script tags.
  </verify>
  <done>
    Left sidebar configurations accordion has transition transitions, stays open automatically on configurations sub-pages, highlights the active sub-item, and correctly uses context-based active path checks.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Layout Unification & Header Standardization</name>
  <files>
    templates/pages/workspaces.templ
    templates/pages/webhooks.templ
    templates/pages/telemetry.templ
    templates/pages/devices.templ
    templates/pages/audit.templ
  </files>
  <read_first>
    templates/pages/workspaces.templ
    templates/pages/webhooks.templ
    templates/pages/telemetry.templ
    templates/pages/devices.templ
    templates/pages/audit.templ
    .planning/phases/11-settings-sidebar-layout-unification/11-UI-SPEC.md
  </read_first>
  <action>
    Perform layout unification on configurations settings pages:
    - In `templates/pages/workspaces.templ`, remove the top tab switcher under `WorkspaceListContent` (per D-05). Change the header layout to standard panels with clean margins, title `Configurações do Workspace` and description `Gerencie workspaces, credenciais de integração e chaves de API.` (per D-06).
    - In `templates/pages/webhooks.templ`, remove the top tab switcher under `WebhooksContent` (per D-05). Update headers to standard style with title `Webhooks & DLQ` and description `Acompanhe falhas de entrega de webhooks de entrada e reenvios na Dead Letter Queue.` (per D-06).
    - In `templates/pages/telemetry.templ`, remove the top tab switcher under `TelemetryContent` (per D-05). Update headers to standard style with title `Telemetry` and description `Indicadores e performance em tempo real de hardware e rede.` (per D-06).
    - In `templates/pages/devices.templ`, update headers to standard style with title `Conexões` and description `Conecte e autentique seus canais de comunicação unificados.` (per D-06).
    - In `templates/pages/audit.templ`, update headers to standard style with title `Logs de Auditoria` and description `Monitore eventos de tráfego, status de envios e recebimentos.` (per D-06). Keep the inbound/outbound sub-tabs as they are log-specific filter tabs, not configuration tabs.
  </action>
  <verify>
    Run `make generate` to compile all templ files, check for compilation errors, and inspect modified templates for matching copywriting texts.
  </verify>
  <done>
    Top tab switchers are removed from Workspace, Webhooks, and Telemetry pages, and headers conform exactly to the copy and style specs in 11-UI-SPEC.md.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update Tests & Verification</name>
  <files>
    cmd/pergo/admin_test.go
  </files>
  <read_first>
    cmd/pergo/admin_test.go
  </read_first>
  <action>
    Review and update tests inside `cmd/pergo/admin_test.go` to match the new settings sidebar URLs and navigation paths:
    - Update assertions that check for `/admin/workspaces` or `/admin/audit` to expect the new `/admin/workspace` and `/admin/logs` submenu links in the sidebar body.
    - Ensure tests still succeed with the revised template structures.
    - Run the entire test suite `make test` and `make test-race`.
  </action>
  <verify>
    Run `make test` and `make test-race` to ensure all tests pass.
  </verify>
  <done>
    All unit and integration tests are updated, compile without errors, and pass successfully.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Collapsible settings configurations sidebar accordion, active route persistence on load, and unified settings layouts with top-tabs removed.</what-built>
  <how-to-verify>
    1. Run `make dev` to start the local development server.
    2. Log into the admin dashboard at `/admin/login`.
    3. Verify that the left sidebar displays "Configurações" with a dropdown chevron.
    4. Click "Configurações" and verify that it expands smoothly to reveal the five options: Conexões, Logs, Workspace, Webhooks, and Telemetry.
    5. Visit each configuration page and confirm that:
       - The configurations submenu remains expanded on load.
       - The current page item is highlighted.
       - Redundant top tabs are gone from Workspace, Webhooks, and Telemetry pages.
       - Headings and descriptions match the 11-UI-SPEC.md copywriting exactly.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Admin API | Untrusted input in workspace creation, webhook config, connection details, and credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | Admin Routes | High | Mitigate | Enforce SessionAuthMiddleware for all new route aliases and redirects to prevent unauthorized access. |
| T-11-02 | Info Disclosure | Templates | Medium | Mitigate | Ensure request path extraction logic does not leak system internal variables. |
| T-11-SC | Tampering | npm/pip/cargo installs | High | Mitigate | slopcheck + blocking human checkpoint |
</threat_model>

<verification>
We will verify that:
1. `make generate` compiles all templates without warnings or errors.
2. `make test` and `make test-race` return clean passes.
3. Automated route checking verifies `/admin/connections`, `/admin/logs`, and `/admin/workspace` return successful authenticated responses.
</verification>

<success_criteria>
1. Configurations submenu expands/collapses smoothly and shows the five options.
2. Left sidebar submenu automatically stays open and active on settings sub-pages load.
3. Old top tab menus are retired from Workspace, Webhooks, and Telemetry pages.
4. Spacing, headings, and descriptions conform exactly to the copy and style specs in 11-UI-SPEC.md.
</success_criteria>

<output>
Create `.planning/phases/11-settings-sidebar-layout-unification/11-01-SUMMARY.md` when done
</output>
