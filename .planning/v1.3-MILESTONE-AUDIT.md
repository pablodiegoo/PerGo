---
milestone: "1.3"
audited: "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
status: "gaps_found"
scores:
  requirements: "13/14"
  phases: "5/5"
  integration: "9/10"
  flows: "6/7"
gaps:
  requirements:
    - id: "TYPE-04"
      status: "unsatisfied"
      phase: "22-typebot-integration"
      claimed_by_plans: ["22-01-PLAN.md", "22-02-PLAN.md", "24.1-01-PLAN.md"]
      completed_by_plans: ["22-01-SUMMARY.md", "22-02-SUMMARY.md", "24.1-01-SUMMARY.md"]
      verification_status: "gaps_found"
      evidence: "TypebotForwarder constructs domain.QueueMessage but omits ConnectionID, SenderIdentity, and TraceID. This breaks downstream deduplication and dispatch."
  integration:
    - from: "internal/integration/typebot/forwarder.go:161"
      to: "messages.outbound (NATS)"
      issue: "domain.QueueMessage is constructed without ConnectionID, SenderIdentity, and TraceID."
  flows:
    - flow: "Flow 3: Customer message → Typebot"
      break_step: "TypebotForwarder publishing to messages.outbound"
      evidence: "Constructed domain.QueueMessage lacks required fields for outbound dispatch."
tech_debt:
  - phase: "24-refactor-webhook-verbs-engine-to-polymorphic-verbhandlers"
    items:
      - "Refactor commit 9944883 caused a regression by dropping Typebot re-wiring when replacing the old SetTypebotForwarder setter with the new router constructor. Fixed in 24.1, but needs a composition-root smoke test that verifies all integrations are wired at startup."
nyquist:
  compliant_phases: ["21-chatwoot-integration", "22-typebot-integration", "23-stateful-handoff-routing"]
  partial_phases: []
  missing_phases: ["24-refactor-webhook-verbs-engine-to-polymorphic-verbhandlers", "24.1-close-gap-wire-typebot-forwarder-and-reconcile-form-schema"]
  overall: "COMPLIANT (Nyquist validation strategy present for feature phases)"
---

# v1.3 Milestone Audit Report

**Milestone:** v1.3 (Chatwoot & Typebot Integrations)
**Audited:** $(date -u +%Y-%m-%d)
**Status:** gaps_found
**Overall Coverage:** 13/14 Requirements Met (92%)

---

## Executive Summary

PerGo v1.3 delivers the Chatwoot and Typebot integrations with a stateful human/bot handoff routing layer. Phase 24.1 successfully closed the Typebot wiring gap, reconciled the admin form schema, and added happy-path tests. 

**However, a new integration gap was found:** The `TypebotForwarder` successfully talks to Typebot but creates outbound messages that are missing critical fields (`ConnectionID`, `SenderIdentity`, `TraceID`). These are required by the queue orchestrator and downstream dispatchers.

**Recommendation:** Insert a closure phase to fix the `TypebotForwarder` message construction logic.

---

## 1. Phase Completion Status

All 5 phases (21, 22, 23, 24, 24.1) are complete.

| Phase | Directory | Per-Phase Status | Integration Status |
|-------|-----------|------------------|---------------------|
| 21 | `21-chatwoot-integration` | passed | passed |
| 22 | `22-typebot-integration` | passed | **gaps_found** |
| 23 | `23-stateful-handoff-routing` | passed | passed |
| 24 | `24-refactor-webhook-verbs-engine` | passed | passed |
| 24.1 | `24.1-close-gap-wire-typebot-forwarder...` | passed | **gaps_found** |

---

## 2. Requirements Verification

- **TYPE-01** (Typebot admin form): **satisfied** (fixed in 24.1)
- **TYPE-02** (Typebot webhook): **satisfied** (tests added in 24.1)
- **TYPE-04** (Typebot inbound forwarder): **UNSATISFIED** (integration gap in message struct fields)

*(Other Chatwoot and Handoff requirements remain satisfied.)*

---

## 3. Integration & E2E Flows

### Integration Gaps (Critical)

1. **Typebot forwarder message fields missing** — `internal/integration/typebot/forwarder.go:161` constructs `domain.QueueMessage` but omits `ConnectionID`, `SenderIdentity`, and `TraceID`. These fields must be populated so the dispatch adapters can send the messages.

