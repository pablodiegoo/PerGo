---
milestone: v1.7
audited: "2026-07-26T19:30:00Z"
status: tech_debt
scores:
  requirements: 21/22
  phases: 3/3
  integration: 4/4
  flows: 3/3
gaps:
  requirements:
    - id: "DISP-02"
      status: "partial"
      phase: "032-template-dispatch-validation-meta-flows"
      claimed_by_plans: ["032-01-PLAN.md"]
      completed_by_plans: ["032-01-SUMMARY.md"]
      verification_status: "partial"
      evidence: "Strict variable count checks dropped — relies on Meta API rejection for mismatched parameters. Template existence and APPROVED status are validated locally."
  integration: []
  flows: []
tech_debt:
  - phase: 032-template-dispatch-validation-meta-flows
    items:
      - "DISP-02: Strict template variable count validation deferred — Meta API rejects bad payloads, but local pre-flight doesn't count {{N}} placeholders in cached template body"
      - "FlowDataExchangeHandler test: DB coupling with pgxpool prevents robust unit testing — needs interface abstraction"
---

# Milestone v1.7 Audit Report

**Project:** PerGo
**Milestone:** v1.7
**Audited:** 2026-07-26
**Status:** ⚡ Tech Debt (no blockers, accumulated debt)

## Score Summary

| Dimension | Score | Status |
|-----------|-------|--------|
| Requirements | 21/22 | 1 partial |
| Phases | 3/3 | All complete |
| Integration | 4/4 | All wiring verified |
| E2E Flows | 3/3 | Template, Flow, Session all work |

## Requirements Coverage Matrix

### Phase 030 — Session Window & Inbound Foundation

| REQ-ID | Description | Status | Evidence |
|--------|-------------|--------|----------|
| SESS-01 | Tracks per-contact session timestamps | ✅ Satisfied | Migration added `entry_point_type` & `notified_expiring_at`, upsert tracking |
| SESS-02 | Rejects non-template msgs outside 24h (HTTP 422) | ✅ Satisfied | Maps `SessionWindowError` to HTTP 422 `SESSION_WINDOW_EXPIRED` |
| SESS-03 | WABA worker re-validates with 5m safety buffer | ✅ Satisfied | Dispatch-time validation in `WABAAdapter.Dispatch` with `5 * time.Minute` |
| SESS-04 | Emits `session.expiring_soon` webhook at 23h | ✅ Satisfied | `SessionTicker` background worker publishing to NATS |
| SESS-05 | Tracks 72h window for CTWA ads | ✅ Satisfied | Supports 72h window calculation for `entry_point_type == "ctwa"` |

### Phase 031 — Template CRUD & Meta Sync

| REQ-ID | Description | Status | Evidence |
|--------|-------------|--------|----------|
| TMPL-01 | Operator creates templates via REST API / UI | ✅ Satisfied | CRUD API and templated UI forms with Header, Body, Footer |
| TMPL-02 | Operator lists/views/edits templates with cache | ✅ Satisfied | In-memory cache in `WABATemplateRepository` |
| TMPL-03 | Syncs templates from Meta (rate-limited) | ✅ Satisfied | `WABAMetaClient` with 15-minute rate limit |
| TMPL-04 | Stores locale-specific variants | ✅ Satisfied | `GetByNameAndLanguage` query for language-specific dispatch |
| TMPL-05 | Tracks status via webhooks | ✅ Satisfied | Handles `message_template_status_update` events |
| TMPL-06 | Stores/displays rejection reasons | ✅ Satisfied | Column + UI inline display |
| TMPL-07 | Tracks quality score drops | ✅ Satisfied | Captures `message_template_quality_update`, logs GREEN→YELLOW/RED |
| TMPL-08 | Visual template previewer with parameters | ✅ Satisfied | `preview.templ` live chat bubble preview |
| TMPL-09 | Exposes REST API endpoints | ✅ Satisfied | `/api/v1/waba/templates` routes |

### Phase 032 — Template Dispatch, Validation & Meta Flows

| REQ-ID | Description | Status | Evidence |
|--------|-------------|--------|----------|
| DISP-01 | `POST /messages` parameter binding | ✅ Satisfied | Parameter normalization (array + map) in `Processor.Ingest` |
| DISP-02 | Validates template params before dispatch | ⚠️ Partial | Template existence + APPROVED status validated locally; strict variable count deferred to Meta API |
| DISP-03 | Blocks dispatch for `status != APPROVED` | ✅ Satisfied | Cache check returns `ErrTemplateNotApproved` |
| DISP-04 | Auto-upgrades freeform to default template | ✅ Satisfied | Smart session window fallback via connection credentials |
| FLOW-01 | Dispatch Meta Flows | ✅ Satisfied | `domain.Interactive` extended with Flow properties |
| FLOW-02 | Auto-parses inbound `nfm_reply` | ✅ Satisfied | Two-stage JSON decoding in webhook handler |
| FLOW-03 | Generates HMAC-signed `flow_token` | ✅ Satisfied | HMAC-SHA256 automatic token generation |
| FLOW-04 | Data Exchange endpoint with RSA/AES | ✅ Satisfied | Flow Crypto Utilities + `POST /api/v1/waba/flows/data-exchange` |

## Cross-Phase Integration Verification

| Integration Point | Status | Detail |
|-------------------|--------|--------|
| Session → Template fallback | ✅ | `processor.go` session window check seamlessly triggers default template upgrade |
| Template cache → Validation | ✅ | `WABATemplateRepository` cache feeds pre-flight validation in `Processor.Ingest` |
| Webhook → Template status | ✅ | Template quality/status webhooks update cache; inbound nfm_reply decodes flows |
| Handler → Error mapping | ✅ | `message.go` maps `SessionWindowError`, `ErrTemplateNotFound`, `ErrTemplateNotApproved` to HTTP 422 |

## Tech Debt

### Phase 032: Template Dispatch, Validation & Meta Flows
1. **DISP-02 variable count validation** — Strict `{{N}}` placeholder counting from cached template body was deferred. Meta API rejects bad payloads server-side, so this is defense-in-depth, not a functional gap.
2. **FlowDataExchangeHandler test coupling** — Handler directly depends on `pgxpool.Pool`, making unit tests require mocks or skips. Needs interface abstraction for testability.

**Total:** 2 items across 1 phase
