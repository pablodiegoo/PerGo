# Plan: WABA Default Templates for Test/New Messages (260716-v4e)

When a user sends a new message or test message, and the channel is WABA, default to using templates.

## Must-Haves
- `NewChatModal` handles pre-selection of `whatsapp_cloud` (WABA Cloud) and hides/shows template and message fields accordingly on load.
- `TestConnectionModal` receives WABA templates when connection is WABA, and renders template select and parameters form instead of plaintext body textarea.
- `DeviceHandler.RunTest` endpoint supports extracting and publishing template name and parameters when `is_template` is true.

## Tasks

### Task 1: Update NewChatModal in templates/components/new_chat_modal.templ
- File: [templates/components/new_chat_modal.templ](file:///home/pablo/Coding/PerGo/templates/components/new_chat_modal.templ)
- Action:
  - Add `selected?={ channel == "..." }` attributes to the select options.
  - Set hidden input `new-chat-is-template` value conditionally.
  - Set initial CSS class `hidden` dynamically on `#new-chat-message-input` and `#new-chat-waba-input` based on the value of `channel`.
- Verify: Render "Novo Chat" with channel pre-selection.

### Task 2: Update TestConnectionModal and RunTest in templates/pages/devices.templ and internal/api/handler/admin/device.go
- Files:
  - [templates/pages/devices.templ](file:///home/pablo/Coding/PerGo/templates/pages/devices.templ)
  - [internal/api/handler/admin/device.go](file:///home/pablo/Coding/PerGo/internal/api/handler/admin/device.go)
- Action:
  - Update `TestConnectionModal` signature to accept `templates []repository.WABATemplate`.
  - In `TestConnectionModal`, conditionally show a template name select and variables preview for WABA connections.
  - Add `showTestTemplatePreview` JS function helper.
  - In `DeviceHandler.TestForm`, query and pass templates to the modal when channel is WABA.
  - In `DeviceHandler.RunTest`, detect `is_template` and build the structured template payload for the outbound queue message.
- Verify: Load test connection modal for WABA connection, select template, and submit.
