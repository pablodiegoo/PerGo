---
quick_id: "260715-f3q"
slug: "review-and-update-gitattributes-and-giti"
type: quick
status: executing
---

# Plan: Review and Update Gitattributes and Gitignore for Agent Files

## Objective
Configure `.gitattributes` with `export-ignore` for agent configurations, metadata, and planning files to prevent them from being included in released packages/archives of PerGo. Configure `.gitignore` to ignore local agent run outputs (like `graphify-out/`) and lockfiles that are not direct product dependencies.

## Tasks
1. **Task 1: Update `.gitattributes` ([.gitattributes](file:///home/pablo/Coding/OmniGo/.gitattributes))**
   - Append `export-ignore` to `.agents/**`, `.opencode/**`, `.planning/**`, `AGENTS.md`, `CONTEXT.md`, `GEMINI.md`.
2. **Task 2: Update `.gitignore` ([.gitignore](file:///home/pablo/Coding/OmniGo/.gitignore))**
   - Add `/graphify-out/` and `/skills-lock.json` to prevent local agent tools state from tracking in git.
3. **Task 3: Verification**
   - Run `git status` to ensure only correct files are tracked.
   - Run a test git archive command to verify files are successfully ignored during packaging.
